# سجل التغييرات

## 2026-07-14 — BATCH-20260714-AUDIT-0001 — تدقيق شامل وإنشاء طبقة الذاكرة

### طبقة الذاكرة الدائمة

- إنشاء `.legalmind/` كذاكرة المشروع. **المحادثة لم تعد ذاكرة.**
- 17 وثيقة حاكمة: `PROJECT`، `ARCHITECTURE`، `LEGAL_ONTOLOGY`، `KNOWLEDGE_MODEL`، `CASE_ENGINE`، `DRAFTING_ENGINE`، `INGESTION_ENGINE`، `RETRIEVAL_ENGINE`، `REASONING_ENGINE`، `DATABASE_RULES`، `CODING_STANDARDS`، `UI_GUIDELINES`، `TESTING`، `DEPLOYMENT`، `GIT_WORKFLOW`، `CHANGE_POLICY`، `RESUME_STATE`.
- إنشاء `.legalmind/TASKS/0001.md` — خطة إكمال فرع الأحوال الشخصية (19 مهمة، 4 مراحل).
- إنشاء `.legalmind/CURRENT_PROJECT_AUDIT.md` — تدقيق كامل بأدلة مُتحقَّقة.

### نتائج التدقيق — تصحيح ادعاءات جوهرية 🔴

- **سحب الرقم 869**: `legislation_objects_generated: 869` **رقم وهمي**. لا توجد ولا ملف `.jsonl` ولا `KW-*.index.json` في المستودع. العدد الحقيقي **صفر**.
- **المعرفة غير محقونة**: الـ 23 مبدأ والـ 6 قواعد الجامعة موجودة **كـ Markdown فقط**. قاعدة البيانات تحتوي على **كائنين تجريبيين** (`branch = اختبار`) لا غير.
- **CI أحمر**: `validate_knowledge.py` يخرج بـ exit 1 على `main` ولم يُلاحَظ. **المدقق سليم** — هو من كشف الرقم الوهمي.
- **`hash_embedding` ليس تضمينًا دلاليًا**: هو *hashing trick* على SHA-256. المرادفات القانونية العربية لا تُسترجع. البحث الدلالي **مستحيل** بالبنية الحالية.
- **لا محرك استرجاع**: لا يوجد أي كود يقرأ من Qdrant. Qdrant مخزن للكتابة فقط.
- **لا محرك صياغة**: «استوديو الصياغة» في الواجهة مُنتقي قضايا بلا توليد.
- **«البحث» في الواجهة ليس بحثًا**: `Array.filter()` على بيانات مُحمَّلة في المتصفح.
- **النسخ الاحتياطي معطّل**: `legalmind-backup.service` فاشلة بـ `203/EXEC` — `deploy/backup.sh` صلاحياته 644 (غير تنفيذي). **لا توجد نسخة احتياطية واحدة.**

### إصلاحات

- إضافة `.gitignore` — المستودع كان بلا `.gitignore`، و`deploy/.env` (يحتوي كلمة مرور قاعدة البيانات) كان معرّضًا للالتزام سهوًا.

### ما يعمل فعلًا ✅

- البنية التحتية سليمة: PostgreSQL 16 + Qdrant v1.14.1 + ثلاث خدمات نشطة.
- خط الإدخال **مُثبت من طرف إلى طرف** (دفعتان مكتملتان).
- **بوابة السند** (`case_coverage`) مُنفَّذة فعليًا — أثمن ما في المشروع.
- الأنطولوجيا v2.0 عمل فكري جاد (452 سطرًا).

### القاعدة المستفادة

> **الصدق قبل البناء.** مشروع يظن أن لديه 869 مادة و23 مبدأً في قاعدة بياناته، بينما لديه صفر، سيبني محرك صياغة فوق الفراغ — وينتج مذكرة قانونية بأسانيد غير موجودة، تُقدَّم إلى محكمة.

---

## 2026-07-14 — BATCH-20260714-WEB-0001

- إنشاء واجهة LegalMind 3.0 باستخدام Next.js وReact وTailwind.
- إضافة لوحة معلومات حديثة وإحصاءات لحظية للمصادر والدفعات.
- إضافة صفحة رفع متعددة الملفات مرتبطة مباشرة بمحرك الإدخال الحالي.
- إضافة شجرة معرفة موضوعية بحسب الفرع والموضوع وعنوان تصنيف محكمة التمييز والمسألة الدقيقة.
- إضافة صفحات الكائنات القانونية ودفعات المعالجة والرسم المعرفي والقضية الجديدة.
- إبقاء ميزة الصياغة النهائية مقفلة حتى اكتمال الأحكام والنماذج اللازمة، منعًا لإنتاج مستند ناقص الأسانيد.
- إضافة بناء static export ونشر آمن عبر Nginx مع HTTP Basic وHTTPS القائم.
- إضافة سكربت `deploy/install_legalmind_v3.sh` للتثبيت والتحديث بأمر واحد.

## 2026-07-13 — BATCH-20260713-ADMIN-0001

- إنشاء بوابة إدارة LegalMind على `admin.soutaladalah.com`.
- إضافة لوحة معلومات لأعداد التشريعات والمبادئ والأحكام والنماذج والدفعات.
- إضافة صفحة رفع متعددة الملفات مع اختيار الفرع والموضوع وعنوان تصنيف محكمة التمييز والمسألة الدقيقة.
- دعم مصادر: الحكم الكامل، مجموعة المبادئ المختصرة، المبدأ المنفرد، التشريع، الصيغة القضائية، ومذكرة الدفاع.
- إنشاء ملف Metadata جانبي لكل ملف مرفوع حتى يعالجه محرك الإدخال وفق التصنيف الموضوعي المحدد.
- إضافة صفحات التصنيف الموضوعي، دفعات المعالجة، والكائنات القانونية.
- حماية البوابة بمصادقة HTTP Basic وكلمة مرور مستقلة محفوظة على الخادم.
- إضافة خدمة systemd وتهيئة Nginx وسكربت تثبيت بأمر واحد.
- تحديث محرك الإدخال لتمييز الأحكام الكاملة عن مجموعات المبادئ وعدم اختلاق وقائع أو تسبيب للمبادئ المختصرة.

## 2026-07-13 — BATCH-20260713-PS-ONTOLOGY-0002

- ترقية `ONTOLOGY.md` إلى الإصدار 2.0.
- إضافة أنواع الكائنات الموضوعية والإجرائية والصياغية والواقعية والتوثيقية.
- إضافة العلاقات الدلالية والإجرائية والزمنية اللازمة لبناء Knowledge Graph.
- إضافة استراتيجية باب الدخول بحسب نوع السؤال: موضوعي، إجرائي، صياغي.
- إضافة قواعد تحديد القانون الحاكم والمذهب ومنع خلط القواعد السنية والجعفرية.
- إضافة نمط موحد للقواعد القابلة للاستدلال وسلسلة الاستدلال الإلزامية.
- إضافة مخطط JSON آلي: `ontology/personal-status.graph.schema.json`.
- إضافة قواعد استدلال قابلة للتنفيذ: `ontology/personal-status.reasoning-rules.yaml`.
- إصلاح `validate_knowledge.py` لقراءة `total_legislation_objects` مع التوافق الرجعي مع `total_articles`.
- إضافة فشل صريح عند غياب فهارس التشريعات بدل إعلان نجاح صفري مضلل.

## 2026-07-13 — BATCH-20260713-PS-LEG-0001

- إضافة خط معالجة تشريعات قابل للتشغيل يدعم DOCX وTXT وMarkdown.
- تسجيل أربعة مصادر تشريعية لفرع الأحوال الشخصية: 51/1984، 124/2019، 12/2015، 53/2026.
- إنشاء فهرس تشريعي يميز القانون الموضوعي والإجرائي والإجرائي الخاص.
- إنشاء استراتيجية باب الدخول بحسب نوع السؤال.
- إنشاء خريطة علاقات أولية بين التشريعات ومبادئ «اختصاص دائرة الأحوال الشخصية».
- وسم المبادئ المتعلقة بالنسب وتصحيح الأسماء لإعادة التقييم بعد قانون 53/2026.
- وسم مبادئ مسكن الحضانة والجوازات لإعادة التقييم بعد قانون محكمة الأسرة 12/2015.
- توثيق ضوابط الفصل بين المصدر غير القابل للتغيير وطبقة المعرفة.

## 2026-07-13 — BATCH-20260713-PS-0001

- إضافة `ONTOLOGY.md` و`WORKFLOW.md` و`RETRIEVAL_ENGINE.md` و`DRAFTING_ENGINE.md` و`VALIDATION.md` و`REASONING_TRACE.md`.
- حفظ المصدر الأصلي لمجموعة «اختصاص دائرة الأحوال الشخصية».
- معالجة 23 مبدأً قضائيًا بمعرفات ثابتة.
- إنشاء تصنيف موضوعي ومسائل دقيقة وتصنيفات ثانوية.
- إنشاء 6 قواعد جامعة بحالة `machine_pending_human`.
- تحديث ملف فرع الأحوال الشخصية وفهرس الكائنات وطابور التوثيق.
- تسجيل 4 مسائل توثيق ومراجعة.

## 2026-07-13 — INITIAL
- إنشاء المواصفة الرئيسية.
- إنشاء بروتوكول الحفظ والاستعادة.
- إنشاء مخطط الكائنات.
- إنشاء قواعد الاسترجاع والربط.
- إنشاء ملفات الفروع والفهرس وطابور التوثيق وحالة الاستئناف.

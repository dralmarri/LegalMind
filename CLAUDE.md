# LegalMind V4 — توثيق المنظومة

> نظام تشغيل قانوني للمحامي الكويتي: قاعدة معرفة قانونية (تشريعات، مبادئ تمييز، أحكام كاملة، صيغ قضائية) + بحث دلالي + استوديو صياغة واستشارات مستند بالمصادر.
> آخر تحديث: 2026-08-06. الخادم: srv1184869 (Hostinger VPS, 72.61.237.80).

---

## 1) البنية التحتية

| المكوّن | التفاصيل |
|---------|----------|
| تطبيق الأدمن (FastAPI) | خدمة `legalmind-admin` على **127.0.0.1:8088** — الملف `/opt/LegalMind/admin/app.py` |
| خدمة API قديمة | `legalmind-api` على 8000 (لا تلمسها — ليست خلفية الموقع) |
| محرك الإدخال | خدمة `legalmind-ingest` — يراقب `/opt/legalmind-ingest/inbox/` كل 10 ثوانٍ |
| الواجهة | Next.js 15 **static export** — المصدر `/opt/LegalMind/web/`، يُنشر إلى `/var/www/legalmind-v3/` |
| nginx | `/etc/nginx/sites-available/legalmind-admin` — يمرر `/api/` إلى 8088 (مهلة 1200s منذ `.bak_hb`) ويخدم الواجهة؛ `client_max_body_size 100m` |
| PostgreSQL | **127.0.0.1:55432** — قاعدة `legalmind` (الاتصال في `deploy/.env` → `DATABASE_URL`) |
| Qdrant | 127.0.0.1:6333 — المجموعة `legalmind_multilingual_e5_base_v1` (768 بعدًا) |
| نموذج التضمين | `intfloat/multilingual-e5-base` |

**بيئتان بايثون — لا تخلط بينهما:**
- `/opt/LegalMind/.venv` — الثقيلة (فيها sentence_transformers): للمحرك والفهرسة والتضمين.
- `/opt/LegalMind/admin/.venv` — الخفيفة (fastapi, psycopg, anthropic): لتطبيق الأدمن فقط.

**مصادقة مزدوجة:** nginx htpasswd + مصادقة التطبيق (scrypt) — المستخدم `dralmarri` في كليهما.

---

## 2) قاعدة المعرفة

> **تنبيه (2026-08-11):** الأعداد والقوائم في هذا القسم لقطة تاريخية من بدايات البناء —
> القاعدة اليوم **58,402 كائنًا** و**92 تشريعًا ولائحة** عبر كل الفروع (منها العمالي كاملًا:
> 6/2010 و28/1969 و68/2015). **الجرد الحي الكامل في `docs/legislation_inventory.md`** —
> يُراجع هو أولًا قبل أي حكم بوجود قانون أو غيابه، وقبل سكّ أي معرف جديد.

(القوائم التاريخية أدناه — 2581 كائنًا وقت كتابتها:)

| object_type | العدد | الوصف |
|---|---:|---|
| `judicial_principle` | 1627 | مبادئ تمييز (1616 من مجموعات المبادئ + 11 مستخرجة من أحكام كاملة) |
| `legislation_article` | 898 | مواد 4 قوانين |
| `judicial_template` | 19 | صيغ قضائية مُعقَّمة — **غير قابلة للاستشهاد** |
| `full_judgment` | 17 | أحكام تمييز كاملة (أحوال شخصية) |
| `legislation_issuing_article` | 16 | مواد الإصدار |
| `legislation_preamble` | 4 | الديباجات |

**الفروع:** `أحوال شخصية` (السنّي والإجرائي) و`أحوال شخصية جعفرية` (قانون 124/2019 وصيغه). فرع `إداري` محجوز للمرحلة القادمة.

**التشريعات المدخلة:**
1. **ق51/1984 الأحوال الشخصية** (سنّي): ديباجة + مواد 1–347 + 4 مكرر. التصنيف: topic=القسم، subtopic=الباب، micro_issue=الفصل؛ المسار الكامل في `metadata.hierarchy`. معرفات: `legis-51-1984-mN`.
2. **ق124/2019 الجعفري**: ديباجة + 6 مواد إصدار + مواد 1–510. topic=الكتاب، subtopic=الفصل، micro_issue=الباب. معرفات: `legis-124-2019-*`.
3. **ق12/2015 محكمة الأسرة** (النص **المعدَّل** — فروق النسخة الأصلية 2015 في المواد 1، 8، 17 موثقة في `metadata.version_note`): 17 مادة + 5 إصدار. معرفات: `legis-12-2015-*`.
4. **مرسوم بقانون 53/2026 دعاوى النسب وتصحيح الأسماء** (ألغى ق10/2010): 20 مادة في 7 فصول + 5 إصدار. معرفات: `legis-53-2026-*`.

**تصحيحات موثقة (كلها في metadata):** م21 سنّي استُكملت من e.gov.kw؛ مواد المواريث الجعفرية 440–452 كانت مفقودة من الملف واستُكملت من النص الرسمي؛ م11 محكمة الأسرة: مرجع التظلم صُحح إلى (164) مرافعات بدل (146) بعد التحقق من الجريدة الرسمية ع1228؛ حكم 439 جعفري نُظف من ذيل دخيل.

**قواعد أمانة ملزمة:**
- النص الأصلي مقدس؛ أي تصحيح يُوثق في `metadata.source_correction`.
- الصيغ (`judicial_template`): `authority_status=non_authoritative`, `usable_as_citation=false`, مُعقَّمة من البيانات الشخصية (أسماء/أرقام مدنية/عناوين/هواتف → عناصر نائبة `[…]`).
- الأحكام الكاملة: أسماء الأطراف مُخفاة، أسماء القضاة باقية؛ المبادئ المستخرجة مربوطة بحكمها عبر `metadata.source_judgment_id`.

---

## 3) خطوط الإدخال

**أ) صندوق الوارد (الأساسي):** ملفا `X.md` (نص المبادئ، تُفصل بنمط `رقم-`) + `X.md.json` (الوصف: branch/topic/subtopic/…) في `/opt/legalmind-ingest/inbox/` — المحرك يلتقطها خلال ~10 ثوانٍ، يزيل التكرار ببصمة SHA-256 للنص المطبَّع، والإدراج آمن للتكرار (`ON CONFLICT (id) DO UPDATE`).

**ب) الرفع الهرمي من المتصفح:** صفحة «رفع مستند هرمي» — DOCX ← معاينة شجرة ← موافقة ← إدخال (المعالج `/opt/LegalMind/tools/kpkg_docx.py` + `/api/docx/*`).

**ج) الإدراج المباشر (تشريعات/أحكام/صيغ):** سكربتات لصق بنمط ثابت: بيانات JSON مضغوطة gzip+base64 + مُدرج بايثون يعمل بالبيئة الثقيلة ويستورد `normalize_text` من المحرك. القالب المرجعي: `/tmp/ingest_tpl.py` على الخادم (يدعم `usable_as_citation`).

**قاعدة الجلسات:** أدخل دفعاتك كلها ثم **فهرسة واحدة** في النهاية.

---

## 4) الفهرسة والاتساق

PostgreSQL مصدر الحقيقة؛ Qdrant فهرس مشتق يُحذف ويُبنى بالكامل:

```bash
cd /opt/LegalMind && set -a; . deploy/.env; set +a; \
HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1 \
/opt/LegalMind/.venv/bin/python -m engine.legalmind_engine reindex
```

- `HF_HUB_OFFLINE=1` **ضروري** — يتخطى تعليق HuggingFace (أخطاء 504).
- النجاح = `"consistent": true` والعددان متطابقان (حاليًا 2581 = 2581).
- تلميح: معرف النقطة يتضمن بصمة محتوى (إصلاح تاريخي لتصادم أفقدَ 233 مبدأ — لا تُعِد القص إلى المعرف القديم).

---

## 5) واجهات API (كلها خلف مصادقة)

| المسار | الوظيفة |
|--------|---------|
| `GET /api/stats` | العدادات (الواجهة تجمع مفاتيح `legislation*` لمربع التشريعات) |
| `GET /api/search?q=&k=` | بحث دلالي: تضمين عبر `engine/embed_query_cli.py` (subprocess بالبيئة الثقيلة) ← Qdrant ← نصوص من PG |
| `POST /api/docx/preview` ، `/api/docx/ingest` | الرفع الهرمي |
| `POST /api/draft` | **استوديو الصياغة** (انظر §6) |

---

## 6) استوديو الصياغة `/api/draft`

**السير:** الطلب (نوع + وقائع + مذهب) ← **Haiku 4.5** يفكّكه إلى 3–6 محاور بحث ← تضمين كل المحاور دفعة واحدة (`engine/embed_queries_multi.py` — تحميل واحد للنموذج) ← بحث Qdrant لكل محور (تشريعات 8 + مبادئ 8؛ والاستعلام الكامل 10+10 وحكمان ونموذجان) ← دمج بالنقطة الأعلى وقصّ (20 تشريعًا + 20 مبدأً + 3 + 3) ← **مُحلّ الإحالات الصريحة** (انظر أدناه) ← حزم الفصول الحاكمة + بحث معجمي + توسعة إحالات `_XREF` ← جلب النصوص من PG بميزانيات لكل نوع (تشريع 40k + مبادئ 24k + 8k + 5k حرفًا) ← **Claude Fable 5** (`claude-fable-5`) عبر **بث متدفق** (`_stream_create` — يتجاوز حد الدقائق العشر، `max_tokens=24000`) مع **تراجع خادمي تلقائي إلى Opus 4.8** عند رفض الأمان ← مخرج بأربعة أقسام: الفحص الإجرائي / سيناريوهات النجاح والفشل / التنفيذ / حدود المصادر.

**مُحلّ الإحالات الصريحة (`_draft_direct_ids` — أضيف 2026-08-04):** بحث هجين يسبق الدلالي: يستخرج من السؤال كل قانون مذكور بالرقم (`_LAWREF_RE`: «رقم N لسنة YYYY» أو «N/YYYY») **أو بالاسم الشائع** (جدول `_LAW_ALIASES`: «تستر»←78/2026، «سجل عيني»←21/2019، «تسجيل عقاري»←5/1959، «دستور»، «قانون التجارة»، «مرافعات»… 17 مدخلًا)، ثم يجلب من كل قانون: المواد المرقمة صراحة في السؤال (`_ARTNUM_RE`) + **المادة الأخيرة حتمًا** (مادة النفاذ — ضمانة `.bak_hybfix2` بعد رصد تقلب محاور Haiku بين الجولات) + حتى 7 مواد تطابق عناوينُها كلمات السؤال بمرادفات مستهدفة (`_DIRECT_SYN`: سريان↔نفاذ↔تنفيذ؛ المذكرات `-memo-` مستبعدة). المخرج (≤24 معرفًا) يدخل السياق بدرجة 0.99 فيتصدره ولا يُقص. سطر `[draft] direct_extra:` في يوميات الخدمة يكشف ما جلبه كل طلب: `journalctl -u legalmind-admin | grep -E "direct_extra|rerank"`.

**ضبط استرجاع المدني والمرافعات (2026-08-07 — `.bak_artfix`):** بأمر المالك («يجب ضبط
الاسترجاع تمامًا واجعله أولوية») بعد رصده تكرار غياب مواد المدني والمرافعات رغم وجودها في
القاعدة. سببان مشخَّصان: (1) `_ARTNUM_RE`/`_AUD_ART_RE` يلتقطان **رقمًا واحدًا** من قائمة
(«المواد 57، 61، 63، 196») لأن الأرقام التالية بلا بادئة — عولج بـ`_expand_art_lists`
(`_ARTSEQ_RE`) الذي يعيد كتابة القائمة فيصير كل رقم مادةً مستقلة، ويُطبَّق على مدخل المحلّ
وعلى نص الإجابة في المدقق معًا؛ (2) **أوراق القضية لم تكن مصدر إحالات**: المحلّ يقرأ السؤال
المكتوب فقط بينما المذكرات تحيل لمواد بأرقامها — عولج بقناة `_draft_doc_refs` (نداء Haiku
مستقل على حتى 4 مستندات يُخرج JSON بـ{law, art}، تُطابَق أسماؤها بـ`_LAW_ALIASES_TBL` أو
`_LAWREF_RE`، وتُتحقق من وجودها في القاعدة قبل الحقن). ومعهما مختصرات شائعة («مدني»،
«اثبات») ذُيِّلت بالجدول فتفوز الصريحة أولًا، وسقف القوانين 6←8. اليوميات: `[draft] doc-refs:`.
اختبار مدمج على مواد القضية الغائبة فعليًا (57/61/63/196/197/703/711/715 مدني + 119/46
مرافعات) = `ARTFIX_PASS` بلا غائب. **درس مرساة:** سطر `blob = _draft_norm_ar(...)` مكرر في
المحلّ والفصول معًا — شرط التفرد أوقف الرقعة، والمرساة الصحيحة `prefixes = []`.
**مديات المواد (`.bak_rngfix`):** `_expand_art_ranges`/`_ARTRNG_RE` تفكّ «المواد **من 56 إلى
61**» إلى كل رقم على حدة (سقف المدى 12) — والإحالات التشريعية نفسها تُكتب بالمدى (م715 مدني
تحيل حرفيًا للمواد 56–61)، فكان يُجلب طرفا المدى ويسقط ما بينهما (م59 وم60 أُعلن غيابهما وهما
في القاعدة). `RNGFIX_PASS`.
**تنقية ضجيج القناة المباشرة (`.bak_noisefix5`):** فرع مطابقة عناوين المواد كان يقبل تطابقًا
جزئيًا بثلاثة أحرف ثم **يرتّب المتساوين أبجديًا بالمعرّف** فتتصدّر `m1/m10/m100/m1000/m1001`
بأعلى أولوية (0.99) وتزاحم النافع على ميزانية السياق. ثلاث قواعد: (1) لا تدخل مادة إلا
بكلمة كاملة معتبرة (≥4 أحرف) مشتركة، والجزئي للترتيب فقط؛ (2) الترتيب رقمي بالمادة بعد قوة
المطابقة، والحصة 7←5 لكل قانون؛ (3) **كلمات اسم القانون تُسقط من المطابقة** — عناوين المواد
صيغتها «المادة N — اسم القانون» فتتساوى كل مواد القانون على كلمة «المدني» وتدخل م1 وم2 وم3.
`NOISEFIX5_PASS` بصفر ضجيج، واعتُمدت الجولة **ببطاريتين متتاليتين 11/11**.
**الضرب الديكارتي — أُغلق (2026-08-07، `.bak_cartfix`):** كان `cand = [p + "m" + n for p in
prefixes for n in artnums]` يضرب كل رقم في كل قانون مذكور، فسؤال عن «المواد 56–61 مدني
والمادة 40 إثبات» يجلب أيضًا م56–م61 إثبات وم40 مدني بأعلى أولوية فتزاحم النافع. العلاج:
`_pair_art_ids` بالمنطق نفسه المطبَّق في المدقق (أقرب اسم قانون مع ترجيح ما يليه)، ومعه
**صمّام حفظ الاسترجاع**: الرقم الذي لا يجد قانونًا قريبًا يبقى مضروبًا كما كان. ودالة
`_law_spans_of` واستثناءات `_PAIR_SKIP` (طعن/دعوى/سند/تاريخ…) صارت مشتركة بين الموضعين.
`CARTFIX_PASS`: المجلوب 24←14 مع حضور المواد النافعة كلها وصفر ديكارتي.
**درس معياري في الاختبارات:** ثلاث محاولات سقطت بسبب **معيار الاختبار لا الرقعة** (قائمة أرقام
محفوظة بدل قاعدة عامة): `م1082` مادة نفاذ مقصودة و`م40 مدني` ضرب ديكارتي، وكلاهما صُنِّف ضجيجًا
خطأً. المعيار يُبنى **مصنِّفًا عامًا** (نفاذ/ديكارتي/ضجيج) يُحتسب من البيانات، لا قائمة ثابتة.
وتصحيح معيار خاطئ مشروع، أما تخفيفه لتمرير رقعة فيُفسد قيمة البوابة.

**توسعة الإحالات الداخلية (2026-08-04، `.bak_sibxref`):** `_sib_expand` — كل مادة تشريعية مسترجَعة يُفحص نصها (أول 4000 حرف) عن إحالات لمواد شقيقة في قانونها («المنصوص عليها في المادة (8)») فتُجلب بدرجة 0.55 قبل المرتِّب فتخضع لوزنه؛ نوافذ الإحالة الحاوية «قانون/مرسوم/لائح» مستبعدة (الخارجية شأن `_XREF`)، والسقف 12. اليوميات: `[draft] siblings:`. ومعها قاعدة موجّه: نقل كلمة فرع المبدأ في سنده حرفيًا دون استنتاج.

**المرتِّب المتقاطع (2026-08-04، `.bak_rerank`):** `engine/rerank_cli.py` بنموذج `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` (محفوظ في كاش HF محليًا؛ الاستدعاء بـ`HF_HUB_OFFLINE=1`) — بعد جلب النصوص تُمرر كل مرشحات التشريعات والمبادئ ذات الدرجة <0.99 (سقف 150 زوجًا، 1200 حرفًا للنص) والدرجة النهائية = ½ متجه + ½ سيغمويد المرتِّب؛ مواد المحلّل (0.99) لا تُمس، وأي فشل يرجع `{}` فلا أثر. زمن الاستدعاء ~11ث (تحميل+ترتيب 150). القياس: جلب مبادئ لم يصل إليها المتجه قط (108/2012 التصالح الجزائي، 1067/2006، الطعنان 7-8/2005 «التسجيل لا يصحح العقود الباطلة»).

**قواعد الموجّه:** استشهاد حصري من السياق المسترجَع؛ لا اختلاق مواد أو طعون؛ إفصاح صريح عند نقص المصادر؛ تمييز المذهبين؛ فحص الإجراءات السابقة الوجوبية (مركز التسوية م9 ق12/2015، لجنة النسب 53/2026، تحكيم الضرر 127–132).

**الإعداد:** `ANTHROPIC_API_KEY` في `/opt/LegalMind/deploy/.env` (يُقرأ عند كل طلب — لا حاجة لإعادة تشغيل عند تغييره؛ سطر واحد فقط!). تبديل النموذج: متغير البيئة `LEGALMIND_DRAFT_MODEL`. الكود بين علامتي `# ==== استوديو الصياغة` و`# ==== نهاية استوديو الصياغة` آخر `app.py` (نسخ احتياطية متدرجة: `app.py.bak_draft*` ثم `.bak_maxtok` ← `.bak_streamfix` ← `.bak_hybrid` ← `.bak_hybfix` ← `.bak_alias`). زمن الطلب 2–6 دقائق (مهلة nginx 1200s).

**تدقيق الاستشهادات (2026-08-04):** خمس جولات قبول على سؤال مركّب أثبتت **صفر اختلاق** — كل مبدأ ومادة واقتباس تتبعناه وُجد له سجل مطابق (بما فيها الذيول المبتورة «الطعن ؟» تُنقل بأمانة). فجوات الاسترجاع (لا الاختلاق) هي التي عولجت بالمُحلّ أعلاه. وجولة قضية «منع السفر بأمر أداء» (2026-08-05): 27/28 استشهادًا مؤكدًا حرفيًا والأخير سجله حقيقي — انظر منظومة تضييق الفجوة أدناه.

**منظومة تضييق فجوة الاسترجاع (2026-08-05 — `.bak_chap*` ← `.bak_selffix` ← `.bak_tune` ← `.bak_lex`):** ثلاث طبقات تغلق فئات الفجوات لا أمثلتها:
1. **الحزم الحاكمة الآلية (`_draft_chap_index`/`_draft_chap_ids`):** مسرد يُبنى مرة واحدة من تصنيف مواد كل التشريعات (topic/subtopic/micro مع تردد كل كلمة عبر التسميات)، فمتى ورد في السؤال لفظ أداة إجرائية («أمر أداء»، «ممنوع من السفر»، «شيك») جُلب فصلها الحاكم حتمًا بدرجة 0.99 دون حاجة لتسمية القانون. قواعد المطابقة المعيارية: تجذيع «ال» كاملةً (لا `lstrip` حرفيًا!) + هيكل صرفي بإسقاط حروف العلة (امر=اوامر) + كلمات قانونية «ضعيفة» (عقد/حكم/دعوى…) تدعم النسبة ولا تفتح المطابقة + قبول الأغلبية (نصف الكلمات وكلمتان) بشرط كلمة نادرة (تردد≤4) قوية + الأحادية بندرة متوسطة (≤12) + حروف الجر تُسقط من الجهتين + الفصل الأحادي الضخم (الشيك ~95) يُشذّب (صدر 12+ذيل 6) لا يُسقط. اليوميات: `[draft] chap_extra:`.
2. **طبقة المدقق (`_audit_answer` — حلّت محل جولة التصحيح الذاتي وابتلعتها، 2026-08-06، `.bak_audit*`):** تدقيق مستقل لكل استشهاد في المسودة لا ينتظر إعلان النموذج: كل مادة (باقتران رقمها بأقرب قانون في نافذتها ±90 عبر `_LAW_ALIASES_TBL` المرفوع لمستوى الوحدة + `_LAWREF_RE`) تُصنف: في السياق ✓ / في القاعدة لا في السياق ← تُجلب / لا وجود لها ← «شبهة» تُعلَّم («احذفها أو ضع [للتحقق]») — وكل طعن يُفحص وجوده بصيغتيه — و«إعلان الغياب الكاذب» (مادة معلَنة الغياب وهي في السياق، بنافذة مقصوصة عند حد الجملة) يولّد تنبيه مراجعة. تقرير ثلاثي بجولة تنقيح واحدة. اليوميات: `[draft] audit:` وحقول الرد `audit_flags`/`audit_incontext`. **جولة اعتماد حية (2026-08-06):** المدقق جلب م297 مرافعات وم32 مدني وكشف إعلان غياب كاذبًا لم298 فخرجت الإجابة تقتبس النصوص حرفيًا؛ وتدقيق خارجي لكل استشهاداتها = صفر اختلاق (والقياس المعلَن «قياسًا على» مقبول). إصلاح `.bak_pairfix`: رقم الطعن المسبوق بـ«طعن» لا يُقرأ رقم قانون في الاقتران. **اختبار أعمى بملف قضية حقيقي (2026-08-06):** قراءة PDF ممسوح (مذكرة دفاع) دقيقة بتصديق المالك مع التقاط تناقض داخلي برقم الوكالة، والمواد 9/9، ورفضٌ صريح لتقرير ما ليس في الأوراق؛ **وطعنان مختلقان أفلتا لأن «246 / 1987» بمسافات حول الشرطة أعمت `_AUD_APP_RE`** — أُصلحت (`.bak_audfix`: `/\s*`). قاعدة معيارية: كل صيغ ترقيم الطعون (مسافات/لسنة/هندية) تُغطى في عين المدقق لا في أمثلة الاختبار فقط. **وحدّ معرفي موثق (جولة المدعي 2026-08-06):** المدقق يضمن وجود الطعن لا صحة نسبة المضمون إليه — رُصد خلط نسبة (مضمون «تقدير أقوال الشهود» أُلبس رقم 333/1988 ومبادئه عن عيب المبيع) لا يلتقطه إلا التدقيق الخارجي. **أُغلق جزئيًا (2026-08-07 — `.bak_attrib5`):** `_audit_attribution` يقارن كلمات الجملة المستشهَد فيها بنصوص مبادئ الطعن نفسه ويعلّم «نسبة الطعن N/Y» عند انعدام التقاطع. **أربع محاولات فاشلة سبقت النجاح، وكلها تراجعت آليًا، ودروسها:** (1) البحث بالنص الجزئي يجعل «9/1994» يطابق «29/1994» — كل مواضع فحص الأرقام تستعمل الآن `_appeal_rx` الواحد بحدود `(^|[^0-9])` ويغطي المسافات (`165 / 1983`) وصيغة «لسنة» والسنة المختصرة، **وقد أصلح ذلك عيبًا قديمًا كان يعلّم طعونًا حقيقية بأنها مختلقة**؛ (2) **الجانبان يُطبَّعان مختلفَين**: نص الإجابة يمر بـ`_draft_norm_ar` (ة←ه) بينما `normalized_text` يُبقي التاء — فبلا `_attr_norm` موحّدة ينهار القياس على كل كلمة منتهية بتاء (وهي أغلب لغة القانون)؛ (3) **الاستثناء المبتلَع يقتل الميزة صامتة** — أُزيل `try/except` الداخلي وصار الغلاف يطبع `[draft] attrib-error:`؛ (4) **سكربت اختبار لا يُرجع رمز فشل يُبطل التراجع الآلي** فيمر كود معطوب للإنتاج — كل اختبار الآن ينتهي بـ`sys.exit(0 if ok else 1)`. **وحدّ باقٍ موثق:** القياس اللفظي يكشف الخلط **الجسيم** (تقاطع صفر) ولا يكشف **الدقيق** — الطعن الواحد قد يحمل مبادئ متقاربة الألفاظ (قيس فعليًا: 421/2002 سجّل 0.54 لأن له مبادئ عن الوكالة وكشف الحساب وإن كانت القاعدة المقصودة عند 531/2000)؛ كشف الدقيق يحتاج مقارنة دلالية تُقاس قبل أن تُبنى.
**قيست فرُفضت — والبند أُغلق نهائيًا (2026-08-07):** على مجموعة موسومة من 12 نسبة (7 صحيحة
و5 خاطئة، كلها من تدقيقات خارجية مؤكدة) قيست ثلاث آليات، والفجوة (أدنى صحيح − أعلى خاطئ)
سالبة في الثلاث: اللفظي −0.054، الدلالي e5 −0.022، **والمرتِّب المتقاطع −7.16** (أقواها في
الترتيب أسوأُها هنا: الخاطئ 993/2003 سجّل 10.80 فوق كل الصحيحة، والصحيح 9/1994 سجّل 3.64).
ثم قيست فرضية ثانية **مختلفة الآلية** — تمييزية لا مطلقة: أين يقع الطعن المنسوب من ترتيب
القاعدة كلها لهذه الجملة؟ (وهي الآلية التي كشفت يدويًا أن 531/2000 أولى بالقاعدة من 421/2002)
— فسقطت أيضًا: أعلى صحيح رتبةً 8 وأدنى خاطئ رتبةً 1، فجوة −7 رتب. **السبب بنيوي لا معايري:**
الطعن المُلبَس مضمونًا خاطئًا يناقش الموضوع نفسه، فكل مقاييس التشابه — مطلقةً ونسبيةً — تجيب
عن سؤال «هل يبحث هذا الطعن هذا الموضوع؟» وجوابه نعم في الصحيح والخاطئ معًا. **فالخلط الدقيق
حدٌّ معرفي معلن يبقى على التدقيق الخارجي**، ولا تُبنى له طبقة تولّد تنبيهات كاذبة تدفع لحذف
استشهاد صحيح. **ودرس معياري:** «أفضل عتبة تكشف 3/5 بصفر تنبيه كاذب» في مجموعة صغيرة **وهم
ملاءمة** حين تمرّ العتبة على بُعد 0.057 من أدنى صحيح ومساحة التداخل تضم الصنفين — فرُفض حتى
رفعُ العتبة اللفظية القائمة، لا الطبقة الجديدة وحدها.
**سدّ تصديق الطعون بتواريخ الجلسات (2026-08-07 — `.bak_appctx`):** كشفه القياس السابق عرضًا
حين جاء طعنان موسومان «خاطئين» بالرتبة 1. السبب: `_appeal_rx` يقبل «10/1985» أينما ورد،
**وتواريخ الجلسات تُكتب هكذا** — فنص «الطعن 12/1996 مدني جلسة 1/**12/1997**» كان يُثبت وجود
الطعن الوهمي «12/1997». القياس: الطعون المختلقة 12/1997 و5/2008 و7/1999 لها 40 و40 و6 سجلات
مطابقة و**صفر بسند طعن** — أي أن المدقق كان يُجيز المختلق وهو وُجد لمنعه، وكل طعن رقمه بين
1 و31 عرضة لذلك. العلاج `_appeal_rows` — **نقطة واحدة يستدعيها الفحصان معًا** (الوجود والنسبة،
فلا ينحرف أحدهما عن الآخر كما انحرفا في عيب المسافات) — بمسارين: (1) صارم في القاعدة
(`_appeal_cue_rx`: سياق طعن/تمييز/لسنة ملاصق **بلا أرقام فاصلة**، فالأرقام الفاصلة تكسر شرط
التاريخ حتمًا ولا يُقتطع بحدّ صفوف)؛ (2) ترشيح بايثوني عند خلوّ الأول (`_appeal_ctx`: صدّ ما
سبقه رقم وشرطة أو تلته شرطة ورقم، وقبول ما في نافذته قبل الرقم لفظ طعن) — وهو اللازم للصيغ
المركبة «الطعنان **7 و8**/2005» التي يصدّها الصارم بحق لأن بين رقمها وسنده أرقامًا. والنسبة
تقارن بمبادئ الطعن الحقيقية وحدها بلا سجلات التواريخ التي تُلوّث القياس.
ومعها **كشف الاستثناء المبتلَع**: غلاف `_audit_answer` كان `except Exception: pass` — أي أن
طبقة المدقق كلها قابلة للتعطل صامتة فتخرج المسودة بلا شبهات كأنها نظيفة؛ صار يطبع
`[draft] audit-error:`. `APPCTX5_PASS` وبطارية 11/11.
**بوابة تُشتق من البيانات لا من وسم بشري (النموذج المعتمد لكل بوابة قادمة):** المراجع الحقيقية
تُستخرج في كل تشغيل من ذيول المبادئ («الطعن رقم N لسنة Y» — 15 مرجعًا)، والوهمية تُشتق من
تواريخ الجلسات («جلسة D/**M/YYYY**» ← مرشح «M/YYYY») ثم **يُتحقق كل مرشح بفحص شامل بلا اقتطاع**
فلا يُقبل وهميًا إلا بصفر سند طعن في القاعدة كلها؛ وإن لم تجد البيانات وهميًّا متحققًا واحدًا
فشلت الجولة ولم تُعتمد الرقعة — فالعلاج لا يُزرع إن لم تُثبت البيانات وجود العلة. المقيس فعليًا:
وهمية مختلفة كل جولة (11/2014 بـ255 سجلًا، 12/2014 بـ375، 7/2014 بـ296، 9/2014 بـ187) رُدّت
صفرًا بلا سقوط مرجع حقيقي واحد.
**ثلاثة إخفاقات سبقت النجاح، وكلها في أداة القياس لا في الرقعة (والرقعة لم يتغير فيها حرف):**
(1) **وسم من مسبار مقتطع** — وسمتُ 12/1997 «مختلقًا» بناءً على مسبار يقتطع عند 40 سجلًا فوجدت
له الدالةُ الشاملةُ سندًا؛ كل مسبار يبني وسمًا يجب أن يكون **شاملًا لا مقتطعًا** وإلا صار حدّ
الاقتطاع هو صانع «الحقيقة» التي تُقاس عليها الرقعة. (2) **مرساة غير مقيَّدة** — البحث بالفهرس
عن `except Exception: pass` بدأ من سطر الدالة ولم يُغلق عند نهايتها فأصاب مطابقتين؛ البحث داخل
دالة يُقيَّد **بطرفيها معًا** (حُوكي بكتلتَي شرك قبلها وبعدها). (3) **فكّ خاطئ لمخرج ثلاثي** —
`_audit_answer` تُرجع `(fetch, flags, inctx)` وكان المِشجب يفتش عن الشبهة في `fetch`، فأعلن
«أفلت» في جولتين متتاليتين ظلمًا للرقعة؛ **مِشجب الاختبار كودٌ يُراجَع كما تُراجَع الرقعة**،
ونسخُه من جولة لأخرى ينقل عيبه صامتًا. **بنية النقطة `/api/draft` تغيرت (`.bak_hb`):** المنطق في `_draft_run` وغلاف `draft_endpoint` يبث **نبضات إبقاء** (فراغ كل 10 ثوانٍ ثم JSON — كروم يقطع الطلب الصامت بعد ~5 دقائق) مع `X-Accel-Buffering: no`، ومهلة nginx رُفعت 300←1200 ثانية. **تنقية اقتران المدقق (2026-08-06، `.bak_audfix3`):** جولة «استجواب شهود النفي» (متابعة بمرفق PDF) اعتُمدت خارجيًا بصفر اختلاق — 13 مادة و9 طعون كلها مؤكدة، بعضها حرفيًا حتى برقم الصفحة (تصويب وحيد: صيغة اليمين «أقسم بالله العظيم» محلها م70 إثبات لا م44) — لكن «شبهاتها» المعلنة كلها ضجيج من مصدرين رُصدا وسُدّا: ذيل كلمة «رقم» يلتقطه نمط «م» («رقم 5947»←م594، «رقم (25787)»←م257) عولج بـ`(?<![ء-ي])م` في `_AUD_ART_RE`؛ وتواريخ الجلسات وأرقام الدعاوى/التوكيلات/السندات تُقرأ أرقام قوانين («جلسة 10/10/2016»←ق10/2016) عولجت بتوسيع استثناءات الاقتران (طعن/دعو/توكيل/سند/قضي/قبض/محضر/جلس/تاريخ/مورخ/حافظ — نافذة 30 حرفًا) + صد أي مرجع تسبقه شرطة أو رقم (جزء تاريخ). اختبار وحدة مدمج على نص يجمع كل الأنماط = `AUDTEST_PASS`. **درس معياري:** `normalized_text` في القاعدة **يُبقي التاء المربوطة** — مسابر LIKE تُكتب بـ«ة» («عبارة التوكيل» 7 سجلات؛ «عباره» صفر زائف).
**اقتران المادة بأقرب قانون (2026-08-07 — `.bak_pairfix2`):** كان `_art_ids` يأخذ **أول اسم
قانون يصادفه في جدول `_LAW_ALIASES_TBL`** داخل نافذة ±90 لا أقربه في النص، فقُرنت «المواد 702
و703 و711» المدنية بقانون الإثبات وولّدت **ثلاث شبهات كاذبة** (رُصدت في يوميات جولة حقيقية:
`شبهات: 3 ['الماده 702 من legis-39-1980' …]`) — وخطرها أن الشبهة الكاذبة تدفع النموذج لحذف
استشهاد صحيح أو وسمه [للتحقق]. العلاج: `_law_spans` يجمع كل مواضع أسماء القوانين وأرقامها
بمواقعها، و`_art_ids` يختار **الأقرب مسافةً مع ترجيح ما يأتي بعد الرقم** (صياغة «المادة 40 من
قانون الإثبات»)؛ واستثناءات الاقتران (طعن/دعوى/سند/تاريخ…) باقية. الاستبدال تم **بالفهرس** بين
مرساتَي `def _art_ids(text):` و`art_refs = _art_ids(norm)` لا بمطابقة نص. `PAIRFIX2_PASS`
بثلاث حالات مصمَّمة لتفشل قبل الإصلاح. **الضرب الديكارتي في القناة المباشرة يبقى مؤجلًا** —
هو يزيد ولا يخطئ، ونقل المنطق نفسه إليه جولة مستقلة تُقاس منفردة.
**مرفقات الاستوديو (2026-08-06 — `.bak_att8/.bak_att2/.bak_att3`):** حد الرفع 30 ميغابايت؛
الصور تُضغط في المتصفح قبل الإرسال (أقصى بُعد 2200 بكسل، JPEG 82% — `compressImage` في page.tsx)
فتعبر حد ~5MB لواجهة Claude مهما كبر أصلها؛ **وتعدد المستندات**: زر الرفع `multiple`، الأولى في
`attachment` والبقية في `attachments[]` (حتى 12، صور/PDF/نص مختلطة)، والخلفية تبني كتل المحتوى
لكلٍّ منها وتأمر النموذج بقراءتها جميعًا. بعد أي تعديل على page.tsx: `npm run build` ثم rsync
(موثق في §7) — ومشغّلات الرقع تتراجع آليًا للنسخة الاحتياطية إن فشل البناء.

**المحاور الواعية بالمستندات (2026-08-06 — `.bak_docaxes`):** كانت محاور Haiku تُبنى من نص
الوقائع المكتوب وحده، فطلبات «استخلص من الأوراق المرفقة» تبحث في العموميات بينما موضوع القضية
حبيس المرفقات (رصدها المالك: 939 مبدأ وكالة في القاعدة غابت كلها عن جولة قضية وكالة). العلاج:
`_draft_subqueries(..., media)` يستلم المرفقات نفسها (حتى 4 صور/PDF عبر `_sq_content`) فيستخرج
المحاور من موضوع المستندات الحقيقي — فتتغذى عليها كل القنوات (متجهات/فصول/معجمي/محلّ).
اليوميات: `[draft] doc-axes:`. **ووضع المتابعة (`.bak_follow`):** حقلا `prior_facts/prior_answer`
في `_DraftIn`؛ الاسترجاع على وقائع الجولتين (`facts_ret`) والنداء محادثة كاملة (`_msgs1`:
سؤال سابق ← مسودة سابقة ← الجديد بمرفقاته) وجولتا التدقيق ترثانها؛ والواجهة: زر «↩ متابعة»
(كتابةً أو بأوراق) + شارة إلغاء + «السجل (N)» — خيط كامل (سؤال/جواب/مرفقات/زمن، ↩ للمتابعات)
في localStorage (آخر 30) مع عرض واسترجاع وحذف. **السجل الخادمي (2026-08-06 — `.bak_thrsrv`):**
رصد المالك أن السجل لا يظهر من الهاتف (localStorage محلي لكل متصفح) — فنُقل للخادم: جدول
`draft_threads` (q/a/أسماء المرفقات فقط/follow/ts، سقف 200) + ثلاث نقاط خلف `require_auth`
(`GET/POST/DELETE /api/draft/threads`؛ POST يمنع التكرار بمفتاح ts+q) والواجهة تجلب عند الفتح
وتدمج مع المحلي **وترحّل** المدخلات المحلية القديمة للخادم تلقائيًا، وكل جولة جديدة تُحفظ فورًا
والحذف يمتد للخادم؛ localStorage باقٍ احتياطًا عند انقطاع الشبكة. فحص الزرع: المسار بلا مصادقة
يرد 401 لا 404. **الرئيسية موجّهة لعمل المحامي والمتابعة من أي جولة (2026-08-07 —
`page.tsx.bak_home`):** رصد المالك أن الرئيسية موروثة من تصميم بناء القاعدة («أحدث النشاطات»
تُغذّى من القضايا فتبقى فارغة أبدًا، والاستوديو غائب عن الدخول السريع، والمتابعة محصورة بآخر
جولة). العلاج: الدخول السريع = استوديو ← بحث ← (قضايا/رفع إداريًا)؛ و«أحدث جولاتك» تُغذّى من
`GET /api/draft/threads` (آخر 8) بالنقر لفتح الجولة وزر «↩ متابعة» بجانبها؛ والجسر بين
الصفحتين كائن وحدة `_openThread{rec,follow}` يقرؤه الاستوديو في `useEffect` عند التركيب؛
وكل سطر في لوحة السجل صار يحمل «↩ متابعة من هنا» — فلكل قضية خيط عمل مستقل لا خيط متسلسل واحد.
**درس مرساة:** كتلة JSX تُستبدل **بالفهرس** (بحث محتوى فريد عن سطري البداية والنهاية) لا
بمطابقة نص فيه مسافات بادئة.
**ملف القضية الحي (2026-08-07 — `.bak_casebind`):** كانت القضية بطاقة بيانات معزولة والعمل
الحقيقي يجري خارجها. الآن: عمود `draft_threads.case_id` (**نصي — `legal_cases.id` من نوع
`uuid` لا bigint؛ كشفه المسبار قبل الزرع**) + جدول `case_attachments` + مجلد
`/opt/legalmind-data/case_files/<uuid>/`. الأوراق تُحفظ **فعليًا على القرص** من داخل
`_draft_run` (البايتات حاضرة هناك فلا رفع مزدوج) عبر `_save_case_attachments` ببصمة محتوى
sha256 تمنع تكرار الورقة عبر جولات المتابعة، وأي فشل يُطبع ولا يعطل الصياغة. النقاط:
`GET /api/draft/threads?case_id=`، `GET /api/cases/{id}/files`،
`GET /api/casefiles/{fid}/download` (كلها خلف `require_auth`). الواجهة: مُحدِّد قضية في
الاستوديو، ومكوّن `CaseActivity` داخل ملف القضية يعرض جولاتها وأوراقها بفتح/متابعة/تنزيل،
وزر «فتح الاستوديو على هذه القضية» عبر `_openThread.caseId`، وشارة اسم القضية في جولات
الرئيسية. **ورفع مباشر للأوراق (`.bak_caseup`):** `POST /api/cases/{id}/files` (متعدد،
حتى 30 ملفًا، البصمة نفسها تمنع التكرار) + `DELETE /api/casefiles/{fid}` (يمحو السجل والملف
من القرص معًا)، وزر «+ إضافة أوراق» وزر حذف لكل ورقة في ملف القضية — فالأوراق تدخل الملف
بطريقين: رفعًا مباشرًا أو إرفاقًا في الاستوديو والقضية مختارة.
**ذاكرة ملف القضية (2026-08-07 — `.bak_casemem`):** بأمر المالك «يطّلع النظام على المستندات
في أي وقت يبحث موضوعًا متعلقًا بالقضية». `_load_case_media` يحمّل أوراق القضية من القرص
ويحقنها في `inp.attachments` **قبل** بناء `_early_media`/`atts` — نقطة حقن واحدة تُغذّي كل
القنوات تلقائيًا (المحاور الواعية بالمستندات + قراءة النموذج عند التحرير) بلا ازدواج كود.
الضوابط: بصمة sha256 تستبعد ما أُرفق في الجولة نفسها، وسقوف (6 أوراق، 4.5MB للورقة، 12MB
للمجموع، الأحدث أولًا)، وكل ورقة توسم في اسمها «— من ملف القضية» فيميزها النموذج عن أوراق
الجولة، وأي فشل يُطبع ولا يعطل الصياغة. اليوميات: `[draft] case-memory:`. اختبار وحدة مدمج
(تحميل + منع تكرار) = `CASEMEM_PASS`. **لا أثر على البطارية**: الحقن مشروط بوجود `case_id`
وحالات البطارية بلا قضية.
**إصلاح سلسلة إنشاء القضية (2026-08-07 — `.bak_casefix`):** رصد المالك غياب مكان للمستندات
في صفحة الإنشاء؛ وكشف التشخيص ما هو أخطر: **جدول القضايا فارغ (صفر)** ولا `POST /api/cases`
وصل الخادم قط. **بنية مهمة موثقة:** وحدة القضايا ليست في `app.py` بل في `admin/cases_api.py`
(راوتر بادئته `/api/cases`) يُضم في `admin/main.py` — **والخدمة تشغّل `admin.main:app` لا
`admin.app:app`**، فأي بحث عن نقاط القضايا في `app.py` وحده يضلّل. العلاج ثلاثي: (1) الترقيم
الآلي صار مقروءًا متسلسلًا سنويًا `ق-2026-0001` في `_case_key` (والعشوائي احتياطًا فلا يفشل
إنشاء)؛ (2) `submitCase` كان يقرأ `event.currentTarget` **بعد** `await` وقد أُزيل النموذج
فيرمي استثناءً يبتلعه المتصفح صامتًا — صار يلتقط عنصر النموذج قبل الانتظار ويغلّف كل شيء
بـ`try/catch` برسالة فشل ظاهرة؛ (3) منطقة «أوراق القضية (اختياري)» داخل نموذج الإنشاء ترفع
الملفات المختارة إلى الملف فور إنشائه. اختبار حي مدمج ينشئ قضية ويتحقق ويحذف =
`CREATE_TEST_PASS`. **درس معياري:** «الميزة غائبة» تُفحص أولًا بعدّ صفوف جدولها — الفراغ يدل
على سلسلة مقطوعة لا واجهة ناقصة.
**إصلاح بوابة التغطية (2026-08-07 — `cases_api.py.bak_covfix`):** كانت شارة القضية تعلن
«الصياغة مقيدة بنقص المصادر — ناقص: تشريع» **كذبًا**، لأن `case_coverage` تبحث عن
`by_type["legislation"]` وهو نوع **لا وجود له**؛ الأنواع الفعلية ثلاثة
(`legislation_article` 1755 + `legislation_issuing_article` + `legislation_preamble`) فجُمعت،
وضُمّت الأحكام الكاملة للمبادئ، وتُرجَع العدادات بتسميات عربية بدل المعرفات الإنجليزية.
اختبار على قضية حقيقية: `COVFIX_PASS` (جاهزة للصياغة، الناقص: لا شيء).
**درس معياري:** أي بوابة تعتمد أسماء أنواع تُتحقق مقابل `SELECT DISTINCT object_type` —
اسم نوع مخمَّن يُنتج منعًا كاذبًا يوقف عمل المحامي بلا سبب.
**ثم استُبدل المربع كله بملخص عمل القضية (2026-08-07 — `page.tsx.bak_casesum3`):** رصد المالك
أنه لا يفهم المقصود منه، والتشخيص أثبت أنه محق: `case_coverage` ترشّح بـ`branch` والقضية
موضوعها غير محدد، فالمعروض حصيلة **الفرع كله** لا القضية (5620 مبدأ مدني)؛ ومع امتلاء الفروع
صارت البوابة تقول «جاهزة» أبدًا — **ومؤشر لا يتغير لا يحمل معلومة**. البديل يُحتسب من القضية
وحدها داخل `CaseActivity` (بياناته حاضرة: `rounds`+`files` فلا نداء شبكة إضافي): عدّاد الجولات
والأوراق، وآخر نشاط، ومقتطف آخر جولة، و**عدّ مواضع `[للتحقق]` في آخر مسودة** (أصفر إن بقيت،
أخضر إن لم تبقَ) — وهو السطر العملي الذي يقول للمحامي ما بقي عليه فعله. والشارة صارت تعرض حالة
القضية. **ثلاثة إخفاقات زرع سبقت النجاح وكلها معياري/مرساة لا منطق:** عودة
`return <div className="space-y-5">` غير متفرّدة (عولجت بالفهرس داخل حدود `function CaseActivity`)،
وشرط ختامي جارف (`drafting_ready` باقٍ في **تعريف نوع** لا في واجهة) — فالشروط تفحص **المعروض
لا المُعرَّف**. `BUILD_OK`. **اعتماد حي (2026-08-06):** جولة «أسئلة
استجواب شهود النفي» بمرفق PDF أثبتت الحلقة كاملة — المحاور استُخرجت من موضوع المستند فعلًا
(«صحة عقد الوكالة الخاصة وحدودها»…) فظهرت مبادئ الوكالة الغائبة (267/99، 539/2004، 134/1985،
17-68/2009 الشهادة السماعية)، والفصل الحاكم جلب مواد الإثبات م39–م50 كاملة لمجرد ذكر الشهود.
وتشخيص «Failed to fetch» العابر: فشل سريع في نداء جولة التنقيح (رد 156 بايت، صفر أخطاء كود) —
إعادة المحاولة تكفي، لا عطب بنيويًا. **اختبار القبول الشامل (2026-08-06):** سؤال مركّب مصمم
ليضرب كل الطبقات (زواج عرفي مختلط المذاهب + نسب + ميراث) — الإجابة أصابت كل العلامات: لجنة
النسب 53/2026 فحصًا وجوبيًا، والإسناد المذهبي الدقيق (الميراث بمذهب المتوفى السني — مادة
الإصدار الرابعة/ب ق124/2019 حرفيًا — مع نصيحة استراتيجية صحيحة أن السني أصلح للزوجة الجعفرية
لأنها ترث معه من الأرض عينًا)، والتدقيق الخارجي الكامل = **صفر اختلاق: 18 مادة و10 طعون كلها
مؤكدة حرفيًا** (بما فيها 902/2012 حتى برقم القاعدة 46 مج42-43، و322/2005 «فحص الجينات
الوراثية» بلفظ القاعدة، والاستشهاد المزدوج بـ278/2005 مبدآن حقيقيان متمايزان لطعن واحد).
**فجوة استرجاع مرصودة:** م92 ق51/1984 (عدم سماع دعوى الزوجية عند الإنكار) في القاعدة ولم
تُسترجع — الإجابة أفصحت بصدق بدل الاختلاق؛ أُغلقت بحالة البطارية 11 (انظر أدناه).
**درس مسابر:** العبارة تُجرَّب بمرادفات القاعدة («فحص الجينات الوراثية» لا «الفحص الجيني»)
قبل الحكم بخلط النسبة.

**حزمة الأدلة الثابتة (2026-08-12 — `.bak_evidence`):** جدول `draft_evidence` يسجل كل جولة
استوديو (qid، السؤال/المذهب/القضية، المحاور، كل المسترجَع بقنواته ودرجاته، ما دخل السياق
`seen`، النموذج، أعلام المدقق) بسطر تسجيل خالص قبل عودة `_draft_run` — فشله يُطبع
(`[draft] evidence-error:`) ولا يعطل الجولة. الغرض: مقارنة نماذج عادلة على نفس الأدلة حرفيًا
(خطة المزوّدين Claude/OpenAI) + تشخيص دائم بلا أركيولوجيا يوميات. اعتُمدت ببطارية 12/12.
**طبقة النماذج الموحدة (2026-08-12 — `admin/llm.py` + `.bak_llm1`):** ثلاث دوال
(`_role_provider` بالبيئة، `fast_create` للمحاور وإحالات الأوراق، `draft_create` للتوليد
ببث fable/تراجع Opus حرفيًا كما كان)، والأدوار تُختار بـ`LEGALMIND_FAST_PROVIDER`
و`LEGALMIND_DRAFT_PROVIDER` (افتراضيًا anthropic؛ مزوّد غير مدعوم يرفع خطأ صريحًا لا
يفشل صامتًا). رُحّلت ثلاثة مواضع نداء في `app.py` بمراسٍ حرفية — ونداء جولة التنقيح تُرك
لجولة OpenAI بمسبار خاص به (لا رقعة عمياء). اختبار وحدة بعملاء وهميين يثبت مساري fable/غيره
قبل مساس الإنتاج. اعتُمدت ببطاريتين 12/12.

**طبقة النماذج الموحدة (2026-08-12 — `admin/llm.py` + `.bak_llm1`):** ثلاث دوال
(`_role_provider` بالبيئة، `fast_create` للمحاور وإحالات الأوراق، `draft_create` للتوليد
ببث fable/تراجع Opus حرفيًا كما كان)، والأدوار تُختار بـ`LEGALMIND_FAST_PROVIDER`
و`LEGALMIND_DRAFT_PROVIDER` (افتراضيًا anthropic؛ مزوّد غير مدعوم يرفع خطأ صريحًا لا
يفشل صامتًا). رُحّلت ثلاثة مواضع نداء في `app.py` بمراسٍ حرفية — ونداء جولة التنقيح تُرك
لجولة OpenAI بمسبار خاص به (لا رقعة عمياء). اختبار وحدة بعملاء وهميين يثبت مساري fable/غيره
قبل مساس الإنتاج. اعتُمدت ببطاريتين 12/12.

**سدّ تصديق المفتاح ودرس الحرف الملتصق (2026-08-13):** أول تفعيل حي لمفتاح OpenAI فشل
بـ`UnicodeEncodeError` من مكتبة `httpx2` (تبعية `openai` 3.0.0) عند بناء رأس `Authorization` —
والتتبع الكامل كشف السبب: **حرف عربي «ا» ملتصق بآخر المفتاح** (طوله 165 بدل 164)، من تبديل
لغة الكيبورد لحظة اللصق في `nano` — توأم عيب «الالتصاق ببداية الأوامر» الموثق مسبقًا، لكن
هنا بنهاية القيمة. **حادثة تسرّب موثقة بصراحة:** مسبار تشخيصي أول طبع رأس Authorization
كاملًا (بما فيه المفتاح) في المحادثة عن خطأ في عدم إخفاء القيمة قبل الطباعة — المفتاح أُبطل
فورًا واستُبدل. **قاعدة دائمة راسخة:** أي أداة تشخيص تلمس سرًّا تُبنى بحجب بنيوي لا اختياري —
دالة `mask()` وحدها تلمس القيمة الخام وتعيد أول 6 وآخر 4 أحرف وطولها فقط، فلا مسار كودي واحد
يمرر السلسلة الكاملة لأي `print`. إدخال أي مفتاح جديد يُنظَّف تلقائيًا (`tr -cd '[:print:]'`
بقفل `LC_ALL=C`) قبل الحفظ لمنع تكرار الالتصاق. اختُبر حيًّا: كلا النموذجين ردّا بنجاح.

**خارطة طريق المزوّدين المعتمدة (بعد مراجعة متقاطعة مع تحليل خارجي):** حزمة الأدلة ✓ ←
`admin/llm.py` واحد (مسجّل مزودين + محوّل وسائط، متغيرات `LEGALMIND_*_PROVIDER` لكل دور)
← OpenAI مزوّدًا ثانيًا (`OPENAI_API_KEY` يضعه المالك في .env بنفسه) ← **التدقيق المتقاطع**
(النموذج الثاني يطعن في الاستشهادات — أول مرشح جدي لسد حدّ «الخلط الدقيق» الموثق) ←
مقارنة عمياء على البطارية ← MCP قراءة فقط آخرًا. القاعدة: ملف واحد يتوسع بما تثبته الأرقام،
لا هيكل مسبق. (قرار المالك: تدوير كلمة سر القاعدة غير مطلوب — نظام خاص وPG على 127.0.0.1.)

**OpenAI مزوّدًا ثانيًا (2026-08-12 — `.bak_llm2`):** `admin/llm.py` v2 — مزوّد OpenAI عبر
Responses API خلف الواجهة نفسها؛ مخرج موحَّد (`.content[0].text` / `.model` / `.stop_reason`)
فلا يتغير حرف في `app.py`؛ محوّل وسائط يحوّل كتل Anthropic (نص/صورة/PDF) إلى نسق OpenAI
(اختُبر على المثال الثلاثي)؛ **صمّاما أمان**: لا تخمين لاسم النموذج (`LEGALMIND_OPENAI_*_MODEL`
صريح وإلا خطأ واضح) ولا فشل صامت عند غياب المفتاح — ومحاكاة الجافة المحلية قبل الشحن كشفت
ترتيب فحص خاطئ (اتصال قبل التحقق من اسم النموذج) فأُصلح قبل أي جولة خادم. `healthcheck()`
للتشخيص بلا كشف قيم. رُحّل موضعا جولة التنقيح الأخيران — **صفر نداء مباشر متبقٍ في app.py،
كل النداءات عبر الطبقة**. النظام يبقى على Anthropic افتراضيًا (خامد حتى يضبط المالك البيئة).
اعتُمدت ببطاريتين 12/12.

**اختيار المزوّد لكل جولة (2026-08-15 — `.bak_draftprov`):** حقل `draft_provider`
(`anthropic|openai`) في طلب `/api/draft` يتجاوز البيئة العامة **لهذه الجولة وحدها** —
تجاوز صريح يُمرَّر وسيطًا (`_role_provider(role, override)` و`draft_create(..., provider=)`)
لا عبر `os.environ` فهو آمن للطلبات المتزامنة. قيمة غير صالحة تُرد 400. مُرّر في نقطتي
النداء (التوليد والتنقيح). هذا أساس زر «قارن الاثنين» القادم. اعتُمد ببطارية 12/12 (إضافي بحت).

**التدقيق المتقاطع المندمج (2026-08-15 — `.bak_crosscheck`):** جوهر الجولة الرابعة بتصميم
المالك («أريد العنب وليس محاسبة الناطور... إجابة أخيرة دقيقة وبدون هلوسة نهائيًا»): بعد
المدقق الحرفي، نموذج مستقل من **المزوّد المقابل** لمن كتب المسودة (`_crosscheck_answer` عبر
`fast_create(..., provider=)` — أُضيف التجاوز للدور السريع أيضًا) يقرأ السياق المسترجَع
والمسودة بمعزل ويحكم على كل استشهاد: confirmed تُحذف من التقرير / suspicious (موجود لكن
المضمون المنسوب لا يطابقه — **حدّ «الخلط الدقيق» الموثق**) / unverifiable. شبهاته تُلحق
بنفس `_parts2`/`_fix_msg` وتُفعّل نفس جولة التنقيح `_r2` القائمة — **إجابة واحدة منقّحة لا
تقرير نقد منفصل**. أي فشل (مفتاح/شبكة/JSON) يُطبع `[draft] crosscheck-error:` ويعيد `[]` فلا
يعطل الصياغة أبدًا. اليوميات: `[draft] crosscheck: N بمزوّد X`. اعتُمد ببطارية 12/12.
**درس مرساة متجدد:** مرساة الجولة سقطت أول مرة لأنها كُتبت من ذاكرة v1 لا من نص v2 المنشور —
العلاج المنهجي: **إعادة بناء حالة الخادم محليًا** (مصدر llm2 + رقعة draftprov تُطبَّق تسلسلًا)
والمحاكاة عليها، مع تشغيل الدالة المزروعة نفسها بعميل وهمي قبل الشحن (كشف أيضًا عيب
`setdefault(...) or` في اختبار الوحدة قبل أن يفشل زورًا على الخادم).

**زر «⚖ قارن الإجابتين» وقائمة «النموذج المجيب» (2026-08-15 — `page.tsx.bak_draftcmp`):**
واجهة خالصة فوق `draft_provider`: قائمة في محددات الصياغة (Claude افتراضيًا / GPT) تسري على
«نفّذ الطلب»، وزر مقارنة يطلق الجولتين **بالتوازي** (`Promise.allSettled` — فشل مزوّد واحد
لا يسقط المقارنة) على السؤال والمرفقات والقضية نفسها، وكلتاهما تمر بالتدقيق المتقاطع.
العرض **أعمى**: نافذة عائمة بعمودين «أ/ب» بترتيب عشوائي + زر «كشف النموذجين»، والعمودان
يُصيّران بـ`MdLite` القائم. «اعتماد هذه الإجابة» يجعلها مسودة الجولة فتُحفظ في السجل وملف
القضية عبر مسار `_snap`/`useEffect` القائم بلا كود حفظ جديد. النافذة مرساتها سطر `return`
الافتتاحي للاستوديو (عنصر `fixed inset-0` لا يعتمد على تخطيط الجوار). المحاكاة المحلية:
الرقعة طُبقت على نسخة مبنية من المراسي الحرفية + توازن وسوم/أقواس صفر انحراف + حارس تكرار.
بوابة الجولة `BUILD_OK` (واجهة فقط — البطارية لا تلزم).
**تكملة (`.bak_draftcmp2`):** أزرار نسخ + Word في رأس كل عمود (`buildHtml(src0?)` صارت
تقبل نصًا اختياريًا)، والرد الناقص (200 بلا نص إجابة) يُعامل خطأً صريحًا برسالته، ورسالة
الخطأ تُعرض في `<bdi>` فلا تُبتلع في سياق RTL.

**جذر فشل عمود GPT وتعطل التدقيق المتقاطع الصامت (2026-08-15 — `.bak_oakey`):** أول مقارنة
حية أسقطت عمود GPT بـ`OPENAI_API_KEY غير مضبوط في البيئة` — **وبيّنت اليوميات أن التدقيق
المتقاطع نفسه كان معطلًا بصمت منذ تركيبه** (`crosscheck-error` كل جولة، يعيد `[]` بأمان فلا
يُلحظ): بيئة خدمة systemd لا تحمّل `deploy/.env`، وفحوص OACHECK نجحت لأن صدفتها حمّلته.
العلاج: `_env(name)` في `llm.py` — بيئة العملية أولًا ثم قراءة الملف مباشرة عند كل نداء
(سلوك مفتاح Anthropic نفسه: التغيير يسري بلا إعادة تشغيل)، وتحولت إليها كل مواضع `getenv`
السبعة (مزوّد/مفتاح/نموذجين/healthcheck). **درسان معياريان:** (1) بوابة اعتماد أي إعداد
تُشغَّل **ببيئة الخدمة لا ببيئة الصدفة** — بوابة الجولة `OAKEY_LIVE_OK` نداء OpenAI حي عبر
الطبقة بـ`env -u` يمحو متغيرات الملف؛ (2) الميزة الآمنة الفشل («تعيد [] فلا تعطل») تحتاج
**فحص حياة دوريًا** في اليوميات وإلا ماتت صامتة وظُنت تعمل.

**ترويسة no-cache للواجهة (2026-08-15 — `.bak_nocache3` بعد جولتين متراجعتين):** رصد المالك
«لم يصل التحديث» للتطبيق، والمسبار أثبت النشر سليمًا و`index.html` بلا `Cache-Control` أصلًا
فويب-فيو التطبيق يخزّن باجتهاده. العلاج: `add_header Cache-Control "no-cache, must-revalidate"`
بعد كل `root /var/www/legalmind-v3;` في الملفات الفعّالة (ETag قائم فالرد 304 رخيص، وأصول
`_next/static` مبصومة فلا تتأثر عمليًا). **دروس الجولتين المتراجعتين:** (1) مضيف التطبيق في
ملف مستقل `sites-available/legalmind-app` — رقعة تصيب `legalmind-admin` وحده لا تمسه؛
(2) **إعادة تحميل nginx غير لحظية**: بوابة فحص بعد `systemctl reload` مباشرة أسقطت رقعة
صحيحة لأن العمال القدامى كانوا يخدمون بعد — `sleep 1` قبل البوابة؛ (3) فحص الأدمن بلا
اعتماد يرد 401 و`add_header` لا يُرفق بـ401 — بوابة الترويسة تُقاس على مضيف بلا htpasswd.

**جولة prinxref (2026-08-11/12 — `.bak_prinxref`، خمس محاولات كل فشلٍ كشف طبقة أعمق):**
انطلقت من رصد المالك («استرجاع القوانين أقل من الأحكام» في سؤال عقد بيع محل تجاري) ورفضه
الإصلاح الفردي — فأُغلقت **أربع فئات** بضربة واحدة، اعتُمدت ببطاريتين 12/12:
1. **صمام سلامة المحاور (الأثمن):** مسبار حي ضبط محاور Haiku منحطةً إلى مفاتيح JSON
(«الاستعلامات_البحثية» تُبحث في القاعدة حرفيًا!) حين يعيد كائنًا بدل مصفوفة — وهذا مصدر
تقلب الاسترجاع كله (306←185 في السؤال الواحد، وقصة م92 على الأرجح). العلاج: استرداد
المحاور من **قوائم** الكائن المنحط وحدها (لا مفاتيحه ولا نصوصه المفردة — «لا شيء» تسربت
في المحاكاة) + صد أي لفظ فيه «_».
2. **قناة `_prin_xref` (توأم الشقيقات):** المبدأ المسترجَع يجر المواد التي يفسرها بذكرها
الصريح («المادة 36 من قانون التجارة») — 2,559 مبدأً يحمل إحالة تشريعية = جسر دائم بين
لغة المحامين ولغة المشرّع في كل الفروع. اليوميات: `[draft] prin-xref:`.
3. **ترادف «محل↔متجر» في `_CHAP_SYN`:** فصل المتجر مبوّب كاملًا في القاعدة وكان محجوبًا
لفظيًا فقط — به صارت م36 (رسمية بيع المتجر) حتمية عبر الفصل الحاكم.
4. **مرآة التوسعات في المشغّل:** البطارية كانت **عمياء عن مرحلة التوسعات** (XREF/شقيقات)
منذ `.bak_sibxref` — رقعةُ جولةٍ سابقة نجحت وحدةً وسقطت بطاريةً فانكشف نقص المقياس؛
أُلحقت المرحلة بالمشغّل بدوال الإنتاج نفسها. والحالة 12 (سؤال المالك حرفيًا، واجبها م36)
معيار دائم للفئة.
**دروس معيارية:** بدائل `any` تُشتق من البيانات لا التخمين (م34-38 المخمّنة لا يذكرها مبدأ
واحد، فرفضتها القاعدة)؛ بوابة «يجب أن يفشل قبل الرقعة» لا تصلح لعلة تقلبية (سابقة gate92)؛
والمحاكاة الجافة المحلية قبل الشحن اصطادت ثلاثة عيوب (ذيل regex يبتلع الإحالة التالية —
عولج بنظرة أمامية؛ تسرب النص المفرد؛ تداخل الاقتباسات) قبل أن تكلف جولة خادم واحدة.

**جولة تصليب المطابق (2026-08-06/07 — `.bak_chapstem`←`2`←`3`←`.bak_close2`):** بأمر المالك
(«حلول كاملة لا ترقيعية») أُغلقت **فئات** لا أمثلة، وكلها محكومة ببطاريتين متتاليتين 11/11:
1. `_chap_stem`: قص لواحق الجذر (ات/ون/ين/ان/يه/ي بشرط بقاء 3) — الزوجية=الزوجين=زوجات.
2. هيكل مصدر التفعيل: نزع تاء «تفعيل» من الجهتين قبل الهيكل — طلاق=تطليق.
3. `_CHAP_SYN`: جدول ترادف فقهي (طلاق↔تفريق/فرقة؛ زواج↔زوجية/نكاح؛ ميراث↔مواريث/إرث/تركة).
4. قاعدة `fullw`: تسمية لم يغب عنها إلا كلماتها الضعيفة تُقبل بقوية نادرة/متوسطة («دعوى الزوجية»).
5. التشذيب يعم كل فصل ضخم (>8 ← صدر6+ذيل2) لا الأحادي فقط، والتوزيع **بجولتين**: الفصل
الصغير (≤6) كاملًا ورأس الكبير (3) أولًا ثم إكمال الكبار — الفصل الصغير المتخصص لا يُزاحَم.
6. نزع الجار المدمج في `_chap_tok` (بال/فال/كال/لل بشرط بقاء 3) — بالتزوير=تزوير؛ و«طعن»
انضمت لـ`_CHAP_WEAK` — ففصل «الطعن بالتزوير» (م32-36 إثبات) يُفتح بكلمة «تزوير» وحدها.
7. **بوابات الإجراءات السابقة الوجوبية** (`_GATES` — حصر تشريعي لا اجتهاد): «نسب» في السؤال
← م7/م8/م10 من 53/2026 حتمًا؛ «طلاق/تفريق/تطليق/خلع» ← م9 ق12/2015 (مركز التسوية) حتمًا؛
«عرفي/زوجيه» ← م92+م93 ق51/1984 (سماع دعوى الزوجية عند الإنكار) حتمًا (`.bak_gate92`
2026-08-10 — أُضيفت بعد رصد أن ضمانة م92 في بطارية القضية 11 كانت متقلبة رهن قُرعة محاور
Haiku: نجحت وفشلت مرتين مرتين في يوم واحد، فرُمّزت؛ اعتُمدت ببطاريتين متتاليتين 11/11).
هذا ذراع الاسترجاع لقاعدة الموجّه «فحص الإجراءات السابقة الوجوبية» — فصول الاستلزام القانوني
لا يصلها تشابه لفظي ولا دلالي، تُرمز حصرًا.
**درس معماري موثق (معايرات semchap 1-5):** فصول التشريعات نوعان — «فصول تشابه» يصيبها اللفظ
والدلالة، و«فصول استلزام قانوني» (لجنة النسب، دعوى الزوجية) رابطها علم قانوني لا لغة؛ جُربت
طبقة دلالية كاملة (فهرس e5 لتسميات الفصول المُثراة + مرتب متقاطع + تقطيع حتمي — الفهرس محفوظ
`chap_sem.pkl`/`chap_sem2.pkl` والأداة `engine/chapsem_cli.py` أصلًا بحثيًا **غير مربوط**)
وأثبت القياس أن مكاسبها مغطاة لفظيًا وضجيجها يزاحم السياق — فرُفض ربطها. القاعدة: **نبني ما
تثبته الأرقام ونرفض ما ترفضه ولو كلفنا بناؤه جهدًا**، والبوابة الحاكمة بطاريتان متتاليتان
مع تراجع آلي (`ROLLED_BACK`) — لا لصقة تمس الإنتاج دون اجتيازهما.

3. **توسعة العرض:** القناة المعجمية 8←16 مرشحًا وسقف المرتِّب المتقاطع 150←300 زوجًا — استرجاع واسع ثم ترشيح دقيق.

**جمع نسخ المبادئ عند الاسترجاع (2026-08-04، `.bak_dedup`):** القاعدة تحوي ~2,140 عنقود تطابق نصي حرفي بين المبادئ (3,289 سجلًا فائضًا — إعادة نشر المبدأ ذاته عبر المجلدات بمواضع نشر مختلفة، وكلها باقية في القاعدة **عمدًا**). الحل في طبقة الاسترجاع: حصة المبادئ المرشحة 32، وعند بناء السياق تُجمع النسخ المتطابقة المطلع (بصمة أول 120 حرفًا مطبَّعة، `_prin_seen`) ويبقى الأعلى درجة. **تحذير معياري:** لا تجمع أبدًا بمرساة (رقم طعن + جلسة) — الطعن الواحد يناقش عدة مواضيع فيولّد عدة مبادئ متمايزة بالذيل نفسه (عناقيد حتى 85 مبدأً مشروعًا لطعن واحد).

**ترميم ذيول «الطعن ؟» (2026-08-04):** من 197 مبدأ فبراير-2025 بذيل مبتور، رُمم **70** باستخراج رقم الطعن من ديباجة الحكم الأم (`source_judgment_id`؛ نافذة 6000 حرف + تطبيع الأرقام الهندية) — كل تصحيح موثق في `metadata.source_correction` والحكم الأم أخذ `metadata.case_ref`، وأعيدت فهرسة السبعين. الـ127 الباقية أحكامها الأم نفسها بلا أرقام في ديباجاتها — تبقى بذيل «؟» الصادق، ولا يُستخرج رقم من عمق الحيثيات (غالبًا إحالات لطعون أخرى).

---

## 6-مكرر) بطارية القياس (صمام الانتكاس الدائم)

`/opt/LegalMind/battery.json` (**11 قضية** معيارية عبر كل الفروع — الحادية عشرة «زواج عرفي
مختلط المذاهب» أضيفت 2026-08-07 بواجبَي م92 ق51/1984 وم8 ق53/2026 — بشروط نجاح صريحة: مواد واجبة
`must` / بدائل `any` / حد أدنى لتمثيل قانون `law_min` / **ضمانة مادة النفاذ ديناميكيًا** `must_last`
تُقرأ آخر مادة من القاعدة لا من رقم مخمن) + المشغل `/opt/LegalMind/tools/battery_run.py` —
يجري خط الاسترجاع كاملًا (محاور Haiku + متجهات + حزم + محلّ + فصول + معجمي) **بلا نداء صياغة**
(3-5 دقائق، كلفة فلسات). التشغيل:

```bash
cd /opt/LegalMind; set -a; . deploy/.env; set +a
/opt/LegalMind/admin/.venv/bin/python /opt/LegalMind/tools/battery_run.py
```

النجاح = `BATTERY_PASS` (خط الأساس 11/11 اعتُمد 2026-08-07 **ببطاريتين متتاليتين** — الثبات
شرط لا النجاح المفرد؛ المشغل محصّن: مجموعة `any` النصية تُلف قائمةً تلقائيًا). **تُشغَّل بعد أي دفعة بيانات أو
فهرسة كاملة أو تعديل على الاستوديو — لا حاجة لها في الاستعمال اليومي.** عند `BATTERY_FAIL`
السطر يسمي العطب (must/any/law/last). نسخ البطارية محفوظة في المستودع (`battery.json` +
`tools/battery_run.py`).

## 7) أوامر التشغيل الشائعة

```bash
# حالة الخدمات
systemctl status legalmind-admin legalmind-ingest --no-pager

# إعادة تشغيل الأدمن (بعد أي تعديل على app.py)
systemctl restart legalmind-admin

# عدّ الكائنات
set -a; . /opt/LegalMind/deploy/.env; set +a
psql "$DATABASE_URL" -c "SELECT object_type, count(*) FROM knowledge_objects GROUP BY 1 ORDER BY 2 DESC;"

# بناء ونشر الواجهة بعد تعديل web/app/page.tsx
cd /opt/LegalMind/web && npm run build && rsync -a --delete out/ /var/www/legalmind-v3/ && chown -R www-data:www-data /var/www/legalmind-v3
# ثم Ctrl+Shift+R في المتصفح
```

**تنبيهات طرفية الويب:** فعّل `bind 'set enable-bracketed-paste on'` قبل لصق نصوص طويلة؛ احذر التصاق حروف عربية خفية ببداية الأوامر (لوحة المفاتيح!)؛ للخروج من عارض psql اضغط `q` أو استعمل `-P pager=off`.

---

## 8) إعادة بناء التصنيف من المصادر (ليلة 2026-08-04/05) — الإنجاز المحوري

بأمر المالك («التصنيف من تبويب المصدر حصرًا»)، رُفعت 18 ملف مصدر وفُكّكت 9 معاجم بمحلّلين
(محلّل أنماط Word يستقرئ دور كل نمط آليًا + محلّل العائلة العارية) إلى **23,877 مدخلًا في 1,950
قسمًا** (`taxonomy/master_map.json.gz.enc` في المستودع — مشفرة AES-256 لعبور المستودع العام،
والمفتاح عبر القناة الخاصة فقط). التطبيق بالبصمة (حروف عربية خالصة، أول 60) حدّث **35,237 مبدأً
(81.5% من المبادئ كلها)** بتصنيف محرري المصدر الحرفي (موضوع/فرعي/دقيق) مع `metadata.taxonomy_source`
والقديم في `prev_topic`. غير المطابق (8,778) = مبادئ مستخرجة من أحكام كاملة (ليست في المعاجم).
ومن ملفي أحكام 2025 الجديدين: 625 حكمًا فُرز (389 مكرر بالرقم + 174 بالبصمة) فدخل **47 حكمًا
و69 مبدأً جديدًا** والعدّ بعدها **57,517 = 57,517**.
**دروس معيارية:** أدوار أنماط Word تُستقرأ من بصمة المحتوى (الذيل/النهاية بنقطتين/القِصر)؛
تبعيات الملفات تُحسم ببصمات التقاطع قبل أي معالجة (12⊂9 بنسبة 100%، 3⊂13 بنسبة 96%)؛ الجسر
الكبير للبيانات = المستودع العام + تشفير (قناة اللصق للمفاتيح والأوامر فقط)؛ وطباعة بايثون
الخلفية تحتاج flush وإلا بدا السجل ميتًا وهو يعمل.
**بقايا الجولة:** لا شيء — أُغلقت كل بنود المراجعة (انظر 8-رابعًا و8-خامسًا). التالي: الكتاب الخامس.

## 8-ثالثًا) إغلاق الأحكام الـ15 المحجوزة (2026-08-05)

أُعيد حجب أطرافها بنسخة v4 «المنطقة الكاملة» (من «صدر الحكم» حتى بداية الحيثيات: كل سطر يُحجب
إلى `[…]` إلا الهيكلي والجهات الرسمية والشركات؛ «الممثل القانوني» أُخرج من قائمة الإبقاء بعد رصده
ناقلًا اسم شخص) — 453 سطرًا حُجب وفحص أنماط الأسماء العام = صفر تسريب في مناطق الأطراف. الفرز
المزدوج كشف أن **14 من 15 مكررة** (12 بالبصمة + 2 بالرقم — من دفعات فبراير السابقة) فدخل **حكم
واحد** بـ`metadata.redaction_pass=v4_zone`. العدّ: **57,518 = 57,518**.
**درس معياري:** الحجب لا يُرقَّع سطرًا سطرًا بل يُقلب المنطق — حجب المنطقة كلها مع قائمة إبقاء
بيضاء أضيق (هيكلي/رسمي/شركات)، والتدقيق النهائي بفحص أنماط أسماء عام لا بعينات.

## 8-رابعًا) تدقيق ملف 17 وترقية ملف 13 (2026-08-05)

**ملف 17 (معجم 2002):** التدقيق ضد المصدر الخام كشف ضياع موضوع «اختصاص» كاملًا (عنوانه المستقل
+ سطر «القواعـد» + فهرس بنقاط قيادية فاتت المحلل فالتُصق بـ«أحوال شخصية»)، وأن آخر سطر من فهرس
كل قسم كبير يعلق «فرعيًّا» زائفًا بينما الفرعيات الحقيقية في خانة الدقيق. صُحح بحزمة 279 بصمة
(`taxonomy/f17_fix.json.gz.enc`) → **360 سجلًا** بعلامة `metadata.f17_fix`.
**ملف 13 (المعجم الهجائي الضخم):** أعيد تحليله من DOCX الأصلي (محفوظ في رفعات الجلسة) — تبين
أنه ليس عاري الأنماط: هرم أحجام خط (80=كتاب، 44+غامق+تسطير=موضوع، 36=فرعي، الشرطة=دقيق) في
منطقتي الحروف والموسوعة، وأنماط دلالية (`a0`=اسم القاعدة/`a1`=عنوان جانبي/`a7`=فرعي) في منطقة
الإثبات الختامية، وفهارس الحروف المنقوطة تُتخطى. النتيجة 177 موضوعًا/845 قسمًا؛ حزمة 6,443 بصمة
(`taxonomy/f13_fix.json.gz.enc`) → **8,876 سجلًا** ترقّى من التصنيف المسطح للشجرة الكاملة.
ومسبار «حرف %» = صفر — بند الـ87 مبدأً كان انحل سلفًا بتطبيق M1.
**دروس معيارية:** «عديم الأنماط» يُعاد فحصه بخصائص الخط (sz/b/u) قبل الحكم؛ فهارس الأقسام
(نقاط قيادية) تُتخطى وإلا لوثت حالة الفرعي؛ عناوين الكتب (سطر ≥50) مواضيعُ لا حاويات تُقفز
وإلا ابتلع الموضوع السابق ما بعدها؛ وأولوية «أول ظهور يفوز» عند تصادم البصمات داخل الملف.
**تنقية التسميات (2026-08-05 مساءً):** مُسحت علامات هامش المصدر «(*)» والنقطتان الذيلية من **1,397 سجلًا** (المدى فاق التقدير لأن التنقية شملت ذيول «:» المتخلفة من دفعات قديمة) + 715 موضعًا في الخريطة الرئيسية، بمزامنة كاملة بلا فشل — `DONE_STARCLEAN`.

## 8-خامسًا) إغلاق مجموعات الأحكام القديمة — ملفات 4/7/11 (2026-08-05)

**ملفا 4 و7 (أحكام كاملة بصيغة النشر المحرر):** فُصلا بترويسات «محكمة التمييز» مع دمج كتل
التكملة بعد فواصل الصفحات (ترويسة مكررة بلا سطر مرجع = تكملة سابقتها) → 520 حكمًا (338 إداري
1997-2013 + 182 متنوعة 2007-2012) و993 مبدأً من كتل الشرح المرقمة. الفرز: **520/520 مكرر** —
وتحقق مسبار مستقل أن كلها **مطابقة نصًا بالبصمة** لا تصادم أرقام (ملف 4 = دفعة admcol
يوليو حرفيًا 338/338). الصيغة المحررة **بلا أسماء أطراف أصلًا** (طاعن/مطعون ضده) فلا حجب.
**ملف 11 (فهرس مج42-43):** ليس فهرس إحالات بل معجم مبادئ موجزة بتبويب هجائي — 3,038 مدخلًا
صالحًا (أُسقطت 256 كتلة «أحكام كاملة» ملحقة مغطاة بمسار 4/7). الفرز بالبصمة: 2,636 مكررًا
و**402 مبدأ جديدًا** أُدخل بتبويب مصدره (`metadata.taxonomy_source.file=11`)، وفرع الفئتين
الإجرائيتين أُخذ من عرف القاعدة (طلبات رجال القضاء→إداري، هيئة عامة→مدني). فهرسة كاملة
(~6 ساعات) → **PG = Qdrant = 57,920**.
**دروس معيارية:** مطابقة الرقم وحدها تُتحقق بمسبار فرع+بصمة قبل تصديق نتيجتها (الأرقام
الصغيرة تتصادم عبر الفروع)؛ الفهرسة الفردية (reindex_one) لعشرات لا لمئات — فوقها إعادة
الفهرسة الكاملة؛ وسجل الفهرسة الكاملة صامت حتى النهاية — التقدم يُقرأ من عداد نقاط Qdrant.

## 8-مكرر) المؤجَّل والتالي

- ~~الفرع الإداري / ملفا فبراير 2025~~ **مُغلقان (2026-08-04):** المطابقة الشاملة (506 مقاطع ضد 1,792 حكمًا في القاعدة) أثبتت أن الملفين مدخلان جوهريًا من دفعات سابقة: 313/336 رقمًا موجودًا، و18 طعنًا مزدوجًا مغطى برقم شريكه الأول (والرقم الثاني في `appeal_nos` أصلًا)، و3 أحكام مجهولة الأرقام (`x-x`) رُممت بأرقامها وجلساتها (الطعنان 398 و516/2020 بنسختيهما + الطعن 494/2017 = `judgment-adm-x-x-05f8ab56`) بتوثيق `source_correction` وإعادة فهرسة — صفر إدخال مكرر. **درسان معياريان:** المطابقة بالمحتوى تكون ببصمة حروف عربية خالصة (تجريد الأرقام والمسافات وعلامات الاتجاه — بصمات النص الخام تفشل مع فروق التطبيع)، ومن مواضع مبكرة في النص لا ذيله (الذيول تُبتر). والحكم المزدوج («المرفوع أولهما من») يحمل رقمين: القاعدة تسميه بالأول والاستشهاد قد يأتي بالثاني.
- **تنقية العناوين الزائفة (2026-08-04):** حُذفت 10 «مبادئ» هي في حقيقتها عناوين أقسام فهرسية من دفعات قديمة («رابعًا: إجراءات الاستئناف:-»…) + سطر سند يتيم — بقائمة معرفات صريحة وقفل عدّ مزدوج PG/Qdrant، بعد التحقق أن تصنيفها كان مستهلَكًا أصلًا في topic/subtopic المبادئ التالية لها (صفر فقد). العدّ بعدها: **57,401 = 57,401**. قاعدة معالجات الاستخراج القادمة: العنوان يُلحق تصنيفًا بما يتبعه ولا يُسجَّل مستقلًّا أبدًا. (تنبيه تقني: حذف Qdrant بالمرشح غير متزامن — أعد قراءة العداد بعد لحظات لا فورًا.)
- ~~تحسينات الاستوديو: ربط القضايا المحفوظة، حفظ المسودات، تصدير Word~~ **مُغلقة** (التصدير
سابقًا، والربط والحفظ 2026-08-07 — انظر «ملف القضية الحي» في §6).
- عند إدخال فرع جديد: أنشئ topic/subtopic من **تبويب القانون الأصلي** (قرار ثابت من المالك).
- **انحياز مذهبي في قناة الفصول (رُصد 2026-08-10 بفحص حي):** سؤال ميراث مطلق المذهب جلب
`chap_extra` فصل الأنصبة الجعفري (م497-499 ق124/2019) حتمًا ولم يجلب نظيره السني من ق51/1984
— والإجابة أفصحت بصدق بدل الاختلاق. جولة ضبط مستقلة: مطابقة فصول الميراث تُخرج الفصلين معًا
عند إطلاق المذهب. (وفي الفحص نفسه: كل الاستشهادات المدققة حقيقية — 5/1959 كامل 72 صفًا،
واللائحة 1496/2020 محفوظة ببادئة `regl-21-2019-*`، والطعن 558/2010 موجود بموضوعه.)
- **فحص حي ثانٍ (تجاري — عيب مبيع بالتقسيط، 2026-08-10):** صفر اختلاق (مرسوم 10/2026 التجارة
الرقمية موجود وفصل إعلاناته جُلب حاكمًا عبر-قانونيًا)، **وعيّنة حية من «الخلط الدقيق»** الموثق
حدًّا معرفيًا: م54 مدني (إعلان الأصيل عن سلطة نائبه — نيابة اتفاقية) أعيدت صياغتها «أعلن عن
أمرٍ» وعُمّمت مبدأً لحجية الإعلانات التجارية، بتحوط «يُستأنس» — لا يلتقطها إلا التدقيق الخارجي.
بنود ضبط: م11 ق39/2014 (بطلان شرط الإعفاء) وم25 لائحة 27/2015 (مدد الرد) غابتا وهما قلب
السؤال؛ فصل «ضوابط استبدال السلعة» لم يُصب رغم «استبدالها» في السؤال؛ وشرارة بوابة نسب
(legis-53-2026-m8) في سؤال بلا نسب — تُفحص مفاتيح البوابات على مجموعة أسئلة محايدة.

## 9) جولة إدخال الجريدة الرسمية (2026-08-08) — خمسة قوانين و113 كائنًا

**أُدخل وفُهرس واعتُمد ببطارية 11/11:** مرسوم بقانون 72/2026 دور العبادة (36) + مرسوم بالقانون
105/1980 أملاك الدولة، القانون فقط بأمر المالك (22) + قرار وزاري 109/2026 لائحة المنصات
الإلكترونية الوسيطة مع مذكرتها ودليل جهاز حماية المنافسة (38) + قرار وزاري 8/2026 سحب القسائم
— الهيئة العامة للصناعة (12) + مرسوم بقانون 148/2025 المعاملات الإلكترونية (5). العدّ بعدها:
PG=Qdrant، والدفعة كلها في `DEPLOY_ingest_all` بنمط اللصق المعتاد.

**قواعد دائمة بأمر المالك رسختها الجولة (تسري على كل إدخال قادم):**
1. **قاعدة التحديث:** القانون الأحدث يستبدل نص المادة القديمة **على معرّفها نفسه** والنص القديم
يُحفظ في `metadata.previous_versions[]` (تاريخ + بصمة + سند التعديل) — لا يُمحى نص قط،
والملغاة تُوسم `repealed_by` ولا تُحذف، والتقرير يطبع جديد/محدَّث/بلا تغيير. طُبقت فعليًا:
م2 وم3 المعاملات الإلكترونية 20/2014 نصاهما النافذان (148/2025) على معرّفيهما الأصليين
وتبويبهما القائم لم يُهدر.
2. **تنقية الناشر:** لا يدخل القاعدة اسم مُعِدّ مرجع ولا هاتف/بريد/موقع/علامة مائية
(المرجاح/almirjah/Lawskw/Mojconcourt/DASMAN) — بوابة `pubscrub.py` على كل حزمة قبل التعبئة.
3. **ما استُبعد بأمر المالك:** جداول المواد المخدرة (أسماء كيميائية لاتينية يستحيل ضمان أمانتها
من مسح ضوئي — خطأ حرف في قضية جنائية كارثة) وتعديلات جمعية السدو.

**دروس معيارية للجولة:** طبقات نص PDF تكذب بأنماط ثلاثة (ترميز مقلوب «املنصات»، سقوط أواخر
«كويت ي»، استبدال حروف «تنميم») — فالنقل بصري أو بمطابقة مصدرين (طبقة نص × OCR، القياس 84%
اتفاقًا) وكل تصحيح يُوثق و«بوابة الأمانة» تقارن كلمةً بكلمة (47 فرقًا كلها موثقة في 72/2026)؛
والمضطرب يُعلَّم `extraction_uncertain` ولا يُخمَّن. `source_key` للتشريعات **فارغ** (قيد أجنبي
على جدول `sources` — يُتبع الاصطلاح القائم لا يُخمَّن). التشغيل الطويل يُغلَّف بـ`nohup` داخل
ملف اللصق نفسه (غلاف `--worker`) فيصمد لانقطاع الطرفية. واختبار الاسترجاع بعد الإدخال يُرشَّح
بقناة التشريعات لا بالبحث الحر — مبادئ التمييز تزاحم بحق في القاعدة الحرة (م19 أملاك الدولة
جاءت ثانية على التشريعات كلها وكان معيار «مادتين» هو الخاطئ).

**جولة «اكمل المؤجل» (2026-08-08 مساءً) — خمس دفعات و136 كائنًا فوق دفعة الصباح:**
- **b2 (64):** ق61/2026 المنشآت البحرية كاملًا (ديباجة + 7 إصدار + م1–44 + مذكرة بكتلتين) +
الأحكام الدستورية الأربعة `full_judgment` بمبادئها الستة `jprin-const-*` مربوطةً عبر
`source_judgment_id` — **وفرع «دستوري» أُنشئ**. الحجب: الأطراف الطبيعيون `[…]` والقضاة
والبنوك والشركات والجهات باقية. `INGEST_B2_PASS` + بطارية 11/11.
- **b3 (13):** ق.و 11/2026 اللائحة التنفيذية لقانون مكافحة احتكار الأراضي الفضاء 126/2023
(رسم 10 د.ك/م² فوق 1500م² من 1/3/2026؛ تضامن الملاك بإحالة م1072 مدني؛ تظلم 60 يومًا؛
حظر تعامل الجهات مع المتخلفين). `INGEST_B3_PASS`. **معلق:** المسبار أظهر 32 نقطة مفهرسة
لبادئة `legis-11-2026-%` والحزمة 13 — شبهة تصادم ترقيم تُحسم بمسبار SELECT قبل أي حزمة
جديدة برقم قرار (لا قانون).
- **b4 (33):** ق.و 152/2023 مقيمو العقار **بنصه النافذ بعد تعديل 99/2025** — قاعدة التحديث
مطبقة في بناء الحزمة: م18 (توفيق حتى 31/12/2027) وم22 (النموذج 60 يومًا) والتعريف المضاف
(الوحدات الخاضعة لرقابة البنك المركزي) والبند 7 من الملحق 2 نافذةً في المتن والأصل في
`previous_versions`؛ **وترقيم الملحق 2 الأصلي مكرر للبند 7** موثق `extraction_uncertain`.
الملاحق الأربعة كائنات مستقلة `legis-152-2023-annex-N`. `INGEST_B4_PASS`.
- **b5 (26):** ق.و 164/2020 تنظيم مهنة السمسرة العقارية كاملًا (أجر 1% مناصفة م15؛ شروط
الاستحقاق م16؛ سجل الصفقات 5 سنوات م17؛ الجزاءات م19–21). اكتُشف في ذيل ملف مقيمي العقار
(ص8–12، ع1508 بترميز مدمَّر) ونُقل بصريًا بأمر المالك. `INGEST_B5_PASS`.
- **درس بنيوي:** ملفات المجمعات تحمل أكثر من جريدة — تُفحص ترويسة كل صفحة قبل افتراض
وحدة المستند (ملف «مقيمي العقار» حمل ثلاث وثائق من ثلاث سنوات مختلفة).

**جولة تجهيز «جاهز عند العودة» (2026-08-09) — الطابور أُفرغ كله في خمسة ملفات DEPLOY:**
حُزم الطابور المؤجَّل الأربعة كلها وسُلّمت جاهزة للصق بالترتيب: `DEPLOY_ing_nat` (الخدمة
الوطنية 20/2015 النافذ بمرسوم 9/2026 — 59) ← `DEPLOY_ing_qsr` (شئون القصّر 67/1983 النافذ
بمرسوم 145/2025 + لائحة 4/1977 — 44) ← `DEPLOY_ing_bld` (البلدية 33/2016 النافذ بمرسوم
59/2026 + لائحة 1358/2018 — 74) ← `DEPLOY_ing_cons` (لائحة حماية المستهلك 27/2015 + **قانون
39/2014 نفسه** — 111) ← `DEPLOY_idx_home` (فهرسة الـ288 بتحميل واحد + 5 اختبارات استرجاع
مرشحة بالتشريعات + مسبار تصادم legis-11-2026 المعلق + البطارية؛ النجاح = `DONE_HOME_ALL`).
كلها بنمط b5 (nohup --worker، قاعدة التحديث، source_key NULL) وبوابات pubscrub والتسلسل الكامل.
**اكتشافان بنيويان في ملف حماية المستهلك:** (1) المجمع ثلاثي — اللائحة **بنسختين متطابقتين**
(ص1-18 وص26-43 بإزاحة 25) صولحتا كلمةً كلمة شاهدَين، وبينهما قانون 39/2014 نفسه (جريدة 1187)
فأُدخل أيضًا؛ صفحاته وملحق الشروط هندستها غير متمركزة فأعيد شقّ أعمدتها **بكشف حضيض الحبر**
(pipe/resplit_law.py) بدل منتصف ثابت. (2) **درس معياري قيس فعليًا:** ترويسات صفحات الجريدة
تُحذف سطرًا سطرًا **قبل** التصالح — بعده تندمج الصفحة سطرًا واحدًا فيمحو حذفُ «سطر الترويسة»
الصفحةَ كلها وكل مادة عابرة لحدود صفحة تفقد تكملتها صامتة (م25 اللائحة: 179 حرفًا بدل 3227؛
الإصلاح استرد +14 ألف حرف). التصحيحات الموثقة كلها تحققت من النسخة الشاهدة (ترويسات 213→13،
216→16، 368→36، «ةدام» المعكوسة م11، متن م35 المستكمل من قراءة الصفحة الكاملة، سطر «الفصل
التاسع» الساقط)، والملحق موسوم extraction_uncertain لتفاوت جودته.
**تنفيذ الجولة (2026-08-10):** الدفعات الأربع أُدخلت (288) وفُهرست واختُبرت — `DONE_HOME_ALL`
والبطارية 11/11 (الكائن الـ289 = ديباجة بلدية قديمة مشروعة).
**تصادم legis-11-2026 — فُكّ جراحيًا (`DEPLOY_fix11`):** مسبار b3 المعلق كشف أن البادئة كانت
تحمل قانونين: دفعة b3 (القرار الوزاري 11/2026 لائحة الأراضي الفضاء) **كتبت فوق** م1-م12
وديباجة **المرسوم بقانون 11/2026 الحماية من العنف الأسري** المدخل قبلها — وقاعدة التحديث
أنقذت النصوص كلها في `previous_versions` فاستُردت حرفيًا على معرفاتها (بتوثيق
`source_correction` وتبويب مأخوذ من الشقيقات الناجيات م13-م31 لأن الأصلي فُقد بالكتابة
الفوقية)، ونُقلت اللائحة كاملة إلى البادئة المميزة `legis-mof-11-2026-*`، ونقاط Qdrant
اليتيمة (معرف النقطة ببصمة المحتوى يجعل الكتابة الفوقية تخلّف يتامى) حُذفت بالمرشح وأعيد
فهرسة الـ26. **قاعدة دائمة رسّخها الحادث:** قبل سكّ معرف لقرار وزاري/لائحة برقم وسنة،
مسبار `SELECT` على البادئة وجوبًا — أرقام القرارات تتصادم مع أرقام القوانين والمراسيم
(والبادئات القائمة المعرضة: legis-27-2015، legis-109-2026، legis-8-2026، legis-4-1977).
**درس معياري متجدد:** `cmd | tail` في بوابة اعتماد يُخفي رمز فشل الأمر الأول (رمز الأنبوب
لآخر أوامره) — فمرّ `BATTERY_FAIL` بسطر `DONE` كاذب؛ كل بوابة تُكتب بـ`set -o pipefail`
أو بفحص السجل بـ`grep` حاكمًا.

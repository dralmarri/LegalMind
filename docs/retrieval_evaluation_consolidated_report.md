# LegalMind Retrieval Evaluation — Consolidated P0/P1 Report

**تاريخ الإصدار:** 2026-09-14
**النطاق:** توحيد نتائج P0 (إصلاحات مؤكَّدة) + P1 (إطار القياس) + Pass A (تشغيلة واحدة) + Pass B
(استقرار 5 تكرارات) في وثيقة مرجعية واحدة، قبل أي قرار معماري (P2).
**قاعدة حاكمة لهذه الوثيقة:** لا تجميل للنتائج. كل رقم يُنسب لمصدره (أي أداة، أي تشغيلة، أي
تاريخ). حيث لا يوجد قياس مباشر، يُذكر ذلك صراحة بدل الافتراض.

---

## A. الملخص التنفيذي (بلا تجميل) — نسخة مصحَّحة 2026-09-14

- **صحة القاعدة:** PostgreSQL = Qdrant = 62,498 كائنًا وقت هذا التحقق (بعد إدخال ثلاثة مراسيم
  87/88/89 لسنة 2026 — 194 كائنًا، لا علاقة موضوعية بتقييم الاسترجاع). **تنبيه إعادة إنتاج:**
  Pass A تحديدًا رُكِّض على لقطة أقدم (62,304، قبل هذه الدفعة) بينما Pass B على 62,498 — انظر
  القسم D-مكرر.
- **P0** أصلح خمسة أعطال محددة مثبتة بالكود — **ولم يقس التغطية الفعلية على نطاق واسع**، وهذا
  بالضبط ما تسده هذه الوثيقة.
- **Pass A** (تشغيلة واحدة، 40 حالة): `raw_candidate_union` Recall = **97.6% (41/42)**؛
  `final_context` Recall = **92.9% (39/42)** لـ`must_find`، **85.7% (6/7)** لـ`must_not_miss`.
  **تنبيه مصطلحات:** `raw_candidate_union` (أداة `gold_metrics.py`) و`draft_hits_pool` (أداة
  `survival_metrics.py`) **ليستا نفس التجميعة بنيويًا** — ثلاثة فروق كود مؤكَّدة (القسم F-مكرر)،
  فلا يُقارَن 41 بـ40 حسابيًا كأنهما نفس المقياس.
- **Pass B** (5 تكرارات، 20 حالة صعبة/عدائية، 27 هدفًا): 20 مستقر تمامًا (5/5)، 1 تذبذب طفيف
  (4/5)، 6 غير مستقر — **مُقسَّمة بدقة حرفية (لا تقديرية) إلى فئتين لا ثلاث:**
  1. **Discovery Failure/Instability** (5 أهداف: `m30` حتمي 0/5؛ وأربعة أخرى تذبذب 3-4/5) —
     في كل هذه الحالات **Discovery = Survival تمامًا**: بمجرد الاكتشاف، القبول مضمون 100%.
  2. **Evidence Admission Failure** (هدفان من نفس القضية `gs-0013`: `m166` اكتشاف 5/5/نجاة 0/5،
     `m167` اكتشاف 5/5/نجاة 1/5) — الاكتشاف شبه مضمون بصرف النظر عن صياغة المحاور؛ الفشل بنيوي
     بحت في الترتيب/الميزانية، **لا علاقة له بعشوائية Haiku إطلاقًا**.
- **مقاييس مستقرة (مصحَّحة، القسم J):** Fully Stable Authorities 20/27=74.07% |
  Stable-or-Near-Stable (≥4/5) 21/27=77.78% | Observed Run-Level Survival 114/135=84.44%.
  **لا يوجد بعد** رقم Complete-Run (Best/Worst-case) حقيقي — يحتاج مصفوفة سلطة×تشغيلة خام غير
  محفوظة حاليًا (القسم J).
- **✅ الفرضية السببية اختُبرت ونجحت (القسم S، 2026-09-14):** تجربة Frozen Axes المضبوطة (5
  حالات × نمطين × 5 تشغيلات = 50 تشغيلة) أثبتت بدليل مباشر عالي الثقة أن عشوائية تفكيك Haiku
  **سبب مباشر مؤكَّد** لـ Discovery Instability في **أربعة من أربعة** أهداف الاختبار الأساسية —
  تجميد المحاور على أي تشغيلة واحدة يُعيد إنتاج نتيجتها (نجاحًا أو فشلًا) **حتميًا بصفر تباين**
  في التشغيلات الخمس التالية. الضابط السلبي (`m167`) أكَّد استقلال الاكتشاف عن المحاور كما
  تنبَّأنا، لكنه كشف مسارَ إنقاذ دلالي غير متوقَّع يجعل نجاته الفعلية متأثرة جزئيًا بالمحاور
  أيضًا — تنقيح للتصنيف لا نقض له.
- **القرار المعلَّق:** لا تعديل معماري (Hybrid Search / Scope Planner / Gates جديدة / رفع
  الميزانية / تغيير Haiku) حتى تُراجَع نتائج القسم S وتُقرَّر خطوة P2.

---

## B. خط الأساس المعماري قبل P0

خط الاسترجاع (قبل أي تعديل من هذه الجولة): تفكيك محاور بحث عبر Haiku (3-6 محاور) ← تضمين
جماعي ← بحث Qdrant متعدد القنوات (دلالي مباشر + حزم الفصول الحاكمة `_GATES`/`_draft_chap_ids`
+ الاستشهاد المباشر `_draft_direct_ids` + المعجمي + XREF + الشقيقات + prin-xref) ← دمج ←
سقف مرحلة أولى `caps_n=20` على القناة الكثيفة فقط ← مرتِّب متقاطع (cross-encoder) على اتحاد كل
القنوات (سقف 300 زوج) ← قصّ بميزانية أحرف لكل نوع كائن (`LBL_BUDGET`) ← سياق نهائي. لا فهرس
متفرّق (Sparse/BM25)، لا مخطِّط نطاق (Scope Planner)، لا طبقة اعتماد أدلة (Evidence Admission
Policy) مطبَّقة إنتاجيًا حتى تاريخه — كلها مقترحات تصميم موثَّقة في `docs/p1_retrieval_evaluation_framework.md` (الأقسام N-2/O) دون تنفيذ.

---

## C. إصلاحات P0 المنفَّذة (§21 من CLAUDE.md)

خمسة إصلاحات، كل واحد مقيَّد الأثر عمدًا، صفر تغيير في منطق الترتيب أو التشابه الدلالي نفسه:

1. **`kb_types.py`** — مصدر تصنيف مركزي وحيد استبدل تعدادات `object_type` المبعثرة في 7
   مواضع فلترة مختلفة (`app.py`, `mcp/server.py`, `cases_api.py`, `battery_run.py`).
2. **`is_legislation_id()`** — يعتمد على بادئة المعرف (`legis-`/`LEG-`/`regl-`/`lreg-`) بدل
   تعداد أنواع، فيشمل الأنواع الكاملة الغائبة تمامًا سابقًا: `legislation` (2,606 كائنًا) و
   `legislation_archived` (307 كائنًا).
3. **تعقّب الفقد (Candidate Attrition)** — عمود `draft_evidence.attrition` (jsonb) + سطر
   يوميات `[draft] attrition:` يسجّل عدّادات مرحلية لكل طلب حي — أول ظهور فعلي لهذه الأرقام.
4. **جمع نسخ المبادئ قبل السقف لا بعده** — بصمة النص المطبَّع نفسها المستعملة سابقًا فقط عند
   بناء السياق صارت تُطبَّق قبل سقف المرحلة الأولى (بعد تحقق سلامة يدوي على عيّنات حقيقية أثبت
   صفر دمج خاطئ).
5. **تعقّب إسقاط الميزانية** — `budget_dropped_count`/`by_type` كاملين دومًا بدل `continue`
   صامت.

**البطارية وُسِّعت 13→15 حالة** (إضافة حالتين تستهدفان `legislation`/`legislation_archived`
تحديدًا)، واعتُمدت بثلاث تشغيلات متتالية 15/15 + نداءا `_draft_run` حقيقيان أثبتا وصول
المحتوى فعليًا للسياق النهائي.

---

## D. سلامة المتن (Corpus Integrity)

- تزامن PostgreSQL/Qdrant تام في كل نقاط الفحص طوال الجولة (آخر رقم مؤكَّد: 62,498 = 62,498
  بعد دفعة 87/88/89-2026، بطارية 15/15 مؤكَّدة بعدها مباشرة).
- بادئتا `query:`/`passage:` لنموذج E5 مُلزَمتان برمجيًا (`ValueError` عند الخطأ) — لا خطر
  خلط بادئات.
- البحث المعجمي `LIKE` بسيط، لا BM25/FTS حقيقي — نقطة ضعف معمارية معروفة، غير مُقاسة الأثر
  مباشرة في هذه الجولة.
- لا `qdrant_client` مثبَّتًا في أي بيئة — كل الاتصال HTTP خام؛ إضافة متجه متفرّق تتطلب إعادة
  بناء المجموعة أو مجموعة موازية.

---

## D-مكرر. اللقطة الزمنية للمتن (Corpus Snapshot) — موثَّقة صراحة، لا مخفاة

**تحقُّق مباشر من الكود والقاعدة (2026-09-14):**

| البند | القيمة |
|---|---|
| git commit حي (`/opt/LegalMind`) وقت هذا التحقق | `2edb2193648490fca9288ecfab059d6cd87df436` — 2026-09-14 08:46:21 +0300 |
| PostgreSQL count (وقت هذا التحقق) | 62,498 |
| Qdrant points_count (وقت هذا التحقق) | 62,498 (متطابق) |
| Gold Set hash (sha256، أول 16 خانة) | `5c54cecdcf2ac284` — 40 حالة |

**تباين موثَّق صراحة بين Pass A وPass B — لم يكونا على نفس اللقطة:**
دفعة المراسيم 87/88/89 لسنة 2026 (194 كائنًا، **لا علاقة موضوعية بمجال Gold Set إطلاقًا**) أُدخلت
وفُهرست **بين** تشغيلة Pass A وتشغيلة Pass B زمنيًا. الاستنتاج الحسابي المباشر:
- **وقت Pass A** (gold_metrics.py + survival_metrics.py، النتيجة 97.6%/92.9%/85.7%):
  **62,304 كائنًا** (= 62,498 الحالي − 194 المُضافة لاحقًا).
- **وقت Pass B** (stability_metrics.py، 20 حالة × 5 تكرارات): **62,498 كائنًا** (بعد الدمج
  والفهرسة التفاضلية للمراسيم الثلاثة).

**الأثر المحتمل غير المقاس:** إضافة 194 نقطة إلى فهرس Qdrant (حتى لو بمجال قانوني مختلف تمامًا)
تُعيد بناء جزئيًا لبنية الجوار التقريبي (ANN/HNSW) نظريًا، فقد تُزحزح ترتيب أقرب الجيران زحزحة
طفيفة لأي استعلام، بما فيها استعلامات Gold Set. **لم يُقَس هذا الأثر تحديدًا في هذه الجولة** —
لا يوجد سبب للاعتقاد أنه كبير (194 نقطة من أصل 62 ألفًا، ومجال موضوعي مختلف تمامًا: عمل خيري/
دوائر اقتصادية/طائرات بدون طيار مقابل مدني/تجاري/أحوال شخصية)، لكن **الأمانة تقتضي تسجيله لا
افتراض عدم تأثيره**. أي محاولة إعادة إنتاج (reproduction) لأرقام Pass A تحديدًا يجب أن تُجرى إما
على لقطة 62,304 (لن تتكرر — الفهرسة تراكمية) أو مع تسجيل هذا الفارق صراحة كمتغيّر غير مضبوط.

**لا تغيير مطلوب بأثر رجعي:** كلا التشغيلتين استعملتا **نفس** إصدار Gold Set (40 حالة، البصمة
أعلاه لم تتغير بينهما) ونفس كود `_draft_build_context` (لا نشر كود بين التشغيلتين) — الفارق
الوحيد هو المتن (194 كائنًا غير ذات صلة موضوعية)، لا المنطق ولا معايير القياس.

---

## E. مجموعة Gold Set

**الحجم الكلي: 40 حالة** (v2 schema كامل). التوزيع:

| البُعد | التوزيع |
|---|---|
| `case_type` | إيجابية 36 \| سلبية (ضوابط) 4 |
| `negative_reason` (للسلبية) | خارج النطاق 3 \| مستبعد بسياسة 1 |
| `difficulty` | متوسط 17 \| صعب 13 \| عدائي 7 \| سهل 3 |
| `known_preexisting_failure=true` | 4 حالات: gs-0017, gs-0034, gs-0035, gs-0036 (فجوات
  رُصدت حيًّا عبر MCP **قبل** أي تشغيلة قياس — لا اجتزاء بعدي) |

**تصحيح موثَّق قبل أي قياس نهائي:** `gs-0038` كان معرّفه `jprin-101-1995-f1574-a90c7a3e5f`
(منقول من نص وكيل فرعي دون تحقق مباشر وقت الإضافة، رغم تحذير مسجَّل صراحة في
`must_find_provenance` وقتها) — Pass A الأول كشف أنه **لا كائن بهذا المعرف إطلاقًا**؛ صُحح
إلى `jprin-101-1995-f1574-ce8b49f13f` (تحقُّق مزدوج `search_legal`←`get_object`)، ورفع
Candidate Recall المُبلَّغ من 95.24% إلى 97.6% الصحيح. **درس مباشر:** فشل عنصر واحد في مقياس
لا يُصنَّف تلقائيًا "فشل نظام" قبل التحقق من صحة معرّف الحقيقة الأرضية نفسه.

**قرار موثَّق برفض فبركة فئة:** بحث ثلاثي مستقل عن حالة `conflicting_authorities` حقيقية
(تعارض فعلي متزامن بين سندين) لم يُثمر — أُعلن **صفر حالات** من هذه الفئة بدل فرض حالة ضعيفة،
وأُعيد تصنيف زوج "حبس احتياطي عام/خاص" إلى `exception` بدل ذلك.

---

## F. Pass A — القياس أحادي التشغيلة

**الأداة:** `gold_metrics.py` (Candidate Recall الخام — `retrieve()` بلا `caps_n`/مرتِّب/
ميزانية) ثم `survival_metrics.py` (قمع النجاة الكامل حتى السياق النهائي، عبر
`_draft_build_context` الحقيقية).

**النتيجة (بعد تصحيح gs-0038):**

| المقياس | القيمة | ملاحظة |
|---|---:|---|
| Candidate Recall (must_find) | **41/42 = 97.6%** | الفشل الوحيد: `legis-1-2016-m30` — لم يدخل المرشحين إطلاقًا |
| Must-Not-Miss Failure (على مستوى الاكتشاف) | 0/7 = 0% | — |
| **Recall الكامل حتى السياق النهائي** (must_find) | **39/42 = 92.9%** | 3 حالات فقد: انظر الجدول أدناه |
| **Recall الكامل** (must_not_miss) | **6/7 = 85.7%** | فقد واحد: `legis-38-1980-m166` (ميزانية) |

**تصنيف الفقد الثلاثة (تشغيلة واحدة، قبل التحقق بخمسة تكرارات):**

| المعرّف | القضية | مرحلة الفشل (Pass A) |
|---|---|---|
| `legis-1-2016-m30` | gs-0036 | `candidate-generation` — غاب عن `hits` كليًا |
| `regl-1-2016-m65` | gs-0036 | `cap` — دخل `hits` لكن أُقصي قبل المرتِّب |
| `legis-38-1980-m166` | gs-0013 | `budget` — نجا للمرتِّب ثم أُقصي بالميزانية (رتبة ~144-157) |

**التوزيع حسب `retrieval_class` (Must-Find Recall):** كل الفئات حققت 100% عدا: `legal_dependency`
(14/17)، `semantic_match` (19/21)، `multi_law` (7/8)، `cross_reference` (10/11)، و**`multi_issue`
(1/3 — الأضعف نسبيًا)**، وكل خسارتَي `multi_issue` تقعان في حالة `gs-0036` وحدها لا موزَّعة عبر
الفئة.

**درس Pass A:** بيانات لحظة واحدة كافية لتحديد **أين** يقع الفقد، لكنها **لا تكفي** للحكم هل
الفقد ثابت أم عابر — وهذا بالضبط ما تسده Pass B.

### F-مكرر. تسوية مصطلحات "Candidate" — من الكود مباشرة، لا من الذاكرة

**السؤال المطروح:** لماذا `gold_metrics.py` أعلن Candidate Recall = 41/42 بينما `survival_metrics.py`
(عبر `_draft_build_context`) أظهر `42 → hits(40)`؟ الجواب من قراءة مباشرة لكود الدالتين (لا تخمين):

`gold_metrics.py` يستدعي `retrieve()` المعرَّفة في `tools/battery_run.py` — وهي **دالة منفصلة
تمامًا** عن `_draft_build_context`، أُعيد بناء منطقها يدويًا في وقت سابق لمحاكاة الاسترجاع بلا
كلفة توليد، **وليست استدعاءً مباشرًا لنفس الدالة الإنتاجية**. المقارنة الحرفية:

| البُعد | `retrieve()` (يستعمله `gold_metrics.py`) | `_draft_build_context` (الإنتاج الحقيقي) |
|---|---|---|
| أنواع الدلو الدلالي | قائمة حرفية قديمة: `["legislation_article","legislation_issuing_article"]` / `["judicial_principle"]` — **لم تُحدَّث بإصلاح P0 (`kb_types.py`)** | `_kb.LEGISLATION_TYPES` / `_kb.PRINCIPLE_TYPES` الموحَّدة — **تشمل `legislation`/`legislation_archived`** |
| سقف مرحلة أولى (Stage-1 Cap) | **لا يوجد** — فقط حدود الدلاء الأولية (10/10/2/2 للاستعلام الكامل + 8/8 لكل محور فرعي) | **يوجد**: `caps_n` = 20 تشريعًا / 32 مبدأً / 3 أحكام / 3 نماذج، يُطبَّق على `best` بعد الدمج بالنقطة الأعلى عبر كل المتجهات |
| قنوات التوسعة | `_draft_bundles` + `_draft_direct_ids` + `_draft_chap_ids` + `_draft_lexical` فقط | كل ما سبق **+ `_XREF`** (إحالات خارجية صريحة، سقف 24) **+ `_sib_expand`** (مواد شقيقة) **+ `_prin_xref`** (إحالة مبدأ لتشريع) |

**الخلاصة الحاسمة: هذان ليسا نفس Candidate Pool بنيويًا — ثلاثة فروق مؤكَّدة من الكود مباشرة**،
بصرف النظر عن أي تذبذب Haiku. **إعادة تسمية معتمدة من الآن (توثيق فقط، صفر تغيير كود):**

- **`raw_candidate_union`** = مخرَج `retrieve()`/`gold_metrics.py` (كان يُسمى Candidate Pool
  ضمنيًا — الاسم مضلِّل لأنه ليس نفس اتحاد الإنتاج).
- **`draft_hits_pool`** = `ctx["hits"]` من `_draft_build_context` (بعد كل التوسعات، قبل الترتيب
  النهائي) — كان يُسمى `hits` فقط في القسم K، رُقِّم الاسم لتمييزه صراحةً عن `raw_candidate_union`.
- **`reranker_pool`** = المجموعة الفعلية المُمرَّرة للمرتِّب المتقاطع (`sc < 0.99` من `hits`).
- **`final_context`** = `ctx["seen"]` (المقبول فعليًا بعد الميزانية) — كما كان.

**هل عدم الحتمية (Haiku) وحده يفسّر الفرق 41/42 مقابل 40/42؟ لا — العاملان متشابكان:**
الفروق البنيوية الثلاثة أعلاه حقيقية وقائمة حتى لو كان تفكيك Haiku حتميًا 100%؛ وعدم حتمية
Haiku (تشغيلتان منفصلتان، صيغ محاور مختلفة حتمًا) يضيف طبقة تذبذب إضافية فوقها. **لا يمكن فصل
نصيب كل عامل من الفرق المشاهَد في هاتين التشغيلتين تحديدًا بلا تجربة مضبوطة تُشغِّل الأداتين
على نفس المحاور المجمَّدة بالتزامن** — وهذا خارج نطاق تجربة Frozen Axes المخطَّطة حاليًا (التي
تقارن Normal مقابل Frozen **داخل** `_draft_build_context` وحدها، لا بين الأداتين). **لا خطر
عملي من هذا التمايز:** `raw_candidate_union` مقياس تشخيصي مساعد فقط؛ الرقم الحاكم فعليًا لتقييم
الإنتاج هو دومًا `final_context` عبر `_draft_build_context`/`survival_metrics.py`.

---

## G. Pass B — قياس الاستقرار (5 تكرارات)

**الأداة:** `stability_metrics.py` — يشغّل `_draft_build_context` خمس مرات مستقلة (بتصفير كاش
الفهرس بين كل تكرار) على **20 حالة** (كل الحالات الموسومة `hard`/`adversarial` — تغطي تلقائيًا
الحالتين الفاشلتين في Pass A + كل حالات `known_preexisting_failure`)، لا الأربعين كلها (ضبط
كلفة متعمَّد). المدة: 3,312 ثانية (~55 دقيقة) لـ100 تشغيلة استرجاع كاملة.

**النتيجة الإجمالية: 27 هدفًا قِيس عبر 17 حالة** (3 من العشرين استُبعدت تلقائيًا: لا `must_find`
لقياسه) — **20 مستقر (5/5) | 1 تذبذب طفيف (4/5) | 6 غير مستقر (≤3/5)**.

**الأهداف غير المستقرة/الطفيفة السبعة بالتفصيل — أرقام Discovery/Survival الحرفية (مستخرجة
مباشرة من JSON الكامل، 2026-09-14 — لا تقدير):**

| المعرّف | القضية | Discovery (اكتشاف) | Survival (نجاة) | مدى الرتبة | التصنيف الدقيق |
|---|---|---:|---:|---|---|
| `legis-1-2016-m30` | gs-0036 | **0/5** | **0/5** | None | **Discovery Failure صرف** (حتمي، لا تذبذب) |
| `legis-38-1980-m166` | gs-0013 | **5/5** | **0/5** | 148-157 | **Evidence Admission Failure صرف** — مكتشَف دومًا، مرفوض دومًا |
| `legis-38-1980-m167` | gs-0013 | **5/5** | **1/5** | 40-83 | **Evidence Admission Failure** (نفس توقيع m166 — مكتشَف دومًا، نجاته النادرة الوحيدة استثناء لا قاعدة) |
| `regl-1-2016-m65` | gs-0036 | 3/5 | 3/5 | 22-28 | **Discovery Instability صرف** — كلما اكتُشف، نجا (لا فقد إضافي عند القبول) |
| `LEG-UNKNOWN-P1-art2-3-6144aa963fc48469` | gs-0015 | 3/5 | 3/5 | 30-32 | Discovery Instability صرف |
| `legis-38-1980-m298` | gs-0001 | 3/5 | 3/5 | 27-29 | Discovery Instability صرف |
| `legis-6-2010-m50` | gs-0035 | 4/5 | 4/5 | 68-85 | Discovery Instability صرف (طفيف) |

**اكتشاف مهم غيَّر التصنيف الأولي:** الأرقام الحرفية أثبتت أن `m167` **ليست** حالة "تذبذب
اكتشاف عادي" كما صُنِّفت مبدئيًا — بل تحمل **نفس توقيع m166 بالضبط** (اكتشاف 5/5، نجاة شبه
معدومة). **كل الأهداف غير المستقرة السبعة تنقسم بنظافة تامة إلى فئتين لا أكثر:**
1. **Discovery = Survival تمامًا** (`m65`, `gs-0015 target`, `m298`, `m50`, وكذلك `m30` بصفر
   مطلق): **بمجرد الاكتشاف، القبول مضمون 100%** — التذبذب كله واقع في مرحلة الاكتشاف، لا القبول.
2. **Discovery ≫ Survival** (`m166`, `m167` فقط، كلاهما من `gs-0013`): **الاكتشاف شبه مضمون
   (5/5)، لكن القبول نادر أو معدوم** — هذا توقيع فشل قبول دليل، لا فشل اكتشاف، بصرف النظر عن
   تفاوت نص محاور Haiku.

هذا التمييز (غير ممكن بلا الأرقام الحرفية) يُقوّي الفرضية القائمة بدل أن يعقّدها: **مصدر
التذبذب في الفئة الأولى مرشَّح جيد لعشوائية Haiku (يفسّر لماذا لا يُكتشف أحيانًا)، بينما فشل
الفئة الثانية (`m166`/`m167`) لا علاقة له بعشوائية Haiku إطلاقًا** — فهما يُكتشَفان دومًا بصرف
النظر عن صياغة المحاور؛ فشلهما بنيوي بحت في مرحلة الترتيب/الميزانية (نفس حزمة "أمر أداء"
الحاكمة، موضعهما الداخلي فيها متأخر).

**إجمالي: 17/17 حالة مقيسة أظهرت `subquery_variance=true`** (Haiku أنتج 5 صيغ محاور مختلفة تمامًا
في كل حالة، بلا استثناء واحد) — انظر القسم L للتمييز الدقيق بين هذه الملاحظة ونتيجة الاسترجاع.

---

## H. تصنيف الفشل (Failure Taxonomy)

بحسب مصدر الفشل الفعلي المؤكَّد (لا حسب تصنيف Pass A أحادي التشغيلة وحده):

| نوع الفشل | العدد | الأمثلة | مصدر التأكيد |
|---|---:|---|---|
| **Discovery Failure** (لا يدخل `draft_hits_pool` إطلاقًا) | 1 مؤكَّد حتمي | `legis-1-2016-m30` (0/5 اكتشاف في خمس تكرارات) | Pass A + Pass B |
| **Evidence Admission Failure** (يُكتشف دومًا تقريبًا، لا ينجو) | **2 مؤكَّدان** (صُحِّح — كان 1) | `legis-38-1980-m166` (اكتشاف 5/5، نجاة 0/5)، `legis-38-1980-m167` (اكتشاف 5/5، نجاة 1/5) — أرقام حرفية مؤكَّدة | Pass A + Pass B |
| **Discovery Instability** (تذبذب اكتشاف صرف — يُقبَل فور اكتشافه دومًا) | **4 مؤكَّدة** (صُحِّح — كان "Retrieval Instability" مبهمة) | `regl-1-2016-m65`, `LEG-UNKNOWN-...` (gs-0015), `legis-38-1980-m298`, `legis-6-2010-m50` | Pass B (أرقام Discovery=Survival حرفيًا) |
| **Temporal Failure** | 0 | كل حالات `temporal` (6/6 must_find) نجت بالكامل | Pass A |
| **Negative-Control Failure** | غير مقيس مباشرة | — | **فجوة قياس معلنة**، انظر القسم N |
| **Ranking Failure** (نجا للسياق لكن برتبة متأخرة تضعف الاستشهاد) | غير مقيس بعد | — | يحتاج أداة منفصلة لم تُبنَ |

**ملاحظة تصحيحية:** بند "Cap Failure" (`regl-1-2016-m65`، لقطة Pass A الوحيدة) أُسقط كصف
مستقل — الأرقام الحرفية (القسم G) أثبتت أنه فعليًا **Discovery Instability** (اكتشاف=نجاة=3/5)،
لا فشلًا في مرحلة القبول بعد الاكتشاف كما أوحى تصنيف Pass A أحادي التشغيلة.

---

## I. الفشل المؤكَّد مقابل الفشل المتذبذب — لا خلط بينهما

**ثلاثة فشول مؤكَّدة بثبات (أو شبه ثبات) كامل عبر 5 تكرارات مستقلة (ليست حظًا سيئًا):**
- `legis-1-2016-m30` — فجوة اكتشاف حتمية (0/5 دومًا).
- `legis-38-1980-m166` — فجوة قبول دليل حتمية (اكتشاف 5/5، نجاة 0/5).
- `legis-38-1980-m167` — فجوة قبول دليل شبه حتمية (اكتشاف 5/5، نجاة 1/5 — نفس توقيع m166).

**أربعة أهداف بتذبذب اكتشاف صرف (نجاتها = اكتشافها تمامًا، لا فقد إضافي عند القبول):**
`regl-1-2016-m65`, هدف gs-0015، `legis-38-1980-m298`، `legis-6-2010-m50` — تُقاس بمدى (نطاق)
لا برقم واحد. القسم J يوضح الأثر على Recall الكلي.

---

## J. Must-Find Recall — لا رقم واحد (مصحَّح — 2026-09-14)

بما أن Pass B قاس 5 تكرارات على 20 حالة فقط (لا الأربعين)، Recall الكلي **لا يمكن تلخيصه
برقم واحد صادق** لكل المجموعة. **تصحيح حسابي وتصنيفي (النسخة السابقة استعملت تسمية "Worst-case
Recall" خطأً على معدل مجمَّع لا على تشغيلة متزامنة فعلية — صُحح):**

القيم الخام لكل هدف (نجاة/5): 20 هدفًا = 5/5، ثم m50=4/5، m30=0/5، m166=0/5، m167=1/5،
m65=3/5، هدف gs-0015=3/5، m298=3/5 (27 هدفًا إجمالًا).

- **Fully Stable Authorities:** 20/27 = **74.07%** (نجاة 5/5 تامة).
- **Stable-or-Near-Stable (≥4/5):** 21/27 = **77.78%**.
- **Observed Run-Level Survival** (مجموع كل النجاحات المفردة ÷ مجموع كل التشغيلات =
  (20×5 + 4+0+0+1+3+3+3) / (27×5) = 114/135): **84.44%**.

**لماذا لا يوجد "Worst-case/Best-case Recall" في هذه الجولة:** حساب أسوأ/أفضل تشغيلة متزامنة
حقيقية (Complete-Run Recall) يتطلب **مصفوفة سلطة×تشغيلة الخام** (أي المعرفة الدقيقة، تشغيلة
تشغيلة، بأي التشغيلات الخمس نجا كل هدف بالضبط) لحساب: هل التشغيلة رقم 3 مثلًا نجت فيها كل
الأهداف معًا أم لا. **هذه المصفوفة الخام غير محفوظة حاليًا** في مخرجات `stability_metrics.py`
— الأداة تُجمِّع `presence_ratio`/`survival_ratio` لكل هدف عبر الحلقة الداخلية ثم **تُسقط**
قائمة `runs` الخام قبل الطباعة (`aggregate_case()` لا يُرجعها). حساب Min/Max/Mean/Stdev
الصحيح لكل تشغيلة مؤجَّل حتى تُبنى هذه المصفوفة صراحة — وهذا بالضبط ما ستوفره أداة اختبار
Frozen Axes القادمة (مصمَّمة لتسجّل تفصيل كل تشغيلة، لا تجميعها فقط).

- **للحالات العشرين الأخرى (غير المقيسة في Pass B):** **لا بيانات استقرار — الرقم الوحيد
  المتاح هو نتيجة Pass A أحادية التشغيلة (92.9%)، ولا يجوز افتراض استقرارها.** هذا فراغ قياس
  معلن، لا استنتاج ضمني بالاستقرار.

**الخلاصة الصادقة الوحيدة الممكنة الآن:** *"في تشغيلة واحدة نموذجية (Pass A)، Recall الكامل
~93%. على العينة الصعبة/العدائية بالذات (27 هدفًا، Pass B)، معدل النجاة الخام عبر كل التشغيلات
= 84.44%، وسلطة كاملة الاستقرار (5/5) = 74.07% فقط — أي أن ~26% من الأهداف الصعبة تحمل بعض
عدم اليقين في كل تشغيلة فردية. لا نعرف بعد استقرار الحالات الأسهل (غير المقيسة بتكرار)، ولا
نملك بعد رقم Complete-Run Recall الحقيقي (يحتاج المصفوفة الخام)."*

---

## K. قمع النجاة (Survival Funnel) — Pass A

من `survival_metrics.py` (تشغيلة واحدة، 40 حالة، 42 `must_find` + 7 `must_not_miss`) — بمصطلحات
القسم F-مكرر المسوَّاة (`draft_hits_pool` بدل "hits" المبهمة):

```
must_find:      42 → draft_hits_pool(40) → dedup(40) → budget(39) → final_context(39)   [92.9%]
must_not_miss:   7 → draft_hits_pool(7)  → dedup(7)  → budget(6)  → final_context(6)    [85.7%]
```

الفارق بين `total` و`draft_hits_pool` (2 عنصرين لـmust_find) = فشل اكتشاف صرف (`m30` + عنصر
آخر مُستوعَب لاحقًا عبر قنوات أخرى في محاولات لاحقة — انظر تفاوت Pass A/Pass B في القسم L).
الفارق بين `draft_hits_pool` و`budget` (1 لكل من must_find/must_not_miss في هذه التشغيلة
تحديدًا، الهدف `m166`) = فشل قبول دليل. لا فقد في مرحلة `dedup` إطلاقًا (صفر — يؤكد أن إصلاح
P0-4 يعمل كما صُمِّم). **تنبيه:** هذا الرقم (`draft_hits_pool=40`) من تشغيلة Pass A الواحدة
فقط؛ لا علاقة له بـ`raw_candidate_union=41` من `gold_metrics.py` — أداتان مختلفتان بنيويًا
(القسم F-مكرر)، لا مفارقة حسابية.

---

## L. تحليل الاستقرار (Pass B) — تمييز حاسم: تفاوت النص مقابل تفاوت النتيجة

**كل حالة من الـ17 المقيسة أظهرت `subquery_variance=true`** — لكن هذه ملاحظة عن **نص** محاور
Haiku (Subquery Text Variance)، وهي **منفصلة منطقيًا** عن **نتيجة** الاسترجاع (Retrieval Outcome
Variance / Authority Stability). القياس الفعلي يفصل بينهما بوضوح:

| الحالة | تفاوت نص المحاور | استقرار كل الأهداف |
|---|---|---|
| gs-0011, gs-0017, gs-0020, gs-0021, gs-0022, gs-0023, gs-0027, gs-0029, gs-0031, gs-0033, gs-0034 | نعم (5 صيغ مختلفة) | **مستقر 5/5 بالكامل** رغم ذلك |
| gs-0001, gs-0013, gs-0015, gs-0035, gs-0036 | نعم | **غير مستقر** (هدف واحد أو أكثر) |

**Authority Stability Rate = 21/27 = 77.8%** من الأهداف بقيت مستقرة (أو شبه مستقرة) **رغم أن
100% من الحالات** أظهرت تفاوتًا في نص المحاور. بعبارة أدق: **تفاوت النص لا يؤدي إلى تفاوت
النتيجة إلا في ~22% من الحالات المقيسة** — وهذا يضعف (لا ينفي) فرضية أن Haiku هو المحرك
الوحيد؛ فلو كان المحرك الوحيد والحاسم، لتوقّعنا ارتباطًا أعلى بكثير بين تفاوت الصياغة وتفاوت
النتيجة عبر كل الحالات، لا في ربعها فقط.

**الصياغة المعتمدة لهذا التقرير (لا أكثر):**
> *Haiku subquery nondeterminism is the leading causal hypothesis for retrieval instability,
> not yet conclusively proven.* الأرقام تدعم الفرضية جزئيًا (كل حالة غير مستقرة تحمل أيضًا
> تفاوت نص) لكنها لا تثبتها حصريًا (أغلب حالات تفاوت النص لم تنتج تفاوت نتيجة) — قد يكون
> العامل الحاسم تفاعلًا بين تفاوت الصياغة **ونوع** الهدف (حواف قرب حد الميزانية/السقف تحديدًا)
> لا تفاوت الصياغة وحده.

**Discovery Stability مقابل Survival Stability — الجدول الكامل النهائي (أرقام حرفية من JSON،
لا تقدير، 2026-09-14):**

| الهدف | Discovery Stability | Survival Stability | الفارق | التصنيف النهائي |
|---|---:|---:|---|---|
| `legis-1-2016-m30` | 0/5 | 0/5 | لا فارق | **Discovery Failure صرف** |
| `legis-38-1980-m166` | **5/5** | **0/5** | فارق كامل | **Evidence Admission Failure صرف** |
| `legis-38-1980-m167` | **5/5** | **1/5** | فارق شبه كامل | **Evidence Admission Failure** |
| `regl-1-2016-m65` | 3/5 | 3/5 | لا فارق | Discovery Instability صرف |
| `LEG-UNKNOWN-P1-art2-3-...` (gs-0015) | 3/5 | 3/5 | لا فارق | Discovery Instability صرف |
| `legis-38-1980-m298` | 3/5 | 3/5 | لا فارق | Discovery Instability صرف |
| `legis-6-2010-m50` | 4/5 | 4/5 | لا فارق | Discovery Instability صرف (طفيف) |

**هذا يثبت نهائيًا (بأمر المالك، معيار الإثبات المحدَّد مسبقًا):** `m166` **و** `m167` معًا
`Pure Evidence Admission Failure` — الاكتشاف شبه مضمون (5/5) بصرف النظر عن صياغة المحاور،
والفشل بنيوي بحت في الترتيب/الميزانية. أما الأربعة الباقية فـ`Pure Discovery Instability` —
الفارق بينهما صفر في كل الحالات (لا فقد إضافي بعد الاكتشاف)، أي أن **كل** التذبذب الملحوظ فيها
واقع في مرحلة الاكتشاف حصرًا، وهي المرشَّح الأنسب لاختبار فرضية عشوائية Haiku (القسم Q-مكرر).

---

## M. دراسات حالة

### M.1 — `gs-0013` / `legis-38-1980-m166` **و** `m167` — Evidence Admission Failure (المرجعية، زوج لا فرد)
السؤال يطلب شروط أمر الأداء على شيكين مرتدّين. المادتان 166 و167 مرافعات (شرط التكليف بالوفاء
الوجوبي وميعاده) **تُكتشفان في كل تكرار تقريبًا** (5/5 كلتاهما) عبر قناة `bundle` (الحزمة
الحاكمة لـ"أمر أداء" تعمل كما صُمِّمت تمامًا — لا خلل في الاكتشاف) لكن موضعهما الداخلي في تلك
الحزمة يمنحهما درجة مرتِّب متقاطع منخفضة (`m166 reranker_raw` ≈ 0.0109 في قياس سابق، رتبة نهائية
~150؛ `m167` رتبة تتراوح 40-83 — أوسع تذبذبًا لكنها **أيضًا** لا تنجو إلا مرة واحدة من خمس) فتُقصَّان
عند حد ميزانية التشريع. **هذا زوج مرجعي متماثل التوقيع لا حالة منفردة** — كلاهما الأنسب لاختبار
أي Evidence Admission Policy مستقبلية. **لا تُصلَح الآن حسب توجيهك الصريح، ولا علاقة لفشلهما
بعشوائية محاور Haiku** (الاكتشاف ثابت 5/5 بصرف النظر عن صياغة المحور — القسم L).

### M.2 — `gs-0036` / `legis-1-2016-m30` — Discovery Failure
السؤال يصف شريكًا يواجه امتناع المدير عن الدعوة وتعطيل النصاب، بلا أي ذكر لمصطلح "اتفاق
المساهمين" أو "تحييد الأسهم" — وهما بالضبط موضوع المادة 30 (الآلية القانونية الفعلية لكسر
هذا التعطيل: طلب أمر على عريضة لتحييد الحصص من التصويت). **تصنيف الفجوة الأولي: `dependency_gap`**
— لا تشابه لفظي ولا دلالي مباشر بين صياغة السؤال العامية ("يعطّلون النصاب") ومصطلح النص القانوني
("اتفاق المساهمين"/"تحييد الأسهم")؛ الرابط قانوني بحت يحتاج معرفة أن هذا تحديدًا هو العلاج
التشريعي للمشكلة الموصوفة — نمط مطابق تمامًا لـ"فصول الاستلزام القانوني" الموثَّقة سابقًا
(رابطها علم قانوني لا لغة). **لم يُبنَ أي gate لهذا الهدف تنفيذًا لتوجيهك.**

### M.3 — `gs-0015` — Retrieval Instability (تاريخي، مؤكَّد الاستمرار)
هدف `LEG-UNKNOWN-P1-art2-3-...` (تعليمات كفاية رأس المال) — نجاة 3/5، رتبة 30-32 (مدى ضيق
نسبيًا مقارنة بـm167). هذا يطابق تمامًا التذبذب الموثَّق تاريخيًا لهذه الحالة بالذات (§ "منظومة
تضييق فجوة الاسترجاع")، مؤكَّد الآن بقياس منهجي بدل الرصد اليدوي السابق.

### M.4 — `gs-0017` — فجوة استرجاع معروفة مسبقًا (natural_language_mismatch)
السؤال: "هل يجوز للقاضي نظر دعوى يكون أحد أطرافها قريبًا له أو صهرًا؟" — صياغة عامية لعدم
صلاحية القاضي (المادة 102 مرافعات). **مستقر 5/5 في Pass B** رغم وسمه `known_preexisting_failure`
عند الإضافة — أي أن الفجوة المرصودة يدويًا سابقًا (باستعلام مختلف الصياغة على الأرجح) **لم
تتكرر** في صياغة هذا السؤال بالذات ضمن القياس الآلي. **درس:** التوثيق التاريخي لفجوة برصد
يدوي واحد لا يعني أنها تتكرر آليًا بنفس الحدة تحت كل صياغة — لا يُفسَّر هذا تناقضًا، بل حدود
طبيعية لتعميم ملاحظة واحدة.

---

## N. الضوابط السلبية (Negative Controls)

4 حالات سلبية (`out_of_scope` 3، `policy_excluded` 1) — الأداتان الحاليتان (`gold_metrics.py`/
`survival_metrics.py`) تتحققان فقط من أن `must_find` (فارغ لهذه الحالات) لم يُخفَق فيه، وهو
**نجاح تافه بالتعريف (0/0 دومًا)**. **هذا لا يقيس ما يهم فعليًا:** هل يجلب النظام مرشحين
عاليي الدرجة زائفين (False Source-Sufficiency) لسؤال خارج النطاق أصلًا؟ **فجوة قياس معلنة
صراحة** — تحتاج نداء توليد فعليًا لفحصها (كما أُشير سابقًا)، ولم تُبنَ في هذه الجولة.

---

## O. نتائج الفحص الزمني (Temporal)

كل حالات `retrieval_class=temporal` (4 حالات، 6 عناصر `must_find`) حققت **100% نجاة كاملة**
في Pass A — لا فجوة زمنية ظاهرة في هذه العينة (نصوص متعاقبة، ملغى مقابل نافذ). لم تُدرَج ضمن
Pass B (غير موسومة صعبة/عدائية) فاستقرارها عبر تكرارات متعددة **غير مؤكَّد بعد**.

---

## P. مصفوفة السبب الجذري

| الفشل | الدليل | الثقة | العلاج المرجَّح (غير مُلزِم) |
|---|---|---|---|
| Discovery Failure (`m30`) | 0/5 اكتشاف حرفي عبر تكرارين مستقلين (Pass A وPass B معًا)، تحليل نوعي يوضح غياب أي تشابه لفظي/دلالي | **عالية** | خريطة استلزام قانوني ضيقة (A3) أو موسّع استعلام حتمي مصمَّم لهذه الفئة تحديدًا — لا Hybrid Search عام |
| Evidence Admission Failure (`m166` + `m167` معًا) | اكتشاف حرفي 5/5 لكليهما + نجاة 0/5 و1/5 على التوالي + درجة مرتِّب منخفضة موثَّقة لـm166 | **عالية** (ارتفعت — زوج متطابق التوقيع لا حالة منفردة) | سياسة اعتماد أدلة متدرجة (Tier A-D) أو ميزانية محجوزة للمواد الحاكمة قانونًا |
| Discovery Instability (4 أهداف: `m65`, `gs-0015`, `m298`, `m50`) | Discovery=Survival حرفيًا في الأربعة (لا فقد إضافي بعد الاكتشاف) + تفاوت نص محاور مصاحب في كل حالة | **متوسطة** (سببية Haiku غير مثبتة، لكن النطاق أدق الآن — محصور بالاكتشاف فقط) | **معلَّق حتى اختبار Frozen Axes** — إن ثبتت السببية: تثبيت/تجميد محاور جزئي؛ إن لم تثبت: فحص ANN/تعادلات/تزامن |
| Negative-Control False-Positive | غير مقيس | **معدومة** | يحتاج أداة تدقيق مولِّدة جديدة قبل أي حكم |
| تباين لقطة المتن بين Pass A/Pass B (194 كائنًا غير مرتبطة) | مؤكَّد حسابيًا (62,304 مقابل 62,498) | **متوسطة** (أثر غير مقيس، لا دليل على حجمه) | لا علاج — توثيق فقط؛ يُراعى عند أي إعادة إنتاج مستقبلية |

---

## Q. إطار قرار P2 (لاحقًا، بعد اختبار السببية — غير مُنفَّذ الآن)

الاختيار يُربط **بتوزيع الفشل الفعلي** لا بانطباع عام:

- **إن كانت Discovery Failure هي الغالبة:** بحث متفرّق (Sparse/BM25) + توسعة استعلام حتمية +
  Scope Planner + طبقة استلزام قانوني (Legal Dependency layer) — موجَّهة لسدّ فجوات لا تصلها
  الدلالة أصلًا.
- **إن كانت Admission Failure هي الغالبة:** طبقات أدلة (Evidence Tiers) + حماية مرساة (Anchor
  Protection) مقيَّدة الأثر + قبول سياق واعٍ بالتغطية (Coverage-Aware Admission) — لا يمس
  الاكتشاف، يمس فقط قرار من يبقى بعد الاكتشاف.
- **إن كانت Instability هي الغالبة:** عمود فقري استرجاع حتمي (نتائج ثابتة لنفس السؤال) +
  تثبيت/تجميد محاور (Frozen/Deterministic Axes) + محاور LLM كإضافة تكميلية لا مصدرًا وحيدًا.
- **إن كانت Temporal Failure كبيرة (لم يظهر بعد في هذه العينة):** طبقة انطباق زمني مخصَّصة.

**لا قرار الآن.** التوزيع النهائي المثبَت حرفيًا (1 Discovery Failure + 2 Evidence Admission
Failures + 4 Discovery Instability من 27 هدفًا مقيسًا) لا يبرر بعد ترجيح مسار على آخر — خصوصًا
أن أكبر فئة (Discovery Instability) لم تُثبَت سببيًا بعد.

---

## R. ما أصلحه P0 — وما لم يصلحه

**أصلحه P0 فعليًا (مؤكَّد بالكود والقياس):**
- وصول أنواع كائنات كانت غائبة تمامًا عن كل القنوات الآلية (`legislation`/`legislation_archived`).
- ازدحام نسخ المبادئ المكرَّرة على حصة السقف (كان يُهدر مساحة على تكرارات بدل مبادئ متمايزة).
- عمى تام عن أحداث الإسقاط (كانت تقع صامتة بلا أي تسجيل من قبل).

**لم يصلحه P0 (ولا ادّعى ذلك — يوثَّق صراحة لمنع الاحتفال المبكر):**
- العمى الدلالي/الاستلزامي (`m30`) — لا علاقة له بتصنيف الأنواع.
- بقاء السياق النهائي (`m166`) — مشكلة ترتيب/ميزانية لا تصنيف.
- عدم حتمية الاكتشاف في الأهداف الأربعة المصنَّفة Discovery Instability — لم يعالجها P0،
  ولا صلة لها بتصنيف الأنواع أو أي إصلاح من إصلاحات P0 الخمسة.
- الانطباق الزمني كطبقة عامة — لم تُختبر إلا على 4 حالات محدودة.
- اكتمال التغطية القانونية على نطاق واسع — 40 حالة عيّنة، لا شمولًا.

**درس مركزي:** بطارية 15/15 (P0) ونتيجة Candidate Recall شبه الكاملة (97.6%) **لا تعنيان أن
مشكلة التغطية محلولة** — أثبتت هذه الجولة بالتحديد أن الفشل الحقيقي ينتقل لمراحل أعمق (النجاة
لا الاكتشاف) بمجرد أن يُقاس بعمق كافٍ (Pass A) وبتكرار كافٍ (Pass B).

---

## S. تجربة السببية المضبوطة (Frozen Axes) — النتائج الكاملة (2026-09-14)

**الهدف: اختبار السببية لا تحسين النتائج.** أداة `tools/frozen_axes_experiment.py`، مونكي-باتش
مؤقت لـ`app._draft_subqueries` يمنع أي نداء Haiku في وضع Frozen (تحقُّق آلي مؤكَّد: مطابقة
حرفية لـ`exact_axes` عبر كل التشغيلات الخمس في كل حالة، صفر استثناء).

### S.1 — commit + corpus snapshot
نفس اللقطة الموثَّقة في القسم D-مكرر (لا تغيير في المتن بين إعداد التقرير وهذه التجربة):
git commit `2edb2193648490fca9288ecfab059d6cd87df436`، PostgreSQL = Qdrant = 62,498، Gold Set
hash `5c54cecdcf2ac284` (40 حالة). كل الثوابت الأخرى (Qdrant collection، embeddings، reranker،
budgets، gates، قنوات التوسعة) كما هي في الإنتاج — لم تُمس.

### S.2 — المحاور المجمَّدة الحرفية لكل حالة (من تشغيلة normal-1)

| الحالة | المحاور المجمَّدة |
|---|---|
| `gs-0015` | كفاية رأس المال البنكي؛ تعليمات بنك الكويت المركزي؛ مكونات رأس المال المطلوبة؛ المخاطر البنكية ومتطلبات رأس المال؛ نسب الرفع المالي والمخاطر |
| `gs-0036` | عزل مدير الشركة ذات المسؤولية المحدودة؛ دعوة الجمعية العامة من قبل الشركاء؛ تعطيل النصاب القانوني؛ حقوق الشريك صاحب ربع رأس المال؛ الطعن في قرارات الشركاء بعدم الدعوة؛ اختصاصات المحكمة في منازعات الشركات |
| `gs-0001` | منع من السفر أمر أداء مبلغ مالي؛ إنكار توقيع العقد والطعن في صحته؛ دعوى تزوير المستند المصور؛ عدم إعلان بأمر الأداء والإجراءات الواجبة؛ الطعن في أمر الأداء بالاعتراض؛ حق الموكل في استرجاع جواز السفر |
| `gs-0035` | الطرد التعسفي من العمل؛ تعويض الفصل بدون سبب؛ حقوق العامل في القانون الكويتي؛ شروط الفصل القانوني للعامل؛ المستحقات المالية للعامل المفصول |
| `gs-0013` (ضابط سلبي) | أمر الأداء في المنازعات التجارية والشيكات المرتجعة؛ شروط وإجراءات استصدار أمر الأداء؛ مواعيد التظلم من أمر الأداء والطعن فيه؛ منع المدير المفوض بالتوقيع من السفر وشروطه؛ التقادم الصرفي للشيك وأثره على المطالبة بأصل الدين؛ مسؤولية مدير الشركة ذات المسؤولية المحدودة الشخصية |

### S.3 — مصفوفة خام لكل تشغيلة (الهدف البؤري، 10 تشغيلات × 5 حالات = 50 سجلًا)

**`gs-0015` / `LEG-UNKNOWN-P1-art2-3-...`:**
Normal: مكتشَف في normal-1/4/5 (قناة dense، رتبة ما قبل المرتِّب 31-34، `reranker_raw`≈0.2215
ثابتة عبر الثلاثة) وغائب تمامًا في normal-2/3. Frozen: مكتشَف ومقبول **بقيم متطابقة حرفيًا**
في التشغيلات الخمس (رتبة=32، درجة=0.8745، `final_score`=0.548) — إعادة إنتاج تامة لنتيجة
normal-1 بالضبط.

**`gs-0036` / `regl-1-2016-m65`:** Normal: مكتشَف في normal-2/4/5 (قناة dense) وغائب في
normal-1/3. Frozen (مجمَّد على محاور normal-1 **وهي بالذات إحدى التشغيلتين الفاشلتين في
الاكتشاف**): غائب تمامًا في كل التشغيلات الخمس — **إعادة إنتاج تامة لفشل normal-1 نفسه**.

**`gs-0001` / `legis-38-1980-m298`:** Normal: مكتشَف في normal-1/3 (dense) وnormal-2 (قناة
**chapter** بدرجة 0.99!) وغائب في normal-4/5. Frozen: مكتشَف ومقبول بقيم متطابقة حرفيًا
(رتبة=49، `final_score`=0.4703) في الخمس جميعًا — إعادة إنتاج تامة لـnormal-1.

**`gs-0035` / `legis-6-2010-m50`:** Normal: مكتشَف في normal-1/2/5 (قناة **bundle**، لا dense
— إثبات أن قناة الحزم أيضًا تتأثر بالمحاور لأنها تستقبل `subqueries` كوسيط) وغائب في
normal-3/4. Frozen: مكتشَف ومقبول بقيم متطابقة حرفيًا في الخمس جميعًا.

**`gs-0013` / `legis-38-1980-m167` (الضابط السلبي) — الاكتشاف الأهم في هذه التجربة:**
`m167` **مكتشَف في كل التشغيلات العشر بلا استثناء** (يؤكد الاستقلالية الأصلية للاكتشاف عن
المحاور) — لكن **عبر قناتين مختلفتين بحسب المحاور**: في normal-1 عبر `bundle` فقط (درجة أساس
0.0، `final_score`=0.0208، **مرفوض**)، وفي normal-2/3/4/5 عبر `dense` **أيضًا** (درجة أساس
0.841، `final_score`=0.4414، **مقبول**) — منطق "الأعلى درجة يفوز" في دمج المرشحين يجعل
اكتشاف dense الإضافي يرفع الدرجة النهائية فينجو. Frozen (مجمَّد على محاور normal-1 — **حالة
bundle فقط تحديدًا**): اكتشاف=5/5 (كما هو متوقَّع)، لكن **مرفوض في الخمس جميعًا بنفس القيم
الحرفية لـnormal-1 (`final_score`=0.0208)** — إعادة إنتاج تامة لحالة الرفض.

### S.4 — Discovery: Normal مقابل Frozen (الهدف البؤري)

| الحالة | Normal Discovery | Frozen Discovery |
|---|---|---|
| gs-0015 | 3/5 | **5/5** |
| gs-0036/m65 | 3/5 | **0/5** (تجميد على تشغيلة فاشلة) |
| gs-0001/m298 | 3/5 | **5/5** |
| gs-0035/m50 | 3/5 | **5/5** |
| gs-0013/m167 (ضابط) | 5/5 | 5/5 (بلا تغيير، كما تنبَّأنا) |

### S.5 — Survival: Normal مقابل Frozen (الهدف البؤري)

| الحالة | Normal Survival | Frozen Survival |
|---|---|---|
| gs-0015 | 3/5 | **5/5** |
| gs-0036/m65 | 3/5 | **0/5** |
| gs-0001/m298 | 3/5 | **5/5** |
| gs-0035/m50 | 3/5 | **5/5** |
| gs-0013/m167 (ضابط) | 4/5 | **0/5** (تراجع — انظر S.9) |

**ملاحظة مهمة عبر الأهداف الأربعة الأساسية:** في كل الحالات الأربع، Survival = Discovery
حرفيًا في كل تشغيلة — أي أن **الاكتشاف هو كل القصة**؛ بمجرد الاكتشاف، القبول شبه مضمون. هذا
يعني أن اختبار السببية على الاكتشاف هو الاختبار الحاسم فعلًا لهذه الفئة، وقد أعطى نتيجة حاسمة.

### S.6 — Complete-Run Recall statistics (لكل حالة/نمط، عبر كل عناصر must_find للحالة)

| الحالة | Normal (min/max/mean/stdev) | Frozen (min/max/mean/stdev) |
|---|---|---|
| gs-0015 | 0.6 / 1.0 / 0.8 / 0.16* | 1.0 / 1.0 / 1.0 / 0.0 |
| gs-0036 | 0.333 / 0.667 / 0.533 / 0.163 | 0.333 / 0.333 / 0.333 / 0.0 |
| gs-0001 | 0.5 / 1.0 / 0.8 / 0.245 | 1.0 / 1.0 / 1.0 / 0.0 |
| gs-0035 | 0.0 / 1.0 / 0.6 / 0.490 | 1.0 / 1.0 / 1.0 / 0.0 |
| gs-0013 | 0.4 / 0.8 / 0.68 / 0.16 | 0.6 / 0.6 / 0.6 / 0.0 |

*(gs-0015 القيمة أعلاه لعنصر must_find الوحيد المقيس ضمن التجربة؛ تطابق تمامًا S.4/S.5.)*
**نمط عام في كل الحالات الخمس دون استثناء: `stdev` Frozen = 0.0 تمامًا** — أي أن تثبيت
المحاور يُنتج تكرارًا حتميًا كاملًا (صفر تباين)، بصرف النظر عن كون النتيجة عالية أم منخفضة.
هذا دليل مباشر ونظيف: **مصدر التباين هو محتوى المحاور نفسه لا أي عشوائية أخرى في النظام**
(لو كانت العشوائية من Qdrant ANN أو تعادلات الترتيب أو التزامن، لَبقي `stdev`>0 حتى تحت
التجميد).

### S.7 — نتيجة الضابط السلبي (Negative Control) — مُصحَّحة عن الانطباع الأول

الاكتشاف تأكَّد **مستقلًا عن المحاور تمامًا** (5/5 في كليهما) — يطابق التوقع. **لكن النجاة لم
تبقَ كما تنبَّأنا حرفيًا ("منخفضة في كليهما بلا تغيير")** — بل كشفت التفاصيل الخام (S.3) آليةً
لم تكن معروفة سابقًا: `m167` له **مسار إنقاذ عبر القناة الدلالية** (dense) يرفع درجته حين
تُصادف المحاور دلاليًا مناسبة، وواجهة "الأعلى يفوز" بين القنوات تمنحه حينها الدرجة العليا
فينجو. هذا **لا يُفنِّد** تصنيف Evidence Admission Failure (فشل `m167` حين يُكتشَف عبر `bundle`
فقط حقيقي وثابت 100% في كل عيّنة رُصد فيها ذلك المسار وحده)، لكنه **يُنقِّحه**: `m167` ليس حالة
"فشل قبول صرف مستقل عن المحاور بالكامل" كما صِيغ في التصنيف الأولي، بل **حالة مركَّبة**: قناة
حاكمة تفشل دومًا + قناة دلالية إنقاذية تتأثر بالمحاور. **لم يُختبَر `m166` بنفس التفصيل في هذه
التجربة** (خارج نطاق الحالات الخمس المتفق عليها) — فجوة معلنة، لا افتراض بامتداد نفس الآلية
إليه بلا دليل مباشر.

### S.8 — الاستنتاج السببي لكل هدف (بمستوى ثقة)

| الهدف | الاستنتاج السببي | الثقة | الدليل الحاسم |
|---|---|---|---|
| `gs-0015` target | **Haiku axis nondeterminism سبب مباشر مؤكَّد للاكتشاف المتذبذب** | **عالية** | Frozen على محاور ناجحة → 5/5 حتمي بقيم مطابقة حرفيًا |
| `regl-1-2016-m65` | **نفس الاستنتاج** — الدليل أقوى لا أضعف: تجميد على محاور فاشلة يُعيد إنتاج الفشل حتميًا | **عالية** | Frozen على محاور فاشلة → 0/5 حتمي، `stdev`=0 يستبعد مصادر عشوائية أخرى |
| `legis-38-1980-m298` | **نفس الاستنتاج** | **عالية** | نفس نمط S.3/S.6 |
| `legis-6-2010-m50` | **نفس الاستنتاج**، وامتداد مهم: التأثر بالمحاور يشمل قناة `bundle` لا `dense` فقط | **عالية** | اكتشاف عبر bundle متذبذب في Normal، حتمي في Frozen |
| `legis-38-1980-m167` (ضابط) | الاكتشاف مستقل عن المحاور (يؤكَّد)؛ **النجاة تتأثر بالمحاور عبر مسار إنقاذ دلالي غير متوقَّع سابقًا** — تنقيح للتصنيف لا نقض له | **متوسطة-عالية** على الآلية (أدلة دقيقة على مستوى الكود)، **منخفضة** على تعميمها على `m166` (غير مُختبَر) |

**الخلاصة العامة:** الفرضية السببية (عشوائية تفكيك Haiku تُسبِّب Discovery Instability) **انتقلت
من "مرجَّحة غير مثبتة" إلى "مؤكَّدة بدليل مباشر عالي الثقة"** لكل الأهداف الأربعة الأساسية —
معيار الإثبات المحدَّد مسبقًا (Frozen يستقر جوهريًا مقابل Normal المتذبذب، مع بقاء كل شيء آخر
ثابتًا) تحقَّق حرفيًا في أربعة من أربعة. الضابط السلبي أضاف تفصيلًا دقيقًا لآلية `m167` دون أن
يُفنِّد وجود Evidence Admission Failure أصلًا.

### S.9 — مفاجآت ونتائج لم تكن متوقَّعة

1. **قناة `bundle` أيضًا حسّاسة للمحاور** (`gs-0035/m50`) — الافتراض الأولي حصر حساسية المحاور
   في البحث الدلالي (`dense`) وحده؛ `_draft_bundles` يستقبل `subqueries` كوسيط فعليًا فيتأثر
   أيضًا. يوسِّع نطاق الفرضية السببية لا يُضيِّقها.
2. **`m167` مسار مزدوج غير موثَّق سابقًا**: بوابة `bundle` الحاكمة لـ"أمر أداء" **تجد** `m167`
   دومًا بدرجة منخفضة (0.0 → `final_score` 0.0208، مرفوض)، لكن حين يُصادف تفكيك Haiku محورًا
   يجد نفس المادة دلاليًا أيضًا (درجة 0.84)، تفوز الدرجة الأعلى وينجو. هذا يعني أن نسبة نجاة
   `m167` في الإنتاج الفعلي تعتمد جزئيًا على "حظ" صياغة المحاور — وهو بالضبط ما تنبأت به فرضية
   Haiku، حتى في حالة صُنِّفت أصلًا كمستقلة عنها.
3. **درجة المرتِّب المتقاطع (`reranker_raw`) ثابتة حرفيًا بدقة 15+ رقمًا عشريًا** لنفس الهدف
   عبر تشغيلات مختلفة وحتى عبر قنوات اكتشاف مختلفة (مثال `m167`: نفس الرقم `0.041638086278522356`
   في bundle وdense معًا) — يستبعد عمليًا أي مساهمة من عدم حتمية داخل المرتِّب نفسه، ويؤكد أن
   نص الاستعلام الذي يُقارَن به المرتِّب (`request_type + facts_ret`) ثابت لا يتأثر بالمحاور،
   كما هو متوقَّع من قراءة الكود.
4. **لا حاجة لفحص بدائل أخرى الآن** (ANN/تعادلات/تزامن) — معيار "توقف إن بقي Frozen غير
   مستقر" **لم يتفعَّل** لأي هدف من الأربعة الأساسية؛ `stdev`=0.0 تحت التجميد في كل الحالات
   الخمس (بما فيها الضابط) يستبعد عمليًا هذه البدائل كمساهم كبير في هذه العيّنة تحديدًا.

**لا P2 الآن، ولا تغيير في سلوك الاسترجاع.** هذه نهاية المرحلة التشخيصية المتفق عليها.

---

## الملحق التقني — حالة نقاط المراجعة الأربع (محدَّث 2026-09-14، كلها أُغلقت)

1. ✅ **حُسم:** `presence_ratio` الحرفي لكل الأهداف السبعة (القسم G/L) — لا "مرجَّح" متبقٍ في
   الوثيقة.
2. ✅ **حُسم:** تفسير 41/42 مقابل `hits(40)` من قراءة مباشرة لكود `retrieve()` و
   `_draft_build_context` (القسم F-مكرر) — ثلاثة فروق بنيوية مؤكَّدة، ومصطلحات مُعاد تسميتها
   (`raw_candidate_union`/`draft_hits_pool`/`reranker_pool`/`final_context`).
3. ✅ **حُسم:** حساب القسم J أُعيد بصيغة `Fully Stable`/`Stable-or-Near-Stable`/`Observed
   Run-Level Survival` بدل "Worst-case" الخاطئة.
4. ✅ **حُسم:** لقطة المتن موثَّقة لكلتا التشغيلتين (القسم D-مكرر) مع تسجيل صريح للفارق
   (62,304 مقابل 62,498) بدل إخفائه.

**الخطوة التالية المتفق عليها (ولا غيرها الآن):** اختبار السببية المضبوط (Frozen Axes) على
أربع حالات + خامسة اختيارية، بتوزيع أدوار دقيق بعد التصنيف النهائي أعلاه:

- **الاختبار الأساسي لفرضية Haiku** (المرشَّحات الحقيقية — Discovery Instability صرفة):
  `gs-0015`، `gs-0036/regl-1-2016-m65`، `gs-0001/legis-38-1980-m298`، واختياريًا
  `gs-0035/legis-6-2010-m50` إن كانت الكلفة محدودة. **التوقع إن صحت الفرضية:** يستقر الاكتشاف
  عند 5/5 (أو قريبًا منه) تحت المحاور المجمَّدة.
- **ضابط تفنيد سلبي (Negative Control)**: `gs-0013/legis-38-1980-m167` — **لا يُتوقَّع أن يتغير
  شيء** (الاكتشاف مستقر 5/5 أصلًا بصرف النظر عن المحاور؛ النجاة يجب أن تبقى منخفضة حتى مع تجميد
  المحاور، لأن الفشل بنيوي في الترتيب/الميزانية لا في تفكيك السؤال). **إن تغيّرت نتيجة m167
  تحت التجميد رغم هذا التوقع، فهذا يستدعي مراجعة تصنيف Evidence Admission Failure نفسه** — أُدرج
  عمدًا كاختبار يمكن أن يُفنِّد التصنيف الحالي، لا لتأكيده فقط.

تجريبة تشخيصية فقط، صفر تغيير في سلوك الإنتاج، تُنفَّذ بعد اعتماد هذه الوثيقة لا قبله.

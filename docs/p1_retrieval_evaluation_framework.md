# P1 — إطار تقييم الاسترجاع (Retrieval Evaluation Foundation)

> حالة الوثيقة: مسودة إطار تحت البناء، بأمر المالك الصريح — **لا يبدأ P2 (Hybrid Search/Scope
> Planner/Knowledge Graph) قبل مراجعة أرقام P1 الكاملة**. هذا الملف يوثق التصميم (Schema،
> تعريفات المقاييس، تصميمَي False No-Source والفصل بين البحث والتوليد) — البناء الفعلي للأداة
> وتعبئة الحالات جارٍ تباعًا في `tools/retrieval_gold_set.json` و`tools/gold_metrics.py`.

## B) مخطط Gold Set (P1-1)

بنية كل حالة (نمو تدريجي — ليست 200 حالة دفعة واحدة):

```json
{
  "id": "gs-0001",
  "question": "نص السؤال/الوقائع كما يُكتب فعليًا",
  "category": "الفرع القانوني (مدني/تجاري/إداري/أحوال شخصية/…)",
  "retrieval_class": ["explicit_citation", "legal_dependency"],
  "relevant_issues": ["وصف قصير للمسائل القانونية التي يثيرها السؤال"],
  "must_find": ["legis-68-1980-m166", "legis-68-1980-m167"],
  "should_find": ["jprin-911-2006-…"],
  "must_not_miss": ["legis-53-2026-m8"],
  "notes": "سياق بشري: لماذا هذه المعرفات، وأي فجوة تاريخية اختبرتها هذه الحالة إن وُجدت"
}
```

**تعريفات دقيقة (كما طلب المالك حرفيًا):**
- `must_find`: مصادر لو غابت عن السياق النهائي **تُبطل** الإجابة (خطأ جوهري لا مجرد نقص جودة) —
  مثال: مادة التكليف بالوفاء الوجوبي في سؤال أمر أداء.
- `should_find`: مصادر تُحسّن الإجابة (سوابق داعمة، مبادئ استئناسية) لكن غيابها لا يُبطلها.
- `must_not_miss`: فئة خاصة من `must_find` تُستعمل لبوابات الإجراءات السابقة الوجوبية
  (`_GATES`) والفصول الحاكمة — إغفالها ليس "نقص استرجاع عادي" بل **إخلال إجرائي** (كإغفال
  مركز التسوية الأسري أو لجنة النسب) يُصنَّف بشدة أعلى من `must_find` العادي في التقارير.
- `retrieval_class`: وسوم من مفردات ثابتة (P1-5): `explicit_citation, semantic_match,
  lexical_match, legal_dependency, temporal, cross_reference, multi_law, exception,
  judicial_principle, procedure` — الهدف تحديد **أين** يفشل النظام لا رقمًا وسطيًا فقط.

**قاعدة تحرير صارمة:** كل `must_find`/`must_not_miss` **يُتحقق بشريًا** (بحث مباشر في القاعدة
يثبت وجود المعرف ومطابقته للسؤال) قبل إضافته — لا توليد آلي من نموذج لغوي دون مراجعة، تمامًا
كقاعدة `battery.json` القائمة.

**مصادر التعبئة الثمانية (P1-4)، بالترتيب المخطط:**
1. الحالات الـ15 الحالية في `battery.json` (تحويل مباشر — أول دفعة، لا حاجة لتحقق جديد).
2. حالات فشلت تاريخيًا وأُصلحت (gate92، chapstem، prinxref، gapfix) — اختبار انتكاس مزدوج الغرض.
3. مسائل ذات مرجع حاكم مؤكد قطعًا (مادة واحدة لا لبس فيها) — قياس أرضية الأداء الأعلى الممكنة.
4. مسائل مرتبطة اعتماديًا/إلزاميًا (`_GATES` الحالية وما يشبهها) — فئة `legal_dependency`.
5. مسائل زمنية (نص مُلغى/مُعدَّل/نسخة سابقة) — فئة `temporal`، تتقاطع مع نتائج القسم I أدناه.
6. مسائل تمتد عبر عدة قوانين — فئة `multi_law`.
7. مسائل استثنائية (نص عام له استثناء صريح في مادة أخرى) — فئة `exception`.
8. أسئلة بصياغة عامية/طبيعية غير قانونية الصياغة (كما يكتبها موكل لا محامٍ) — تختبر تفكيك
   Haiku للمحاور، لا الاسترجاع وحده.

## C-F) المقاييس (P1-2) — تعريفات دقيقة قبل أي تشغيل

كل هذه تُحسب **بلا نداء توليد** (`draft_create`/`fast_create` غير مستدعاة) — استرجاع فقط، بنفس
روح `battery_run.py` القائمة:

- **Candidate Recall**: نسبة `must_find` الموجودة في **أي** نقطة من خط الاسترجاع (حتى لو
  أُسقطت لاحقًا) — يقيس هل المشكلة استرجاع أصلًا أم إسقاط لاحق.
- **Recall@20/50/100**: نسبة `must_find` الموجودة ضمن أعلى K مرشح **بعد** الدمج والترتيب
  النهائي (`ordered`)، قبل قصّ الميزانية — يقيس جودة الترتيب لا الميزانية.
- **Must-Find Recall**: `عدد must_find الواصل للسياق النهائي / إجمالي must_find` — المقياس
  الأهم عمليًا (هل وصل ما يلزم فعليًا للنموذج؟).
- **Must-Not-Miss Failure Rate**: نسبة الحالات التي غاب عنها **أي** عنصر من `must_not_miss`
  عن السياق النهائي — يُقاس منفصلًا عن Must-Find Recall لأنه يمثل إخلالًا إجرائيًا لا نقص جودة.
- **Unique Principle Recall**: نفس Must-Find Recall لكن بعد تطبيق مِبصَم P0-4 (حتى لا يُحسب
  نجاحًا وهميًا لو نجت نسخة مكررة من مبدأ بينما غابت النسخة "الصحيحة" المقصودة بالمعرّف تحديدًا
  — عمليًا: النجاح يُحسب بمطابقة المضمون لا المعرّف الحرفي فقط، عبر نفس بصمة `_draft_norm_ar`).
- **Survival Rate Trace** (لكل `must_find` على حدة، آلي الاستخراج من نفس حقول `_attr` القائمة):
  `retrieved (في per_axis/bundles/…) → نجا من P0-4 dedup → نجا من caps_n → نجا من Stage-2
  dedup → نجا من قصّ الميزانية → دخل السياق النهائي`. مثال العمل المطلوب (من طلب المالك):
  `100→98→96→94→93→89→89` — كل انخفاض يُنسب لمرحلة بعينها من الأعمدة القائمة فعلًا في `_attr`
  (`principle_precap_dup_dropped_ids`, `cap_dropped_ids`, `stage2_dup_dropped_ids`,
  `budget_dropped`) دون الحاجة لأي حقل جديد — **البنية الحالية تكفي لهذا التتبع بالفعل**، بشرط
  أن تُحسب المقاييس من `draft_evidence.attrition` (أو من نداء مباشر لنفس دوال الاسترجاع دون
  توليد) لا من إعادة بناء الحالة يدويًا.
- **Failure Classification** (لكل `must_find` غائب): يُصنَّف آليًا من الحقل الذي يظهر فيه أولًا
  في قوائم الإسقاط أعلاه، إلى واحدة من: `query-understanding` (لم يظهر في `per_axis` إطلاقًا —
  فشل تفكيك المحاور)، `scope` (فلتر فرع/موضوع صلب استبعده)، `candidate-generation` (لم تُرجعه
  أي قناة من الثمانية)، `dedup` (سقط في P0-4 أو Stage-2 خطأً)، `cap` (سقط بـ`caps_n`)،
  `reranking` (كان حاضرًا قبل المرتِّب وغاب بعده — يحتاج فحص تصادم اسمي)، `budget`
  (`budget_dropped`)، `temporal` (النص الصحيح مُلغى/مُستبدَل ولم يُحدَّث السؤال)، `dependency`
  (بوابة `_GATES` لم تُطلَق رغم انطباق الحالة).

**الخطوة العملية التالية (بعد وصول battery.json الحي):** بناء `tools/gold_metrics.py` يستدعي
دوال الاسترجاع الحقيقية من `admin.app` مباشرة (كما يفعل `battery_run.py` أصلًا) لكل حالة في
`retrieval_gold_set.json`، يلتقط `_attr`-المكافئ لكل حالة، ويُخرج تقريرًا بكل المقاييس أعلاه —
**بلا أي نداء توليد حقيقي**، متوافقًا مع تعليمات المالك الصريحة لهذه الجولة.

## G) False No-Source — التصميم (P1-3)

المقياس المطلوب: كم مرة **يسمح النظام** بإجابة تُفصح عن غياب مصدر بينما مصدر مناسب كان موجودًا
فعليًا في القاعدة وكان بالإمكان استرجاعه؟ هذا ليس نفس "الإفصاح الصادق" (وهو سلوك مرغوب حين
يكون المصدر غائبًا فعلًا) — بل حالة فرعية خطيرة: **الإفصاح كاذب** لأن السبب استرجاع لا غياب حقيقي.

**تصميم القياس (بلا تطبيق كامل الآن — تصميم يُمكِّن قياسًا آليًا مستقبلًا):**
1. **الطبقة الآلية القائمة فعلًا وتصلح أساسًا مباشرًا:** `_audit_answer`'s "إعلان الغياب الكاذب"
   (موثق في §6 — يفحص إن كانت المادة المعلَن غيابها حاضرة فعليًا **في السياق نفسه** بنافذة مقصوصة
   عند حد الجملة). هذا يغطي فئة واحدة فقط: الغياب الكاذب عن **السياق المُرسَل فعليًا**، لا الغياب
   عن **القاعدة كاملة**.
2. **الفئة الأوسع والأخطر معرفيًا (غير مغطاة بعد):** حالات إفصاح المسودة عن غياب مصدر **وهو غائب
   عن السياق المرسَل، لكنه موجود في القاعدة ولم يصله الاسترجاع أصلًا** — هذا بالضبط ما تعالجه
   `_gap_search`/`_gap_confirmed_art` جزئيًا (§17)، لكنها تُصحِّح الحالة بعد وقوعها (تعمل على
   المسودة الأولى وتحاول الجلب)، ولا **تقيس** بشكل منهجي متى فشلت (أي: هل يبقى إفصاح صادق حقًا
   بعد محاولة `_gap_search`، أم أن مصدرًا مناسبًا كان موجودًا وأخطأه البحث الدلالي أيضًا؟).
3. **التصميم المقترح للقياس الآلي المستقبلي:** لكل حالة Gold Set تحمل `must_find` معروفًا مسبقًا
   (بشريًا)، شغّل خط الاسترجاع كاملًا **بلا توليد**، وافحص: هل `must_find` غاب عن السياق النهائي؟
   إن غاب، هذه **بالتعريف** حالة "كان يمكن أن تُنتج False No-Source لو صِيغ السؤال بلغة إفصاح" —
   لا حاجة لتوليد فعلي لقياسها، لأنها مكافئة تمامًا لـ"Must-Find Recall الفاشل" في القسم أعلاه.
   القياس الحقيقي الإضافي المطلوب فقط عند التوليد الفعلي (لاحقًا، خارج نطاق القراءة-فقط لهذه
   الجولة): من بين الحالات التي غاب فيها `must_find` عن السياق، **كم مرة أنتجت المسودة فعلًا
   عبارة إفصاح صريحة** (`_GAP_RE`) بدل السكوت التام أو الاختلاق؟ هذا يحتاج توليدًا حقيقيًا واحدًا
   لكل حالة سلبية — مؤجَّل عمدًا حتى تُعتمد أرقام P1 الاسترجاعية أولًا، بلا نداء إضافي الآن.

**الخلاصة:** لا حاجة لبنية جديدة لقياس "متى حدث الشرط الذي **يمكن** أن يُنتج False No-Source" —
هي بالضبط Must-Find Recall المعكوس، قابلة للحساب من بيانات الاسترجاع وحدها. الحاجة الفعلية
لأداة جديدة تقتصر على القياس النهائي (هل أفصح النموذج فعلًا أم اختلق؟) وهذا يتطلب توليدًا حقيقيًا
محدودًا يُخطَّط له في جولة تالية بعد اعتماد P1، لا الآن.

## H) فصل البحث عن التوليد (P1-6) — ✅ نُفِّذ بموافقة صريحة من المالك (2026-09-10)

**الوضع السابق:** `_draft_run` دالة واحدة ضخمة تُنفّذ الاسترجاع بالكامل ثم تستدعي `draft_create`
(توليد حقيقي) ثم `_audit_answer`/`_crosscheck_answer` (نداءا نموذج إضافيان) ثم `_gap_search`
(قد تستدعي توليدًا إضافيًا عبر جولة تنقيح) — كل هذا داخل جسم دالة واحد، فلا توجد نقطة واحدة
يمكن استدعاؤها لبناء السياق المسترجَع فقط دون تحمّل كلفة/زمن التوليد.

**القرار:** عُرض هذا المقترح على المالك عبر `AskUserQuestion` (بديلين: تعديل الكود بأمان الآن،
أو الاكتفاء بعدد محدود من نداءات `_draft_run` حقيقية كل مرة) — واختار **"عدّل الكود بأمان
أولًا"** صراحة. نُفِّذ فورًا بنفس الانضباط الكامل.

**التنفيذ (`admin/app.py.bak_buildctx_20260910_202609`):** استخراج **حرفي بالفهرس** (لا إعادة
كتابة يدوية) للكتلة الكاملة "بناء الاستعلام ← البحث الكثيف ← إزالة تكرار P0-4 ← سقف Stage-1 ←
الحزم/الاستشهاد المباشر/الفصول/المعجمي/XREF ← جلب النصوص ← الشقيقات/prin-xref ← المرتِّب
المتقاطع ← الترتيب النهائي ← الميزانية ← بناء `context` ← قاموس `_attr`" (238 سطرًا، من
`admin/app.py` مباشرة عبر مرساتين فريدتين، لا تحويل يدوي) إلى دالة مستقلة
`_draft_build_context(client, inp, facts_ret) -> dict` تُرجع `{context, attr, seen, hits,
subqueries, bundle_added, direct_added, chap_added, lex_added, sources_used}`. `_draft_run`
تستدعيها الآن ثم تُكمل (توليد، تدقيق، تدقيق متقاطع) بصفر تغيير سلوكي.

**التحقق قبل أي لمسة للخادم (صفر نداء توليد حقيقي طوال الاعتماد):**
1. **مطابقة بايت-لبايت:** الكتلة المستخرَجة بالفهرس من إعادة بناء محلية للملف الحي (منقولة
   حرفيًا من مسبار قراءة حي لـ`admin/app.py`) طابقت الكتلة الأصلية 100% (12,958 حرفًا، 238 سطرًا).
2. **فحص اعتماد متغيرات آلي (AST):** برنامج تحليل ثابت يجمع كل الأسماء المُسنَدة داخل الكتلة
   المستخرَجة وكل الأسماء المستعملة بعدها في بقية `_draft_run`، تحقق أن كل اسم مطلوب بعد نقطة
   القطع (`context, _attr, seen, hits, subqueries, bundle_added, direct_added, chap_added,
   lex_added`) حاضر في القاموس المُعاد. الأسماء التي بدت مشبوهة أول الأمر (`_a, _t, _p, _sc,
   oid`) ثبت — ببرهان AST إضافي، لا افتراض — أنها إعادة ربط محلية داخل حلقات `for` لاحقة في
   `_draft_run` نفسها، لا اعتمادًا حقيقيًا على الكتلة المنقولة.
3. **صمّام تكرار آمن:** فحص `"_draft_build_context" in src` قبل أي تعديل — تشغيل ثانٍ بالخطأ
   يتوقف بأمان بلا إفساد بنية الملف (بخلاف عثرة `.bak_restructure`/`.bak_style` التاريخية).

**الاعتماد الحي:** نسخة احتياطية ← مرساة (`ANCHOR_COUNTS start=1 end=1 def=1 return=1`) ← فحص
صياغي ← استيراد حي يتحقق من توقيع الدالة الجديدة (`inspect.signature`) ← إعادة تشغيل ←
`whoami`=401. اعتُمد حيًّا: `DONE_BUILDCTX`. **الفائدة الفورية:** `tools/survival_metrics.py`
يستدعي هذه الدالة مباشرة على كل حالات Gold Set لحساب قمع النجاة الكامل (§C-F) — بلا أي نداء
توليد حقيقي، وبمصدر حقيقة واحد لخط الاسترجاع (لا نسخة موازية معرَّضة للانحراف كما كانت حالة
`battery_run.py` تاريخيًا).

## I) Must-Find Budget Loss (P1-7)

بعد بناء `tools/gold_metrics.py`، الرقم الأهم عمليًا ليس "كم عنصرًا أُسقط بالميزانية إجمالًا"
(رقم موجود بالفعل عبر `budget_dropped_count`) بل **كم من هذه الإسقاطات كانت فعليًا `must_find`
معروفًا مسبقًا** — أي: هل الميزانية تُسقط ضجيجًا هامشيًا أم مصادر حاكمة فعلية؟ هذا يُحسب مباشرة
بمقارنة `budget_dropped` (الحقل القائم، يحمل المعرفات) مع قائمة `must_find` لكل حالة Gold Set —
لا حاجة لأي حقل جديد، فقط تقاطع بيانات موجودة بالفعل. **لن تُرفَع الميزانية قبل حساب هذا الرقم
على حالات Gold Set فعليًا — بانتظار بناء الأداة وتشغيلها.**

---

## الجولة الثانية (2026-09-10 مساءً) — بعد مراجعة خارجية (ChatGPT) لتقرير P1 الأول

المالك نقل نقدًا تفصيليًا من ChatGPT لتقرير القسمين C-F/H أعلاه، مع تعليمات تنفيذية صريحة (حُفظت
حرفيًا في محادثة الجلسة). تبنّيت معظمها كتحسين هندسي حقيقي، وعدّلت بندًا واحدًا بحكمي المستقل —
موثَّق في القسم M أدناه. **لا شيء في هذه الجولة غيّر منطق الاسترجاع/الترتيب/الميزانية —
قياس وتصنيف وتصميم فقط، كما أُمر صراحة.**

## J) استقرار gs-0015 — خمس تكرارات مستقلة + بيانة سادسة عرضية

بدل تصديق فشل مفرد أو نجاح مفرد (الدرس المعياري القائم في CLAUDE.md §10)، شُغِّل
`_draft_build_context` **خمس مرات مستقلة متتالية** على سؤال gs-0015 نفسه (كفاية رأس المال
المصرفي)، بتصفير كاش الفهرس بين كل تكرار لمحاكاة طلب مستقل حقيقي — بلا أي نداء توليد:

| تكرار | حاضر في hits_pool | القناة المعجمية | قناة حتمية (استشهاد/فصل) | نجا للسياق النهائي | dense_union_count |
|---|---|---|---|---|---|
| 1 | ✓ | ✗ | ✗ | ✓ (score=0.5533, rank=31) | 89 |
| 2 | ✗ | ✗ | ✗ | ✗ | 87 |
| 3 | ✓ | ✗ | ✗ | ✓ (score=0.5448, rank=31) | 86 |
| 4 | ✗ | ✗ | ✗ | ✗ | 85 |
| 5 | ✓ | ✗ | ✗ | ✓ (score=0.5452, rank=31) | 87 |

**النتيجة: حضور 3/5، نجاة 3/5.** بيانة سادسة عرضية (ضمن دفعة `survival_metrics.py` الكاملة
لاحقًا في نفس اليوم) أضافت نجاحًا سادسًا — 4/6 إجمالًا عبر كل الملاحظات المرصودة، متسق إحصائيًا
مع معدل بقاء تقريبي 55-65% لا حالة ثنائية.

**الحكم وفق المعيار الثلاثي المطلوب:** ليس 5/5 نجاحًا (فينتفي `TRANSIENT_SINGLE_EVENT`)، وليس
0/5 حضورًا (فينتفي `CANDIDATE_GENERATION_FAILURE_CONFIRMED`) — الحكم:
**`RETRIEVAL_INSTABILITY_NONDETERMINISM`** مؤكَّد بالبيانات، لا افتراضًا.

**السبب الجذري المرجَّح (من الأدلة المرصودة، لا تخمينًا):**
1. **صفر تغطية حتمية**: `target_in_lexical` و`target_in_deterministic_channel` كانا `False` في
   الخمس تكرارات كلها بلا استثناء — هذا الهدف يعتمد **حصرًا** على القناة الكثيفة (المتجهات)،
   بلا أي شبكة أمان من `_draft_lexical`/`_draft_chap_ids`/`_draft_direct_ids`. أي عنصر بلا
   قناة حتمية مساندة معرَّض بنيويًا لهذا النمط من التذبذب.
2. **تباين `dense_union_count` بين التكرارات** (89→87→86→85→87) رغم أن السؤال والمرفقات
   ثابتان حرفيًا — الاختلاف الوحيد الممكن بين النداءات هو تفكيك Haiku للمحاور
   (`_draft_subqueries`)، الذي يُعيد صياغة/ترتيب محاور بحث مختلفة قليلًا كل مرة (نموذج لغوي
   غير حتمي)، فتتغيّر متجهات التضمين المُرسَلة لكل محور، وتتغيّر معها نتائج Qdrant القريبة من
   حافة القطع. هذا يطابق فئة `Haiku subquery-decomposition variance` المقترحة في نقد المالك
   حرفيًا، وليس سلوك Qdrant التقريبي (ANN) نفسه — إذ لو كان السبب في محرك البحث فقط لتوقّعنا
   ثباتًا أكبر في `dense_union_count` طالما المتجه المُرسَل واحد.
3. **لا دليل على تعادل درجات (score ties) أو غياب ترتيب حتمي في SQL** هنا تحديدًا (بخلاف حادثة
   §15 التاريخية في `_draft_chap_index`) — القناة المتأثرة هنا كثيفة (Qdrant) لا SQL، والمشكلة
   في المُدخَل (المحاور) لا في استقرار قراءة القاعدة.

**لم يُعدَّل أي كود لهذا البند في هذه الجولة — تشخيص بحت كما طُلب صراحة.**

## K) مقاييس الاسترجاع المصحَّحة — صيغة Confirmed/Pending/Worst-case

القياس السابق (Must-Find Recall = 83.3%، 10/12) صحيح رقميًا لكنه **يخلط** فشلًا حتميًا مؤكَّدًا
بحالة غير مستقرة إحصائيًا في رقم واحد بلا توسيم — وهذا بالضبط ما رُفض. الصيغة المصحَّحة (من
تشغيل `survival_metrics.py` الكامل، 15/15 حالة، `must_find` إجمالي=12):

```
حالات مستقرة (فشل حتمي مؤكَّد بمسار غير معتمد على تذبذب المحاور):
  - legis-38-1980-m166   (gs-0013) — فشل: budget (تحليل كامل في القسم M)
  - LEG-20-49-e62d299077f0e672 (gs-0014) — فشل: cap (تحليل كامل في القسم M)

حالة غير مستقرة (مؤكَّدة نظام تذبذب، لا فشل ثابت ولا نجاح ثابت):
  - هدف gs-0015 — RETRIEVAL_INSTABILITY_NONDETERMINISM (القسم J)، ~4/6 نجاة مرصودة تجريبيًا

Confirmed Must-Find Recall (10 عناصر مستقرة الحالة إلى جانب الحالة غير المستقرة الحادية عشرة
والثانية عشرة المستقرتَي النجاح): 9 / 11 = 81.8%  — من أصل 11 عنصرًا ذا حالة نجاح/فشل ثابتة
  (9 نجحت باستقرار، 2 فشلت باستقرار: m166 وLEG-20-49)
Pending/Unstable: 1 (هدف gs-0015 — غير مصنَّف pending عاديًا بل RETRIEVAL_INSTABILITY_NONDETERMINISM
  مؤكَّد إحصائيًا، وليس مجرد بانتظار قياس إضافي)
Worst-case Recall (احتساب الحالة غير المستقرة فشلًا): 9 / 12 = 75.0%
Best-case Recall (احتسابها نجاحًا، الأقرب لسلوكها الأغلبي المرصود): 10 / 12 = 83.3%
تقدير القيمة المتوقَّعة (بمعدل بقاء تجريبي ~60% لهدف gs-0015 عبر 6 تكرارات): 9 + 0.6 = 9.6 / 12
  ≈ 80.0% — تقدير احتمالي عبر تكرارات متعددة، لا رقمًا واحدًا "يُحقَّق" في تشغيل بعينه.
```

**الفارق الجوهري عن التقرير الأول:** الرقم 83.3% (10/12) لم يكن خطأً حسابيًا لكنه كان يعرض
**نتيجة تشغيل واحد فقط**، وذاك التشغيل الواحد كان أحد التشغيلات التي نجا فيها هدف gs-0015
صدفة — فأخفى أن أحد عنصرَي "النجاح" العشرة غير مستقر فعليًا. **القاعدة المعتمدة من الآن
فصاعدًا لأي تقرير قادم:** أي `must_find` لم يُختبَر بتكرار كافٍ (٥ تكرارات مستقلة على الأقل)
يُصنَّف صراحة كـ"غير مؤكَّد الاستقرار" ولا يُدمَج في بسط/مقام رقم واحد بلا توسيم.

## L) نسب المرشحين (Candidate Provenance) — التصميم والتنفيذ (2026-09-10، `.bak_provenance`)

**نُفِّذ حيًّا اليوم** (لا تصميم مؤجَّل) — بأمر المالك الصريح («فقط اجعلها قابلة للقياس أولًا»،
بلا أي تحويل لقاعدة قبول صلبة، وهو ما التُزم به حرفيًا: **صفر تغيير في من يدخل السياق أو كيف
يُرتَّب**، إضافة قياس خالص فوق المسار القائم).

**ما أُضيف إلى `_draft_build_context`:**
1. كل مرشح يُسَم عند نقطة دخوله الثماني إلى `hits` بحقل `_source` داخل حمولته:
   `dense`/`bundle`/`direct`/`chapter`/`lexical`/`xref`/`sibling_xref`/`principle_xref`.
2. عند إعادة كتابة الدرجات بالمرتِّب المتقاطع، يُحقَن `_pre_rerank_score` (الدرجة قبل الدمج)
   و`_reranker_raw` (الدرجة الخام من المرتِّب، أو `None` إن لم يدخله العنصر أصلًا) — بلا تغيير
   في قيمة الدرجة النهائية المستعملة فعليًا للترتيب/الميزانية.
3. بعد اكتمال الترتيب النهائي (`ordered`)، تُبنى قائمة `provenance` (تُعاد في `ctx` فقط، **بلا
   تخزين قاعدة بيانات**): `{object_id, object_type, source, pre_rerank_score, reranker_raw,
   final_score, rank, admitted}` لكل مرشح.
4. إصلاح مصاحب: `budget_dropped` (كان مقصوصًا `[:60]`) صار ثلاثة حقول: `budget_dropped_count`
   (كامل دومًا)، `budget_dropped_by_type` (تجميع كامل دومًا)، `budget_dropped_sample` (عيّنة
   محدودة `[:60]` للعرض/اليوميات) + `ctx['budget_dropped_full']` (القائمة الكاملة بلا حد،
   Python فقط، لأدوات التحليل).

**التحقق قبل النشر:** 10 مراسٍ `count()==1` على نسخة مُعاد بناؤها حرفيًا من الخادم، ثم محاكاة
تنفيذية كاملة (لا فحص صياغي فقط) بمكوّنات وهمية تغطي القنوات الثماني + سيناريو 130 عنصر إسقاط
ميزانية متعمَّد (يحاكي حجم `gs-0013` الحقيقي 251) يثبت أن 70 عنصرًا كانت ستختفي خلف قصّ `[:60]`
القديم وتظهر الآن كاملة، مع تحقق صريح أن كل قناة تُسمّى بمصدرها الصحيح وأن العناصر المثبَّتة 0.99
(استشهاد مباشر/فصل حاكم) لا تدخل المرتِّب فتبقى `reranker_raw=None` كما هو متوقَّع منطقيًا.
نُشرت وأُعيد تشغيل الخدمة، `whoami`=401 مؤكَّد.

**حدّان مُعلَنان صراحة (لم يُحلَّا هذه الجولة):**
1. **إسناد قناة واحدة فقط لكل مرشح:** بنية إزالة التكرار القائمة (`picked`) تمنع أي عنصر من
   دخول `hits` مرتين أصلًا (أول قناة تطلبه تفوز)، فـ"تعدد القنوات لنفس المرشح" غير قابل للرصد
   ببنية اليوم دون تعديل إضافي — مؤجَّل صراحة لمتابعة **P1.6** منفصلة، لا مُنفَّذ الآن.
2. **لا تمييز كودي بين "تطابق فصل موضوعي" و"بوابة `_GATES` إلزامية"** — كلاهما يمران عبر
   `_draft_chap_ids` نفسها ويُسَمَّيان `chapter` في `provenance` دون تفريق (خلافًا لمثال JSON
   المقترح الذي افترض قيمة `gate` مستقلة) — يحتاج تفكيك `_draft_chap_ids` داخليًا، غير منفَّذ.

## M) نسب `gs-0013`/`legis-38-1980-m166` الكامل + التحقيق في الترتيب (بندا التقرير 4 و5)

**السؤال:** كيف دخلت المادة تحديدًا، ولماذا جاءت بهذا الترتيب المتأخر؟ — إجابة كاملة من بيانات
`provenance` الحية، لا تخمين:

```
source=bundle | rank=144 | pre_rerank_score=0.0 | reranker_raw=0.0109 | final_score=0.0054 | admitted=False
```

**التفسير الكامل، مرحلة بمرحلة:**
1. **لم تُفشل قناة الاكتشاف (Discovery) إطلاقًا** — المادة دخلت عبر `_draft_bundles` (الحزمة
   الحاكمة لسؤال أمر الأداء)، لا عبر البحث الدلالي. هذا يعني أن آلية **SCOPE** (الاعتراف بأنها
   تخص مسألة "أمر أداء") عملت فعليًا — البوابة الآلية عرفت أنها معنية بهذه المسألة، وهذا بالضبط
   ما تفحصه طبقة `_GATES`/الفصول الحاكمة.
2. **لكن موضعها داخل ترتيب الحزمة الداخلي ≥18** — فأخذت الدرجة الذيلية (`_bscore=0.0`) بدل
   الدرجة العالية (`0.85`) المخصَّصة لأول 18 عنصرًا في الحزمة. هذا يعني أن **19 مادة أخرى على
   الأقل في حزمة "أمر أداء" الحاكمة تسبق م166 بالترتيب الداخلي** — سؤال مشروع (هل ترتيب الحزمة
   نفسه يضع المادة الجوهرية الأولى بعيدًا؟) لكنه **خارج نطاق هذه الجولة صراحة** (لا تغيير سلوك
   استرجاع) — يُسجَّل كمرشح فحص لجولة لاحقة، لا يُمس الآن.
3. **دخلت المرتِّب المتقاطع** (لأن درجتها <0.99) وحصلت على درجة خام منخفضة جدًا: `0.0109`. **هل
   هذا طبيعي؟** بمقارنتها بعناصر أخرى من `budget_dropped_sample` لنفس الحالة ونفس النوع
   ("تشريع")، أعلى الدرجات المُسجَّلة هناك: `legis-38-1980-m10`→0.2168، `legis-38-1980-m24`→
   0.1591، `legis-35-1978-m25`→0.1018 — أي أن م166 (0.0054 نهائي، مبني على 0.0109 خام) أدنى
   بفارق كبير من عناصر أخرى وصلت لمنطقة الإسقاط بالميزانية نفسها. **هذا نمط لافت يستحق فحصًا
   مستقلًا** (غير مُنفَّذ هذه الجولة، خارج نطاق التفويض): نص م166 على الأرجح قصير وشكلي (شرط
   ميعاد إجرائي) بخلاف م10/م24 (نصوص أطول وأكثر تفصيلًا موضوعيًا) — فرضية "المرتِّب المتقاطع
   يُخفِّض تلقائيًا قيمة النصوص القانونية القصيرة/الإجرائية أمام نص سؤال طويل مركّب" **معقولة من
   نقطة بيانات واحدة، غير مثبتة إحصائيًا بعد**. التوصية: قياس محدود (ليس تنفيذًا الآن) — مقارنة
   درجات المرتِّب لعيّنة من مواد إجرائية قصيرة معروفة الأهمية مقابل مواد موضوعية طويلة، بنفس
   الأسئلة، لتأكيد أو نفي التحيّز قبل أي قرار تصميم.
4. **السبب النهائي الظاهر (Terminal cause): ميزانية** — رتبتها 144 جاءت بعد استنفاد ميزانية
   التشريع (48000 حرف) عند من سبقها بدرجات أعلى.

**التصنيف وفق التصنيف المقترح (بلا تحويله إلى قيمة برمجية جديدة هذه الجولة — انظر تعديلي الوحيد
في القسم أدناه):**
- **Candidate Generation: لم يفشل** — وصلت فعليًا عبر `bundle`.
- **Ranking: فشل** — درجة ذيلية صفرية + درجة مرتِّب منخفضة جدًا (نمط يستحق فحصًا مستقلًا).
- **Budget: السبب التقني الظاهري المباشر** — نفدت الميزانية عند الرتبة 144.
- **فجوة معمارية (لا قيمة `classify()` رسمية بعد):** المادة معترَف بانتمائها لمسألة "أمر أداء"
  إجرائيًا (عبر الحزمة)، لكن بمجرد دخولها `hits` تُعامَل كمرشح دلالي عادي يتنافس على نفس الميزانية
  مع عشرات المرشحات الأدنى أهمية قانونية — لا إشارة تحفظ خصوصيتها كسند حاكم عبر مرحلتَي الترتيب
  والقبول. هذا يطابق حرفيًا وصف "فشل قبول الدليل" (Evidence Admission Failure) المقترح.

**مقارنة: `LEG-20-49-e62d299077f0e672` (gs-0014 — الخدمة الوطنية العسكرية):**
```
غائب تمامًا عن ordered/provenance — سقط بـ Stage-1 caps_n قبل بناء hits أصلًا
```
فشل **أبكر وأشد** من حالة م166: لم يصل حتى إلى مرحلة الترتيب بالمرتِّب المتقاطع، بل أُقصي عند
سقف الـ20 مرشحًا الأوائل بالدرجة الكثيفة الخام لكل نوع (`caps_n["تشريع"]=20`) — من أصل 44
مرشح تشريع خام في هذه الحالة (`dense_precap_by_label`)، أكثر من نصفها يُقصى بهذا السقف المبكر
بصرف النظر عن أي اعتبار قانوني لاحق. **هذا فشل "Ranking" في أقصى صوره (سقف مبكر بالدرجة الخام
وحدها، قبل أي فرصة للمرتِّب المتقاطع أو الحزم لإنقاذه)** — ولا آلية بوابة/فصل حاكم تعرَّفت على
أهمية هذا العنصر تحديدًا (بخلاف م166 الذي أنقذته الحزمة جزئيًا إلى الرتبة 144 على الأقل).

## N) تقييم هندسي وقانوني لاقتراح Evidence Admission Policy (تصميم بلا تنفيذ)

**قراري المستقل بعد المراجعة (لا تبنٍّ حرفي لما اقترحه النقد الخارجي):** أتفق مع **المبدأ**
(تصنيف الأدلة قبل التنافس على الميزانية أفضل من تنافس أعمى)، لكن أرى **نطاق Tier A كما اقتُرح
أوسع من اللازم وخطِرًا لو طُبِّق كما هو** — وهذا بالضبط تحذير المالك نفسه («لا تجعل كل مرشح من
`_GATES` محميًا»). تفصيل المخاطر:

1. **خطر هندسي — الخلط بين "نطاق البحث" و"سند حاكم":** المقترح الأصلي يضع "مربوط عبر إحالة
   تشريعية صريحة من سند حاكم" و"مادة مذكورة صراحة في السؤال" في نفس الطبقة (Tier A). لكن
   قناتَي `chapter`/`gate` (الموحَّدتين كوداويًا في `_draft_chap_ids`، القسم L أعلاه) تُنتجان
   **حزمًا كاملة من عشرات المواد** (مثال حي: `chap_added` في `gs-0013` = 21 مادة، وفي `gs-0014`
   = 27 مادة) — لو صُنِّف كل ناتج هذه القناة Tier A تلقائيًا، فالنتيجة عمليًا حماية فصول كاملة
   من ضغط الميزانية، وهذا **بالضبط** الخطر الذي حذّر منه المالك حرفيًا. الفارق الجوهري بين
   "نطاق بحث" (Scope Candidate: نص ضمن مدى يستحق البحث فيه) و"سند حاكم" (Anchor: نص له سبب
   قوي محدد للقبول في الملف النهائي) **حقيقي وجوهري** ولا تلتقطه قناة `chapter` وحدها.
2. **خطر قانوني:** قاعدة برمجية "هذا النص يحكم هذه الواقعة دومًا" هي في جوهرها **حكم قانوني
   مُجسَّد في كود** — خطأ فيها أخطر من خسارة مادة في تنافس الميزانية العادي، لأنه إما (أ) يُقحم
   نصًا بصفة "حاكم" في كل إجابة مهما كانت الوقائع الدقيقة، أو (ب) يُغري ببناء قائمة Tier A
   طموحة قبل أن تُثبت البيانات حاجتها — بينما قاعدة المشروع الراسخة (وثّقتها كل جولة تاريخية في
   §6 من CLAUDE.md) هي: **بناء ما تثبته الأرقام لا ما يبدو منطقيًا نظريًا**.
3. **التعديل الذي أُصرّ عليه:** Tier A الأولي يجب أن يقتصر على **الاستشهاد الصريح فقط**
   (`direct_citation`: رقم مادة مذكور حرفيًا في السؤال أو المرفقات، عبر `_draft_direct_ids`/
   `_draft_doc_refs` القائمتين بالفعل) — القناة الوحيدة التي مخاطر الإيجابية الكاذبة فيها شبه
   معدومة (المستخدم كتب الرقم بنفسه). عناصر `chapter`/`gate` تبقى **Tier B** ابتداءً، ولا تُرقَّى
   لـ Tier A إلا لعنصر بعينه (لا الفصل كله) بعد أن يُثبِت القياس فعليًا أنه سقط تكرارًا رغم
   أهميته القانونية المؤكَّدة — تمامًا كما أثبت اليوم `legis-38-1980-m166` نفسه حاجته لذلك عبر
   بيانات حقيقية (القسم M)، لا افتراضًا مسبقًا. هذا يحوّل بناء Tier A إلى **عملية تراكمية مقيسة
   لكل بوابة `_GATES` على حدة** بدل قرار جارف دفعة واحدة.
4. **الطبقات المتبقية من المقترح (B/C) مقبولة كما هي مبدئيًا** — Tier B (أفضل نصوص الفصل الحاكم،
   مبادئ مرتبطة مباشرة، إحالات عالية الصلة) وTier C (كل ما تبقى من مرشحات دلالية/معجمية) لا
   تحمل نفس مخاطر Tier A لأنها لا تزال تتنافس على ميزانية عادية، فقط بترتيب أولوية معلوماتي لا
   قاعدة قبول صلبة.

**الخلاصة: لا اعتماد للتصميم كما طُرح — اعتماد جزئي مُعدَّل لنطاق Tier A فقط، والباقي كما اقتُرح.**
لا تنفيذ في هذه الجولة على أي حال (تصميم فقط، بانتظار مراجعة المالك لهذا التعديل تحديدًا).

## O) ميزانية محجوزة/عامة (Protected + General Budget) — تصميم معماري بلا أرقام

بناءً على Tier A المُعدَّل في القسم N (استشهاد صريح فقط ابتداءً + ترقيات فردية مقيسة لاحقًا):

```
لكل نوع (تشريع/مبدأ قضائي/…):
  ميزانية النوع الكلية (كما هي اليوم، بلا تغيير رقمي الآن)
    ├─ ميزانية محجوزة (Anchor) — تُقبَل منها مرشحات Tier A فقط، بترتيب ثابت (لا تنافس درجات)
    │    ضوابط وجوبية (بنيوية لا رقمية، حتى تُقاس الأرقام لاحقًا):
    │      • سقف عدد مرشحات Anchor لكل مسألة قانونية واحدة (يمنع مسألة واحدة من ابتلاع
    │        الحجز كله — مباشرة يعالج مثال gs-0013 المتعدد المسائل: 5 مسائل فرعية، لا يصح أن
    │        تستأثر واحدة بكل الحجز على حساب البقية)
    │      • سقف عدد مرشحات Anchor لكل مصدر/قانون واحد (يمنع فصلًا واحدًا من الهيمنة)
    │      • إزالة تكرار (بصمة P0-4 القائمة نفسها، تُعاد استعمالها لا تُبنى من الصفر) قبل القبول
    │      • اختيار واعٍ بالتغطية (Coverage-aware): إن تعدد مرشحو Tier A لنفس المسألة، يُفضَّل
    │        تنويع المسائل المغطاة على تكديس عدة مرشحات لمسألة واحدة مغطاة أصلًا
    └─ الميزانية العامة (المتبقية) — تُقبَل منها مرشحات Tier B/C بنفس آلية التنافس بالدرجة
         القائمة اليوم بلا أي تغيير في منطقها
```

**صمّام أمان بنيوي (لا رقمي):** إن استهلك الحجز جزءًا كبيرًا جدًا من ميزانية النوع (حالة نادرة
متوقَّعة إن ضعفت الضوابط أعلاه)، يُسجَّل تحذير تعقّبي (نمط `[draft] anchor-budget-warning:`
مماثل لأنماط التسجيل القائمة) بدل تدهور صامت للميزانية العامة — **قرار الحد الرقمي نفسه مؤجَّل
حتى تُقاس بيانات حقيقية**، تطبيقًا لتعليمات المالك («لا أطلب تحديد الأرقام الآن»). لا تنفيذ.

## P) نموذج أولي لفاحص التغطية (Coverage Checker MVP) — تصميم لـ P2، بلا تنفيذ

المشكلة التي يحلها بدقة (أثبتتها `gs-0013` اليوم حرفيًا): النظام **عرف** أن مسألة "أمر أداء"
تستوجب مادة حاكمة (البوابة أطلقت الحزمة فعليًا) لكنه **فقدها لاحقًا في المنافسة على الميزانية**
دون أن تتواصل طبقة "الاعتراف بالمسألة" (Scope) مع طبقة "بناء السياق" (Context Builder) حول
نتيجة النجاة النهائية.

**تصميم النموذج الأولي (بيانات القسم L/M تكفي لبنائه اليوم كأداة عرضية غير منتِجة، بلا حاجة
لتغيير الإنتاج):**
```
لكل مسألة يتعرَّف عليها _GATES/الفصول الحاكمة في هذا الطلب:
  السند المرشَّح (من الحزمة/الفصل) → وُجد في hits؟ (نعم/لا، من provenance)
                                    → نجا للسياق النهائي؟ (admitted: نعم/لا)
                                    → حالة التغطية: COMPLETE / INCOMPLETE
```
مثال حي من بيانات اليوم (`gs-0013`):
```
issue: أمر الأداء
candidate authority: legis-38-1980-m166
candidate found: نعم (source=bundle, rank=144)
survived final context: لا (admitted=False)
coverage_status: INCOMPLETE
```
**نطاق P1 (الآن، غير منفَّذ هذه الجولة أيضًا لكنه رخيص وجاهز للبناء التالي):** أداة عرضية
(بنمط `survival_metrics.py`) تربط `must_find`/`relevant_issues` في Gold Set بنتيجة `provenance`
فتُخرج تقرير **تغطية لكل مسألة** لا رقم استرجاع مسطّح واحد فقط — يكشف حالات كـ`gs-0013` حيث
4 من 5 مسائل فرعية مكتملة التغطية بينما "أمر الأداء" تحديدًا ناقصة، وهذا أدق تشخيصيًا من
"4/5 = 80%" بلا تفصيل.
**نطاق P2 (لاحقًا، يعتمد على القسمين N/O أولًا):** فحص فعلي **قبل** بدء التوليد يمنع/يُعلِّم
الجولة حين تكون مسألة معترَف بها (`_GATES`) ناقصة التغطية — يستوجب وجود تصنيف Tier A/الميزانية
المحجوزة أولًا (القسمان N/O) ليكون له معنى عملي (بلا Tier A، "لا تُبدأ الصياغة" لكل فصل حاكم لم
ينجُ بالكامل قد يوقف الصياغة على ضجيج لا أهمية حقيقية).

**تمييز أربعة مفاهيم — لا تُدمَج في قيمة منطقية واحدة مبكرًا (كما طُلب حرفيًا):**
- **Search Coverage**: هل بُحث في كل النطاقات المطلوبة؟ (اليوم: نعم غالبًا — `_GATES`/الفصول
  تُطلَق بمجرد التعرّف اللفظي/الدلالي على المسألة).
- **Evidence Coverage**: هل توجد سلطة كافية لكل مسألة في القاعدة أصلًا؟ (سؤال عن محتوى القاعدة،
  لا الاسترجاع — خارج نطاق أي رقعة برمجية).
- **Context Survival**: هل وصلت تلك السلطة فعليًا لسياق التوليد النهائي؟ (هذا بالضبط ما يقيسه
  `survival_metrics.py` اليوم، وما فشل فيه `gs-0013`).
- **Search Exhaustion**: متى يحق للنظام قول "غير موجود في المصادر" بثقة؟ (يتقاطع مع تصميم
  False No-Source في القسم G أعلاه، ولا يُحسَم إلا بعد Context Survival مؤكَّدة الفشل فعلًا).

## Q) تحسين تعقّب الإسقاط (Attrition Telemetry) — منخفض المخاطر، نُفِّذ اليوم

مُغطّى تفصيلًا في القسم L (نفس الرقعة). ملاحظة تصميمية إضافية: `provenance` و`budget_dropped_full`
**لا يُخزَّنان في `draft_evidence.attrition`** (يبقى العمود بحجمه الحالي، `_attr` وحدها تُخزَّن
كما كانت مع الحقول المُصحَّحة الثلاثة الجديدة فقط) — القائمتان الكاملتان تعيشان فقط في `ctx`
المُعاد من `_draft_build_context` لحظيًا، تُستهلَكان مباشرة بأدوات كـ`survival_metrics.py` دون
أي تخزين دائم. **قرار مؤجَّل بوعي:** لو احتاج تشخيص حوادث إنتاج مستقبلية (لا Gold Set) لعيّنة
`provenance` محفوظة، يحتاج قرارًا صريحًا لاحقًا بشأن تخزين نسخة محدودة منها — غير مطروح الآن.

## R) خطة توسعة Gold Set إلى 50 حالة (تخطيط فقط — لا تنفيذ هذه الجولة)

**الشروط المسبقة الأربعة التي اشترطها المالك — حالتها الآن:**
1. حسم استقرار `gs-0015` ✅ (القسم J — مُشخَّص تشخيصًا كاملًا؛ "الحسم" هنا يعني التوصيف لا
   الإصلاح، فالإصلاح خارج نطاق هذه الجولة).
2. تتبّع نسب المرشحين ✅ (القسمان L/M — منفَّذ حيًّا ومُستعمَل فعليًا في التشخيص).
3. توضيح تصنيف الفشل ✅ جزئيًا — التوصيف السردي الكامل (القسم M) منجز؛ **لم تُضَف قيمة
   `classify()` برمجية جديدة (`evidence_admission_failure`) هذه الجولة** — قرار مقصود (انظر
   تبريري في القسم N: بيانة واحدة لا تكفي لتثبيت تصنيف عام جديد في أداة قياس، وأفضّل جمع 2-3
   حالات إضافية مؤكَّدة بنفس النمط قبل ترسيخه كقيمة تصنيف رسمية دائمة — هذا اجتهادي المستقل).
4. تصميم Evidence Admission Policy ✅ (القسم N — مُقدَّم بتقييم هندسي/قانوني وتعديل مُصرّ عليه،
   لا تبنٍّ حرفي؛ بانتظار رد المالك على التعديل تحديدًا).

**الخطة (تنفيذ لاحق، بعد موافقة المالك على القسمين N/التصنيف):**
- **50 حالة "صعبة" لا 35 سهلة لتضخيم العدد** — توزيع أدنى مقترح عبر: `multi_issue` (مسائل
  متعددة بسؤال واحد، كـ`gs-0013` نفسها)، `multi_law`، `legal_dependency`، `temporal` (نص
  ملغى/معدَّل)، `exception` (استثناء صريح)، `cross_reference`، `conflicting_authorities`
  (سوابق متعارضة ظاهريًا)، `previous_vs_current_legislation`، `procedure` (إجراء سابق وجوبي)،
  `judicial_principle`، `natural_language_mismatch` (صياغة عامية لا قانونية)، و**ضوابط سلبية**
  (Negative Controls).
- **الضوابط السلبية — منهجية جديدة مطلوبة صراحة:** أسئلة يُتحقَّق **بشريًا** أنها غير قابلة
  للإجابة من القاعدة بثقة تامة (كسؤال عن قانون أجنبي، أو مسألة صدر فيها نص بعد آخر تحديث للقاعدة)
  — لكل حالة سلبية، عند تنفيذها لاحقًا بتوليد حقيقي (خارج نطاق هذه الجولة)، تُقاس نتيجتان
  منفصلتان لا واحدة: **False No-Source Rate** (النظام يزعم الغياب خطأً رغم وجود سند فعلي —
  مغطّى جزئيًا بالقسم G القائم) و**False Source-Sufficiency Rate** (النظام يزعم كفاية الأدلة
  خطأً رغم عدم وجود سند حقيقي كافٍ — مقياس جديد، لم يكن موجودًا قبل هذا النقد، ويحتاج توليدًا
  حقيقيًا للقياس، مؤجَّل).
- **قاعدة حاكمة ملزمة، مؤكَّدة صحتها اليوم بالفحص المباشر (لا افتراضًا):** `retrieval_gold_set.json`
  لا يُستورَد من `admin/app.py` ولا `mcp/server.py` ولا أي مسار إنتاج — فقط أداتا `gold_metrics.py`
  و`survival_metrics.py` تقرآنه، وكلتاهما مستقلتان تمامًا عن مسار الإنتاج (تحقَّق بالبحث المباشر
  في شجرة `tools/` قبل كتابة هذا السطر). **هذا الفصل يبقى شرطًا دائمًا لكل توسعة قادمة** — لا
  معرّف Gold Set يُستعمَل كقاعدة أو استثناء في أي كود إنتاج مهما كانت الحالة "معروفة الحل".
